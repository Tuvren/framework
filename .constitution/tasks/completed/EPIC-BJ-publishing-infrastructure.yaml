id: BJ
name: Publishing Infrastructure (KRT)
kind: product
capabilities: []
depends_on: []
scope:
  paths: [typescript/runtime/src/index.ts, typescript/runtime/src/lib/create-tuvren.ts, typescript/sdk/src/index.ts, typescript/runtime/package.json, typescript/sdk/package.json, typescript/runtime/README.md, spec/conformance/engine/plans/runtime-api-batteries-included.json, typescript/host/repl/package.json, typescript/host/repl/src/cli.ts, typescript/host/repl/src/lib/repl-types.ts, typescript/host/repl/src/lib/repl-replay.ts, typescript/host/repl/src/lib/proof-extension.ts, typescript/host/repl/src/lib/repl-config.ts, typescript/host/repl/src/lib/repl-scenarios-support.ts, typescript/host/repl/src/lib/repl-builtin-tools.ts, typescript/host/repl/src/lib/repl-scenarios.ts, typescript/host/repl/src/lib/repl-host.ts, typescript/host/repl/src/lib/repl-provider.ts, typescript/host/repl/src/lib/repl-transcript.ts, typescript/host/repl/src/lib/repl-live-output.ts, typescript/host/repl/src/lib/repl-headless-mode.ts, typescript/host/repl/src/lib/repl-shell.ts, tools/scripts/host-boundary-gate.ts, tools/scripts/check.ts, tools/scripts/verify.ts, package.json, typescript/core/src/telemetry/index.ts, typescript/core/src/telemetry/telemetry-destination.ts, typescript/runtime/src/lib/runtime-core-telemetry.ts, spec/core/authority-packet.json, spec/conformance/telemetry/plans/framework-operational-telemetry.json, spec/conformance/telemetry/scenarios/operational-telemetry-scenarios.json, tools/conformance/harness/run.ts, typescript/core/src/capabilities/index.ts, typescript/core/package.json, typescript/backend-memory/package.json, typescript/runners/react/package.json, typescript/providers/bridge-ai-sdk/package.json, typescript/streaming/core/package.json, typescript/tools/mcp-client/package.json, typescript/telemetry/otel/package.json, files, tools/scripts/release-lane.ts, tools/scripts/release-check.ts, ".changeset/config.json"]
  tech_spec_patchable: []
estimated_diff_lines: 0
tickets:
  - id: KRT-BJ001
    title: SDK Composition-Tier Migration (ADR-057)
    type: Chore
    effort: 5
    depends_on: []
    category: Tech-Debt
    scope:
      in: [typescript/runtime/src/index.ts, typescript/runtime/src/lib/create-tuvren.ts, typescript/sdk/src/index.ts, typescript/runtime/package.json, typescript/sdk/package.json, typescript/runtime/README.md, spec/conformance/engine/plans/runtime-api-batteries-included.json]
      out: ["typescript/host/repl/**", "typescript/core/src/**"]
    verification:
      command: "bun run nx run sdk:typecheck && bun run nx run framework-runtime:typecheck && bun run conformance"
      expected: exit 0
    stop_conditions: ["STOP if any intermediate commit state leaves `runtime` peer-depending on `sdk` while `sdk` depends on `runtime` (dependency cycle); land the peer-dep removal and the new dependency edge in the same commit.", "STOP if a host-facing contract type is found to originate only in `@tuvren/runtime` (contradicting the ADR-057 audit) rather than already existing on a `@tuvren/core` subpath; escalate instead of inventing a new home for it."]
    description: "Perform the coordinated ADR-057 migration as one unit: relocate createTuvren and its curated re-exports from @tuvren/runtime's root export to @tuvren/sdk's root export; correct the currently inverted dependency edge between the two packages; retire the ADR-040 string-kind backend/runner shorthands so CreateTuvrenOptions accepts constructed instances/factories only (backend, runner required, no \"memory\"|\"sqlite\"|\"postgres\" tags, no implicit \"react\" runner default); strip @tuvren/runtime's public surface of curated @tuvren/core re-exports so it carries no host-facing convenience aliases; mark @tuvren/runtime as an internal engine package in its README and package.json description; move the runtime-api-batteries-included conformance check set's target surface from @tuvren/runtime to @tuvren/sdk with assertions updated for the instances-only option shape"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ002
    title: Reference Host Re-Point to the Host Import Contract
    type: Chore
    effort: 3
    depends_on: [KRT-BJ001]
    category: Tech-Debt
    scope:
      in: [typescript/host/repl/package.json, typescript/host/repl/src/cli.ts, typescript/host/repl/src/lib/repl-types.ts, typescript/host/repl/src/lib/repl-replay.ts, typescript/host/repl/src/lib/proof-extension.ts, typescript/host/repl/src/lib/repl-config.ts, typescript/host/repl/src/lib/repl-scenarios-support.ts, typescript/host/repl/src/lib/repl-builtin-tools.ts, typescript/host/repl/src/lib/repl-scenarios.ts, typescript/host/repl/src/lib/repl-host.ts, typescript/host/repl/src/lib/repl-provider.ts, typescript/host/repl/src/lib/repl-transcript.ts, typescript/host/repl/src/lib/repl-live-output.ts, typescript/host/repl/src/lib/repl-headless-mode.ts, typescript/host/repl/src/lib/repl-shell.ts]
      out: ["typescript/sdk/**", typescript/host/repl/src/lib]
    verification:
      command: "bun run nx run host-repl:typecheck && bun run nx run host-repl:test"
      expected: exit 0
    stop_conditions: ["STOP if any of the 14 files needs a symbol that is not re-exported from `@tuvren/sdk` or a `@tuvren/core` subpath and has no leaf-package instance equivalent; escalate rather than reintroducing a `@tuvren/runtime` import."]
    description: "Re-point every Reference Host module that currently imports @tuvren/runtime to import createTuvren and curated primitives from @tuvren/sdk (or @tuvren/core subpaths where the sdk does not re-export them) and to construct leaf-package instances (backends, runner, stream adapters, provider bridge) directly; update typescript/host/repl/package.json to drop the @tuvren/runtime dependency and add @tuvren/sdk. This makes the Reference Host the first proof that the ADR-057 host import contract is exercised by first-party code, not just documented"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ003
    title: Automated Host-Boundary Check
    type: Chore
    effort: 3
    depends_on: [KRT-BJ002]
    category: DX
    scope:
      in: [tools/scripts/host-boundary-gate.ts, tools/scripts/check.ts, tools/scripts/verify.ts, package.json]
      out: ["typescript/host/repl/src/**", tools/scripts/authority-guardrails/authority-guardrails.ts]
    verification:
      command: bun run check
      expected: exit 0
    stop_conditions: ["STOP if enforcing the check against current documentation examples would require rewriting doc content outside this epic's file scope; narrow the gate to `typescript/host/**` and file a follow-up for docs instead of silently excluding the whole documentation-examples clause."]
    description: "Add a new fail-loud gate script, following the existing portability-gate.ts pattern (license header, explicit rationale comment, deterministic exit code), that scans typescript/host/ (and documentation code examples where present) for imports of @tuvren/runtime, @tuvren/kernel-protocol, or @tuvren/kernel-runtime, and fails the run if any are found. Wire the new gate into check.ts's cheap authority-gate step list and into verify.ts's canonical verification path so the ADR-057 host import contract cannot silently regress"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ004
    title: Funnel-Routing Contract (ADR-058)
    type: Feature
    effort: 5
    depends_on: [KRT-BJ001]
    category: Feature-Evolution
    scope:
      in: [typescript/core/src/telemetry/index.ts, typescript/core/src/telemetry/telemetry-destination.ts, typescript/sdk/src/index.ts, typescript/runtime/src/lib/runtime-core-telemetry.ts, spec/core/authority-packet.json]
      out: [typescript/providers/bridge-ai-sdk/src/lib/ai-sdk-provider-bridge-utils.ts]
    verification:
      command: "bun run nx run shared-core:typecheck && bun run nx run sdk:typecheck && bun run nx run framework-runtime:typecheck"
      expected: exit 0
    stop_conditions: ["STOP if implementing failure isolation would require the Kernel syscall surface or the gRPC interop layer to become funnel-aware (a proto change); ADR-058 §5 explicitly forbids this — escalate instead of adding one.", "STOP if the `telemetry` option widening breaks an existing `TuvrenTelemetrySink`-only construction call; the union must stay backward-compatible per ADR-058's consequences."]
    description: "Introduce the TelemetryDestination contract on @tuvren/core/telemetry as a durable delivery target distinct from the existing push-based TuvrenTelemetrySink, with buffering/backpressure policy owned by the destination descriptor and delivery failures surfaced only through an operational-signal channel, never thrown into the caller. Widen the telemetry option on CreateTuvrenOptions (now homed on @tuvren/sdk per KRT-BJ001) to accept a sink, a destination, or a route object combining both, giving hosts a construction-time funnel-routing seam for split, unified, or mixed-substrate topologies. Extend the one-directional failure-isolation invariant already implemented for sinks in runtime-core-telemetry.ts to destinations, so a destination delivery failure degrades telemetry only and never fails, blocks, or delays a content-funnel commit or kernel checkpoint. Declare the new binding in the core authority packet so cross-language conformance has a machine-readable anchor"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ005
    title: Funnel-Isolation Conformance Check Set
    type: Chore
    effort: 3
    depends_on: [KRT-BJ004]
    category: Feature-Evolution
    scope:
      in: [spec/conformance/telemetry/plans/framework-operational-telemetry.json, spec/conformance/telemetry/scenarios/operational-telemetry-scenarios.json, tools/conformance/harness/run.ts]
      out: ["<lang>/conformance-adapter/**", "typescript/core/src/telemetry/**"]
    verification:
      command: bun run conformance
      expected: exit 0
    stop_conditions: ["STOP if a proposed assertion would require an adapter to compute pass/fail or map a protocol failure into `$.result.error`; keep grading in the shared runner per the repository's conformance rules."]
    description: "Add three new checks to the telemetry conformance plan per ADR-058 §5: (a) a destination healthy-vs-unavailable session-result equivalence check (the flow §4.19 unhappy path — same turn produces identical session results regardless of destination health); (b) a no-content-payload-on-telemetry-funnel check under default routing; (c) a failure-to-operational-signal mapping check (a destination delivery failure surfaces a signal rather than an exception or a silent drop). All assertions are shared-runner-owned and evaluate implementation-emitted events/results, never adapter-side grading"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ006
    title: Experimental Markers on the Capabilities Surface (ADR-056)
    type: Chore
    effort: 3
    depends_on: []
    category: Docs
    scope:
      in: [typescript/core/src/capabilities/index.ts, spec/core/authority-packet.json]
      out: [typescript/core/src/lib/capability-shapes.ts]
    verification:
      command: "bun run nx run shared-core:typecheck && bun run lint"
      expected: exit 0
    stop_conditions: ["STOP if any export under `typescript/core/src/capabilities/index.ts` cannot be given an individual `@experimental` tag (e.g., a re-export syntax that only allows a whole-statement comment) without restructuring the barrel; escalate rather than silently leaving an export untagged, since ADR-056's consistency floor treats an untagged export under a declared-experimental subpath as a defect."]
    description: "Apply the ADR-056 canonical experimental-surface marker — a TSDoc @experimental release tag on the individual export — to every current export of @tuvren/core/capabilities, plus a subpath-level experimental notice in the module doc comment so the boundary is visible without reading each export's docs. Record the marker declaration (which exports/subpath carry @experimental and why) in the core authority packet's surface listing so conformance plans, generated docs, and the Epic BL freeze-gate expectations read from one authored source"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ007
    title: Package Manifest Publication Readiness
    type: Chore
    effort: 5
    depends_on: [KRT-BJ001]
    category: Feature-Evolution
    scope:
      in: [typescript/core/package.json, typescript/sdk/package.json, typescript/backend-memory/package.json, typescript/runners/react/package.json, typescript/providers/bridge-ai-sdk/package.json, typescript/streaming/core/package.json, typescript/tools/mcp-client/package.json, typescript/telemetry/otel/package.json, files]
      out: ["rust/**/Cargo.toml", npm publish]
    verification:
      command: "bun run lint && bun run nx run shared-core:typecheck"
      expected: exit 0
    stop_conditions: ["STOP if a package directory named in this ticket does not exist as listed; re-verify the real path with `ls typescript/**/package.json` before editing rather than guessing a plausible name.", "STOP if any package intended to stay private per ADR-057's stable-core enumeration is found to be a runtime dependency of a to-be-published package in a way that would force it to also publish; escalate the topology question rather than flipping its private flag as a side effect."]
    description: "For every publishable TypeScript package in the ADR-057 stable-core set (@tuvren/core, @tuvren/sdk, and the leaf packages — backends, stream adapters, ReAct runner, AI SDK provider bridge, MCP client, telemetry packages), add a files allowlist scoped to dist and essential metadata, a license field matching the root LICENSE (Apache-2.0), a description, and a repository field. Decide the private flag per package: internal-only packages (testkits, certification wrappers, conformance-adapters, @tuvren/runtime, @tuvren/repl-host, kernel protocol/runtime packages) keep private: true and receive no publication fields. Establish the versioning baseline that will replace the current 0.0.0 placeholder at actual publish time (a version scheme decision, not a version bump performed in this ticket)"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
  - id: KRT-BJ008
    title: Release Versioning Pipeline Scaffolding
    type: Chore
    effort: 5
    depends_on: [KRT-BJ007]
    category: Feature-Evolution
    scope:
      in: [tools/scripts/release-lane.ts, tools/scripts/release-check.ts, package.json, ".changeset/config.json"]
      out: [npm publish, "rust/**/Cargo.toml"]
    verification:
      command: "bun run nx run shared-core:typecheck && bun tools/scripts/release-check.ts"
      expected: exit 0
    stop_conditions: ["STOP if the chosen changeset-class tool's default multi-version-per-leaf model would require leaf packages to peer-dep divergent `@tuvren/core` versions; ADR-037's single-instance guarantee (one `@tuvren/core` version across leaves) must not be weakened — configure the tool to enforce a single core version, or escalate if it cannot.", "STOP before wiring any real registry publish step; this ticket is scaffolding only."]
    description: "Introduce changeset-class release-versioning tooling (recording per-package change intents and computing version bumps) configured to honor the ADR-037 peer-dependency single-instance model, so every leaf package's peer-dependency on @tuvren/core resolves to one version across a release. Add a release lane script that runs the versioning computation and manifest updates without invoking an actual registry publish, leaving the real publish step for Epic BL"
    acceptance:
      mode: gherkin
      evidence: TODO
    flows: []
    certainty: assumed
    status: done
closeout:
  tickets_completed: [KRT-BJ001, KRT-BJ002, KRT-BJ003, KRT-BJ004, KRT-BJ005, KRT-BJ006, KRT-BJ007, KRT-BJ008]
  tickets_deferred: []
  points_completed: 32
  spec_patches: []
  changelog_entries: []
  candidate_terms: []
  routed_upstream: []
  follow_ups: []
  settled: []
