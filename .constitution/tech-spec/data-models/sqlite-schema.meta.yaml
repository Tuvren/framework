artifact_path: "typescript/kernel/backends/sqlite/migrations"
surface: "data-model"
flows:
  - flow-crash-mid-checkpoint-recovery
  - flow-host-lists-threads-reads-state
  - flow-tenant-offboarding-erasure
boundaries:
  - BND-18
companion: "README.md"
certainty: "assumed"
assumption: "The official SQLite schema is the migration directory the backend applies. The companion points at it; it does not restate columns."
settles_when: "A conformance plan or package test that exercises this artifact is cited as repository evidence by a closeout."
