# Stage 3 (TechSpec) Changelog

Local SemVer history for `.constitution/tech-spec/`.

- v0.33.3 - **Epic BL closed — ADR-054/056/057 §5/ADR-037 publication realization notes (execution sessions 2026-07-09 → 2026-07-11; no ADR text changed, no new decision).** The freeze-and-publish half of the SDK-readiness ADR set is now physically realized: **ADR-054/056** — the API-surface snapshot / freeze gate exists (`tools/scripts/api-freeze-gate.ts`, a TSDoc-aware TypeScript compiler-API source-barrel walker with checker-derived structural signatures; the tooling choice ADR-056 left open resolved **against** api-extractor because BJ006's barrel-level `@experimental` tags are invisible to canonical-declaration tag readers), implements the ADR-056 diff table verbatim with a self-test on fixture surfaces at every run, enforces the consistency floor against the core authority packet's wholly-experimental declaration for `@tuvren/core/capabilities`, and runs in both `check` and `verify` with a committed baseline. **v0.33.1's recorded align-at-BL mismatch is resolved:** KRT-BL001 **retracted** `@tuvren/telemetry-semconv` from stable-core membership to the ADR-057 §5 published-internal tier (it is consumed only by non-host-facing packages), while `@tuvren/telemetry-otel` stays a stable published leaf per its ADR-042 anchor; and the private-dep publish topology resolved by **extending ADR-057 §5's published-internal treatment** beyond `@tuvren/runtime` to `@tuvren/kernel-protocol`, `@tuvren/kernel-runtime`, and `@tuvren/provider-api` (published for dependency resolution, marked internal, never documented host-installable; `provider-api` reclassified its `@tuvren/core` edge from regular to peer dependency to preserve ADR-037's single-instance guarantee in consumer trees). **ADR-037** — the tilde `@tuvren/core` peer range is materialized at publish time by `tools/scripts/publish-registry.ts` (workspace-protocol `workspace:~` → `~0.1.0`) and enforced as a preflight STOP. **First real publish executed 2026-07-11:** 19 packages live on the public registry at `0.1.0` (the operator-chosen launch version) in changesets lockstep, with npm provenance via a GitHub Actions OIDC workflow (`release.yml`); a fresh-install consumer completed a first Turn via `createTuvren` from the published `@tuvren/sdk`, closing the ADR-054 publication commitment for CAP-P0-070. Adopter-facing documentation of the ADR-056 badge semantics, the ADR-057 §3 host import contract, and the ADR-058 §2 topologies ships at `docs/guides/publishing-and-adopter-onboarding.md`. Registry-operations note: the bootstrap `NPM_TOKEN` must be replaced by npm trusted publishing (OIDC) and deleted before npm's January 2027 bypass-2FA-token sunset — recorded as a mandatory hand-off in the archived epic file and in `release.yml`'s operator notes. No physical schema, contract shape, or gate posture beyond the additions above changed; `stack.md` §SDK-distribution posture was checked and reads accurately post-publish.
- v0.33.2 - **Epic BK closure freshness pass (2026-07-08).** Two genuine staleness corrections found by rereading this layer against Epic BK's realized changes: **ADR-058**'s Consequences paragraph claimed the ADR-044 secret-screening gap named by the 2026-07-04 audit "remains open and tracked separately" — now false, since Epic BK's `KRT-BK004` closed it (`screenValueForSecretPatterns` in `@tuvren/core/security`, wired into both AI SDK bridge `bridgeExtras` construction points, proven by a new `secretPatternAbsence` conformance assertion); the sentence is corrected to record the closure without reopening or rewriting the ADR's original decision body. **`stack.md`**'s "Current repository reality" package enumeration omitted `@tuvren/backend-shared` (KRT-BK001's new shared reclamation/run-transition-legality/immutability invariant core the memory/SQLite/PostgreSQL backends delegate to) and its "Current kernel conformance posture" line stated Rust "remains capability-scoped and non-applicable where it does not advertise the relevant extension" without noting KRT-BK010 promoted Rust past that for `kernel.run-liveness`/`kernel.reclamation` (42/70 → 54/70 applicable checks, including crypto-shredding erasure); both corrected in place. Everything else in this layer (ADR-011, ADR-050, ADR-051, `data-models/README.md` §3.2's run-liveness field documentation, the `contracts/`/`data-models/` pointer indexes) was checked and found already accurate against Epic BK's realized changes — no edit needed. No physical schema, contract shape, gate posture, or ADR decision changed.
- v0.33.1 - **Epic BJ closed — realization notes for ADR-056/057/058/059 and ADR-037 (execution sessions 2026-07-05 → 2026-07-07; no ADR text changed, no new decision).** All four SDK-readiness ADRs this layer recorded at v0.32.0/v0.33.0 are now physically realized and verification-gated: **ADR-057/059** — `@tuvren/sdk` is the composition tier (root + `/advanced`), `@tuvren/runtime` is the internal engine (regular sdk→runtime dep; identity payload codec + `isPayloadEnvelope`/`ENVELOPE_MAGIC` relocated into `@tuvren/core/lifecycle` to break the sdk⇄runtime source cycle — the coordinated single-commit migration ADR-057's consequences called out), the Reference Host imports only the §3 contract surfaces, and the automated gate (`tools/scripts/host-boundary-gate.ts`, TypeScript-AST specifier extraction over `typescript/host/**`) runs in `check`/`verify` as `ADR-057 host import boundary gate`. **ADR-058 §1–§5** — the `TelemetryDestination`/`TelemetryRoute`/`TelemetryRouting`/`TelemetryOperationalSignal(Kind)`/`TelemetryBufferingPolicy` contract lives on `@tuvren/core/telemetry` (`telemetry-destination.ts`), the `createTuvren`/`RuntimeCoreOptions` `telemetry` option is widened backward-compatibly, one-directional failure isolation is realized at the `@tuvren/runtime` emitter boundary (including a review-hardened throw-proof projection: a non-coercible host throw inside the catch handler degrades to the last-resort warning rather than re-entering the session path), declared in `spec/core/authority-packet.json` + `spec/core/bindings/typescript.md`, and promoted to three shared-runner conformance checks on `framework-operational-telemetry.json` 0.4.0 with compatibility evidence refreshed (475→478 checks); no kernel/gRPC/proto surface became funnel-aware (§5 respected). §6's official durable destination adapters remain deferred to the named Epic BO (CAP-P1-073). **ADR-056** — all 22 `@tuvren/core/capabilities` exports carry individual `@experimental` TSDoc tags (one-export-per-statement barrel layout; Biome's `assist/source/organizeImports` had to be file-locally suppressed because the organizer merges tagged statements back into a grouped export) plus the subpath-level notice and the authority-packet marker declaration; two gate-tooling constraints are recorded for KRT-BL002 in the archived epic file (barrel-level tags are invisible to canonical-declaration tag readers like api-extractor — a `.d.ts`-comment walker is proven viable; the `@packageDocumentation` block is the file's second comment). **ADR-037 + ADR-057 §5 (publication)** — the 14-package publishable set carries `description`/`license: Apache-2.0`/`repository`/`files: ["dist"]` manifests, and `@changesets/cli` is configured with a `fixed: [["@tuvren/*"]]` lockstep group so every release computes one `@tuvren/core` version across leaves (`tools/scripts/release-lane.ts` re-proves the invariant post-bump; no publish step exists — Epic BL). **Recorded mismatch (align-at-BL, not patched here):** ADR-054/ADR-057 §5/ADR-059 §4's shorthand leaf enumerations ("backends, stream adapters, ReAct runner, AI SDK provider bridge, MCP client") never name the telemetry packages that the execution-stage ticket explicitly included in the publishable set; `@tuvren/telemetry-otel` is anchored by ADR-042 §3, while `@tuvren/telemetry-semconv` (currently consumed only by private packages) must have its stable-core membership explicitly confirmed or retracted by KRT-BL001 before the ADR-054/056 freeze snapshot locks the published set. The related private-dep publish topology (publishable sdk/backends/leaves depending on private `@tuvren/runtime`/`@tuvren/kernel-protocol`/`@tuvren/kernel-runtime`/`@tuvren/provider-api`) is likewise a KRT-BL001 input — ADR-057 §5 already anticipates the runtime resolution ("remains published … marked internal"). Directory layout is unchanged from what `guidelines.md` §5.1 already describes (including the ADR-059 leaf at `typescript/kernel/grpc-client/`); no `contracts/`/`data-models/` pointer-index drift was found in the close-out freshness pass.
- v0.33.0 - **Advanced composition surface + `@tuvren/kernel-grpc-client` leaf; host import contract extended (execution of Epic BJ / KRT-BJ002, session 2026-07-05).** New accepted **ADR-059** (amends ADR-057 §3/§6). Executing the Reference Host re-point revealed that ADR-057's grounding audit checked host-facing *types* only and missed that the host consumes engine *factory functions* from `@tuvren/runtime` with no `@tuvren/sdk`/`@tuvren/core` or leaf home — `createOrchestrationRuntime` (multi-agent, which `createTuvren`'s single-default-agent shape cannot express), `createRunnerRegistry`, `createTuvrenRuntime` (the primary *synchronous* `createReplHost` path), `createGrpcRuntimeKernel` (rust-grpc mode, runtime-only), and `createRuntimeKernel` (kernel-runtime, also forbidden by §3). ADR-059 gives them curated homes without re-opening host→`@tuvren/runtime` coupling: a new opt-in subpath **`@tuvren/sdk/advanced`** re-exports the four composition/local-kernel factories (sdk already deps runtime + kernel-runtime, so install weight is unchanged), and a new published leaf **`@tuvren/kernel-grpc-client`** (`typescript/kernel/grpc-client/`) owns `createGrpcRuntimeKernel` + the relocated kernel-interop generated bindings/codec (buf.gen.yaml out-path, `kernel-interop-governance` constants, and the `kernel-interop-grpc` Nx outputs retarget from `typescript/runtime` to the leaf; runtime drops its `@connectrpc/*` + `@bufbuild/protobuf` deps). The §3 host contract is extended to allow `@tuvren/sdk/advanced` and `@tuvren/kernel-grpc-client` while still forbidding `@tuvren/runtime`/`@tuvren/kernel-protocol`/`@tuvren/kernel-runtime`; the KRT-BJ003 boundary gate enforces the partition. The new leaf joins the publishable set (in scope for KRT-BJ007/BJ008 + the ADR-054/056 freeze); `@tuvren/sdk/advanced` is an additional entry of the already-frozen `@tuvren/sdk`. The Reference Host re-points with no behavior change (sync `createReplHost` keeps engine-path construction, now sourced from `@tuvren/sdk/advanced` + `@tuvren/kernel-grpc-client`).
- v0.32.0 - **SDK-boundary and two-funnel revision (PRD v0.12.0, Architecture v0.12.0; post-audit interview, session 2026-07-04).** Three new accepted ADRs: **ADR-056** fixes the experimental-marker mechanism required-but-deferred by ADR-054 — TSDoc `@experimental` tags on individual exports are the canonical marker (graduation = tag removal, zero import churn), the API-snapshot freeze gate derives its stable/experimental classification from the tags (four-case diff table), a consistency floor fails the build on untagged exports under a declared-experimental subpath, and every current `@tuvren/core/capabilities` export is tagged at initial freeze. **ADR-057** (amends ADR-037/040/054) makes `@tuvren/sdk` the slim convenience/composition tier: `createTuvren` + curated re-exports move to the sdk root; the ADR-040 string-kind shorthands are retired in favor of instances-only options (hosts install and pass their chosen leaf adapters — the interview's host-import contract `{core, sdk, leaf packages}`, never `@tuvren/runtime` or kernel packages, enforced by an automated boundary check); `@tuvren/runtime` is demoted to a published-but-internal engine package excluded from the amended ADR-054 stable-core enumeration; the Reference Host re-points pre-freeze (grounding confirmed no runtime-only host-facing types exist — the rehoming is a re-export cleanup — and surfaced the current inverted `runtime → sdk` peer-dependency as a one-commit migration hazard). **ADR-058** introduces the construction-time funnel-routing contract realizing CAP-P0-071/072: a net-new `TelemetryDestination` contract in `@tuvren/core/telemetry` (delivery batches in the canonical vocabulary, adapter-owned bounded buffering, operational-signal channel instead of throws), the `createTuvren({ telemetry })` option widened to `sink | destination | { sink?, destination? }` as the routing seam (split/unified/mixed topologies, bound once at construction like the Scope seam), a normative one-directional failure-isolation invariant (telemetry failure can never fail/block/corrupt a content commit; recovery never reads telemetry), a funnel-isolation conformance check set, kernel/interop staying funnel-unaware (no proto change), and official destination adapters deferred additively per CAP-P1-073. Supporting integration: `guidelines.md` gains the missing `typescript/sdk` tree entry, re-pointed runtime/telemetry tree comments, updated testing expectations (instances-only composition tests), and a new §5.8 migration-constraints section (5.8.1–5.8.3); `stack.md`'s package/publication posture and target-state paragraphs are retargeted to the sdk-as-composition and funnel-routing world. Out of scope by design: the storage persistence-model decision surfaced by the 2026-07-04 audit (postgres blob-per-write) is deliberately NOT decided here — it is evidence-gated behind a benchmark spike in the execution plan.
- v0.31.1 - Post-review correction to the v0.31.0 tranche (M10.3b review, P2): four "Tuvren Runtime" product-name usages survived the full rewrite of the two pointer indexes — `contracts/README.md` (the authentication non-goal and the provider-contract compatibility-strategy lines) and `data-models/README.md` (the durable-recovery delta and official-backend definition lines) — re-centered to "Tuvren" per issue #87 §14, completing the re-centering the v0.31.0 entry recorded for `stack.md`; the v0.31.0 residual-strings disclosure below should be read as also having omitted these four (now fixed) product-name residuals. No other content changed.
- v0.31.0 - Epic #87 (Sovereign Polyglot Framework Restructure) constitution rewrite, second tranche (M10.3b, following the M10.3a prd/architecture rewrite that took both of those layers to v0.11.0): the tech-spec layer is rewritten to the post-#87 world, resolving the v0.30.5 "intentionally left unedited until the M10 gate" posture for this layer. Scope and policy: (1) **ADR disposition — annotate, never rewrite.** All 23 ADRs carrying stale `driver` terms or `boundaries/`-rooted paths (ADR-004, 014, 021, 022, 023, 025, 026, 029, 033, 034, 035, 037, 039, 040, 041, 042, 043, 045, 048, 049, 050, 054, 055) received a prepended "Path/terminology note (2026-07, epic #87, M10.3b)" block mapping the retired names/paths to the new world; every ADR body below its note is preserved verbatim as the historical decision record (matching the M10.3a precedent of preserving retired definitions as quotations; no ADR text or decision changed). (2) **Non-ADR tech-spec files — full rewrite.** `guidelines.md` (including the whole §5.1 repository tree, the §5.5/§5.6 migration-plan paths, and the pre-M3.4 `boundaries/framework/conformance/plans/runtime-api-*` and pre-M4.1 providers-fixtures citations), `stack.md`, and the `contracts/` and `data-models/` pointer indexes are repointed from `boundaries/<area>/{contracts,conformance,interop,implementations}/...` to the new homes — `spec/<port>/` for language-neutral authority (e.g. `spec/core/authority-packet.json`, `spec/kernel/{authority-packet.json,cddl/}`, `spec/tools/mcp/authority-packet.json`, `spec/providers/`, `spec/host/client-endpoint-integration.md`, `spec/interop/proto/`, `spec/telemetry/semconv/`), `spec/conformance/<port>/{plans,fixtures,scenarios,schemas}/` for conformance assets (the former framework plans directory is `spec/conformance/engine/plans/` per the M9.1 naming ruling), `typescript/<area>/` and `rust/<area>/` for implementation packages, and `tools/conformance/harness/run.ts` for the shared semantic conformance engine (the stale `tools/conformance/runner/` citations corrected in the same pass); every repointed path was verified against the working tree before being written. (3) **driver→runner (M6) carried through:** ReAct Driver → ReAct Runner (`@tuvren/runner-react` at `typescript/runners/react/`), the `@tuvren/core/driver` subpath → `@tuvren/core/runner`, `RuntimeDriver*`/`DriverExecution*`/`driverId`/`DriverKind` type and field citations → their `RuntimeRunner*`/`RunnerExecution*`/`runnerId`/`RunnerKind` forms (verified against source), and "above driver discretion"-family prose → runner. (4) **Reality-tense corrections without decision changes:** ADR-037/040 absorption described as landed (the four contract shims `@tuvren/runtime-api`, `@tuvren/event-stream`, `@tuvren/tool-contracts`, `@tuvren/driver-api` and the `@tuvren/runtime-core` shim are removed; `@tuvren/core-types` remains the one live deprecated shim), the ADR-041 playground deletion recorded as closed, and the two retired "Tuvren Runtime" product-name usages in `stack.md` re-centered to "Tuvren" per issue #87 §14. Residual `driver`/`boundaries/` strings in this layer exist only inside ADR historical bodies below their annotation blocks, inside the annotation blocks' own old→new mappings, in changelog history (this file), or as explicitly-historical quotations of retired package names/paths. Same-tranche companions outside this layer: the two live `.constitution/tasks/` files (`critical-path.md`, `changelog.md`) swept (four runner-term fixes in `critical-path.md`; the tasks changelog is history and stays verbatim), and the two stale diagnostic reports (`bh005-bridge-provider-executed-fidelity-audit.md`, `epic-al-rust-re-entry-gate-reassessment.md`) annotated with dated path-notes rather than rewritten; the gate-checked `epic-al-portability-inventory.json` / `epic-al-portable-surface-conformance-gap-inventory.md` artifacts were verified current via a green portability gate and left untouched. No physical schema, contract shape, gate posture, or ADR decision changed; `bun run check` green.
- v0.30.5 - Reviewed for upstream delta; parallel structural migration tracked in GH issue #87 (Tuvren/runtime, "Epic — Restructure into a Sovereign Polyglot Framework"). That epic is explicitly not a `.constitution/` epic (status and milestone tracking live on the GitHub issue itself, per its own §12) and is out of scope for the four-stage planning framework; it relocates `boundaries/*`/`implementations/*` into a `spec/` + per-language idiomatic layout without changing any physical schema, contract shape, or ADR decision recorded here. No layer-specific changes required. `guidelines.md` (39 references) and `stack.md` (4 references) currently cite the pre-migration `boundaries/*` paths; those are intentionally left unedited milestone-by-milestone per the epic's own M10 gate ("`.constitution/` and `AGENTS.md` rewritten to the new world"), rather than being patched piecemeal and re-invalidated by the next milestone's moves. Every path this migration touches, its destination, and its equivalence proof are tracked in the repo-root `MIGRATION_INVENTORY.md` (deleted at cutover) rather than in this changelog.
- v0.30.4 - Epic BH (Conversation-State Ownership Hardening) closed, realizing ADR-053 (Tuvren is the unconditional conversation-state owner; provider server-side state, carried continuity artifacts, and provider-side caching are reconstructable optimizations, never a correctness dependency — extends ADR-005) and delivering the ADR-055 baseline-bridge `providerExecuted`/`dynamic` fidelity audit, for PRD CAP-P0-069 exactly as accepted — no ADR text or decision changed; the realization the ADRs anticipated is now repository reality. Each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA before the next began. **KRT-BH001** aligned machine authority first (declare-then-promote, the BE/BF/BG precedent): added the `provider-api-conversation-state.json` conformance plan with checks gated behind a new `providers.conversation-state-ownership` capability bit and registered it in the provider-api authority packet (`authoritativeSources` + `conformancePlans` + applicability target), keeping the §1.1 coverage-matrix entry honestly `missing-conformance-follow-up` until promotion — no runtime behavior change. **KRT-BH002 realization:** proved a next-turn provider request is reconstructable purely from rebuilt durable head-state with no provider-held state (the reconstructed request equals the live-path request), and proved continuity artifacts are shreddable. **No new persistence surface was required:** message-part continuity (`providerMetadata` on assistant/reasoning/tool parts) is already crypto-shreddable through the BF005 `MESSAGE_PAYLOAD_EDGE` codec, so M2 asserted that existing coverage (typed `ErasedPayload` on key destruction, byte-identical lineage hashes) rather than adding a `provider.continuity` edge — the `data-models/` and `contracts/` pointer indexes are therefore unchanged (the open question "add a `provider.continuity` edge only if continuity is carried outside the already-encrypted message records" resolved to *no edge*). **KRT-BH003 realization:** closed the AY005 multi-turn `providerContinuity` round-trip gap (recorded in v0.29.3) with a real multi-turn test; the existing message-part `providerMetadata` replay (`mapAssistantReplayProviderOptions`) already round-trips turn-1 continuity into the turn-2 prompt, so the test is the deliverable and no driver extraction code was added. **Two-artifact layering caveat:** continuity is carried on two distinct layers — per-message-part `providerMetadata` replay (bridge prompt mapping) and the turn-level `TuvrenPrompt.providerContinuity` field threaded via `continuityToProviderOptions` — and the multi-turn round-trip test exercises the message-part replay layer; the BH003 docs follow-up clarified that layering boundary so the test's scope is not mistaken for turn-level extraction. **KRT-BH004 realization:** proved provider-side caching is correctness-neutral — a cache miss and a cache hit for the same turn produce an identical canonical result; only cost/latency differ. **Scope + coupling follow-up observation (recorded here as the BH004 test header directs):** the neutrality claim is scoped to the *produced content* (the model-facing outcome / assistant message), **not** to the whole durable record. The bridge's per-turn cost bookkeeping (`providerMetadata.aiSdkBridge.rawUsage`, whose `cacheRead` reflects whether the provider served the turn from cache) is persisted onto the assistant message's `providerMetadata` and thus into the content-addressed durable record — so that record's hash is cache-neutral only with respect to produced content, while cache-varying cost telemetry rides alongside it. This is a *pre-existing* cost-bearing-not-correctness-bearing coupling; whether the `aiSdkBridge` bookkeeping ought to live in the content-addressed record at all is out of scope for BH and is left as a deferred follow-up observation (no ADR change, BG-precedent realization note). **KRT-BH005 realization:** performed the ADR-055 baseline-bridge `providerExecuted`/`dynamic` fidelity audit against the `parseToolCall` landmine (vercel/ai #10888, open as of 2026-06). Finding 1 — the bridge is **structurally immune** to #10888: it consumes the low-level `LanguageModelV3.doGenerate`/`doStream` contract directly (never the `generateText`/`streamText` orchestration where `parseToolCall` lives), and provider-executed tool routing is keyed by the host's *declared* provider-native/mediated tools (`ProviderToolClassLookup`), never the user function tool map, so the mis-validation has no code path to fire. Finding 2 — **a real defect was found and fixed**: `rejectUnsupportedProviderOwnedToolPart` rejected *every* `providerExecuted`/`dynamic` `tool-call` part regardless of declaration, so a realistic provider-executed round-trip (real providers emit the inline `tool-call` *before* its `tool-result`, verified against `@ai-sdk/openai@3.0.53`) aborted at the call before reaching the `tool-result` that carries the provider-native attribution; the fix **skips** a *declared* provider-executed `tool-call` (the matching `tool-result` carries the AY002/AY004 provider-native attribution and the call must not contaminate client-facing parts with a function `tool_call` the runtime would execute) while still **rejecting an undeclared** one (baseline protection unchanged), symmetrically across the generate and stream paths. Finding 3 — per-class observation limits (no cancel/retry/audit) already hold and are proven by the existing `runtime-core.provider-execution-class.test.ts` (KRT-AY003); this milestone added no runtime change for criterion 3. **Orphan-call boundary (made explicit at the M5 review):** a *declared* provider-executed `tool-call` whose `tool-result` never arrives this turn (truncated/interrupted/multi-step) yields *no* provider-native record and *no* client-facing function `tool_call`, and does **not** throw the way the prior over-broad rejection did — the result is the attributable observation, the call is provider bookkeeping that contributes nothing on its own; no skip-tracking/diagnostic state was added (deferred to the ADR-055 native-client phases, consistent with the `tool-input-start` minimalism), and the behavior is pinned by tests. The audit is recorded in `.constitution/reports/bh005-bridge-provider-executed-fidelity-audit.md`. `ai@6.0.142` and `@ai-sdk/provider@3.0.8` confirmed locked (matches ADR-055's recorded surface); no version drift. **Authority cascade:** the `docs/KrakenFrameworkSpecification.md` §1.1 v0.21 conversation-state-ownership note was promoted from `missing-conformance-follow-up` to `authority-backed-conformance-covered` through a dedicated `conversationStateOwnership` evidence anchor in the docs-authority freeze gate (adapterCapability `providers.conversation-state-ownership`, the provider-api authority packet, the runnable `provider-api-conversation-state.json` plan, the provider fixture, and the providers compatibility evidence — all five resolve on disk); the providers conformance adapter advertises the capability and answers the conversation-state operations while staying assertion-free, and per-lane compatibility evidence was refreshed. Freeze gate `--check` verifies 264 classified claims (`authority-backed-conformance-covered` 223 → 224, `missing-conformance-follow-up` 5 → 4). Freshness pass: `stack.md` already carried the conversation-state-ownership + correctness-neutral-caching + ADR-055-audit posture from v0.30.0 and needed no edit; `guidelines.md`, the `data-models/` and `contracts/` pointer indexes, and the `docs/Kraken*Specification.md` specs were left unchanged — the specs stay frozen human semantic authority (the v0.21 framing is intentional, promotion tracked only in the machine-authority coverage matrix, matching the BE/BF/BG precedent). No new physical surface, no schema or generated-artifact change; the only production-code change is the BH005 bridge fix, which *removes* a spurious rejection rather than adding surface. No product-scope or container redesign; stayed within logical container boundaries (the bridge stays AI-SDK-low-level and provider-neutral). Traces to PRD CAP-P0-069, Architecture v0.10.0, and ADR-053/ADR-055.
- v0.30.3 - Epic BG (Backend-Authoritative Lease Clock + Side-Effect-Once) closed, realizing ADR-050 (the execution-lease clock is backend-authoritative for shared multi-worker backends) and ADR-052 (side-effect-once under preemption via a `(runId, callId, fencingToken)` idempotency envelope, no-retry of in-flight `nonRetryable` work on authority loss, and client-result-as-proposal) exactly as accepted — no ADR text or decision changed; the realization the ADRs anticipated is now repository reality, for PRD CAP-P0-068. **ADR-050 realization:** `RuntimeBackendTx` gained an optional `now(): EpochMs` that surfaces the backend's own per-transaction clock, and `BackendCapability` gained a `shared-lease-clock` bit (a semver-minor addition per §9.1). `@tuvren/backend-postgres` advertises `shared-lease-clock: true` and captures server time once per transaction (`extract(epoch from clock_timestamp())`), so `tx.now()` is the authoritative clock; the kernel lease operations in `runtime-kernel-runs.ts` resolve "now" from `tx.now()` for shared-clock backends — `createLeasedRun`/`renewLease` re-base the worker-supplied absolute expiry onto backend time (stamp `tx.now() + (suppliedExpiry − workerNow)`), and `renewLease`'s expiry check, `listExpired`, and `preemptExpired` judge expiry by `tx.now()` rather than the caller-supplied `nowMs`, so a peer whose wall clock is skewed ahead cannot preempt a lease the backend clock still considers live (split-brain eliminated by construction). `@tuvren/backend-memory` and single-file `@tuvren/backend-sqlite` advertise non-support and keep the exact in-process path unchanged (single-writer, no cross-worker contention). The framework renewal margin (`runtime-core-liveness.ts`) is measured by elapsed local monotonic delta for shared-clock deployments so it does not mix backend-time expiry with worker-time now. No kernel syscall surface change: only the clock source within the existing lease operations and the capability advertisement changed. **ADR-052 realization:** the idempotency identity derived from `(runId, callId, fencingToken)` is threaded onto the Tuvren-server tool dispatch path (exposed via `ToolExecutionContext`) and the Client Endpoint Boundary `ClientInvocationEnvelope`, reusing the existing `idempotent`/`nonRetryable`/`maxRetries` metadata with no new tool-definition fields; on loss of execution authority an in-flight `nonRetryable` invocation is not retried under the dead owner (durably-staged completed results are still recovered by `callId` per §4.9), and a client-reported result is a proposal that commits to durable history only through a runtime commit under a valid run fencing token, so a stale or late client report cannot mutate committed state. **Realization-accuracy note on ADR-052 decision 1 (recorded here, ADR frozen, per the BE/BF precedent of recording corrections in the changelog/spikes rather than editing accepted ADR text):** the `(runId, callId, fencingToken)` triple is a *per-attempt* envelope key, not a recovery-stable identity — `runId` is a fresh `randomUUID` per run and `fencingToken` rotates on renewal and on recovery, so only `callId` is stable across a recovery (the §4.9 "skips completed tool calls by `callId`" path). ADR-052 decision 1's "a re-dispatch after recovery presents the same identity for the same logical call" holds for re-presenting the envelope to an external system *within* a run/fencing epoch (the intended external-dedup use), but cross-recovery side-effect-once is not carried by the triple alone; it is achieved by **composition** — ADR-050 backend-clock no-split-brain (only one owner is ever live) + no-retry of in-flight `nonRetryable` on authority loss + recovery skip-by-`callId` (§4.9) + client-result-as-proposal. The conformance proves exactly this composition rather than an identity match across recovery. **Authority cascade:** the `docs/KrakenKernelSpecification.md` §5.2 backend-authoritative lease-clock claim and the `docs/KrakenFrameworkSpecification.md` "Running Lease Ownership" side-effect-once / client-result-as-proposal claims were promoted from `missing-conformance-follow-up` to `authority-backed-conformance-covered`: the kernel-protocol authority packet (0.9.0 → 0.10.0) registers a `kernel.shared-lease-clock`-gated `clock_skew_preemption` check on the `kernel-run-liveness.json` plan (0.1.0 → 0.2.0) that drives two owners against one PostgreSQL backend under simulated worker-clock skew and proves no split-brain preemption while the backend clock says the lease is live, genuine preemption once the backend clock advances past expiry, the completed side-effecting call incorporated by `callId` (driven at most once) with no uncommitted residue; the shared-core authority packet (1.6.0 → 1.7.0) registers a `client-result-as-proposal-authority-lost` check on the `runtime-api-lifecycle-extended.json` plan (0.1.0 → 0.2.0) proving a client result returning after authority loss is never committed; per-backend compatibility evidence was refreshed and the kernel conformance adapters advertise `kernel.shared-lease-clock` (PostgreSQL only) while staying assertion-free. Freshness pass: `stack.md` already carried the shared-lease-clock + side-effect-once posture from v0.30.0 (it described the design BG now realizes) and needed no edit; `guidelines.md`, the `data-models/` and `contracts/` pointer indexes, and the `docs/Kraken*Specification.md` specs were left unchanged — the specs stay frozen human semantic authority (the v0.12/v0.21 framing is intentional, with promotion status tracked only in the machine-authority coverage matrix, matching the BE/BF precedent of not editing the spec at promotion). The kernel hashing algorithm and canonical-blob identity are unchanged. No product-scope or container redesign; the kernel remains data-only/protocol-only (ADR-001). Traces to PRD CAP-P0-068, Architecture v0.10.0 §4.18, and ADR-050/052.
- v0.30.2 - Epic BF (Data Lifecycle: Reclamation + Crypto-Shredding Erasure) closed, realizing ADR-051 (a kernel reachability-reclamation primitive plus crypto-shredding erasure of host-key-encrypted untrusted-edge payloads) exactly as accepted — no ADR text or decision changed; the realization the ADR anticipated is now repository reality, for PRD CAP-P0-066/067. The kernel gained a capability-gated `maintenance.reclaim` surface backed by a structural mark-and-sweep: it marks durable state reachable from live roots (non-archived branch heads, thread roots, active-run staged work) within the Scope, unions that with everything created at or after the grace horizon (the oldest active run's `createdAtMs`, else unbounded), and sweeps only the unreachable remainder older than the horizon, so it never releases reachable state and never races recovery; cross-branch structural sharing is honored because a shared Object stays marked via any live root. All three production backends realize the backing operation and advertise the `maintenance.reclamation` `BackendCapability` bit (memory: scoped in-memory deletes; SQLite: scoped `DELETE`s; PostgreSQL: snapshot-draft mutation + re-persist), and a backend advertising non-support rejects reclamation with `TuvrenPersistenceError` code `kernel_capability_unsupported`. Crypto-shredding erasure ships as an interface-first `PayloadCodec` contract in `@tuvren/core/lifecycle` (encrypt-on-write / decrypt-on-read keyed by a host `keyRef`, typed `ErasedPayload` result) plus a default `createAesGcmPayloadCodec({ keyring })` AES-256-GCM codec (`node:crypto`); the framework applies the codec at the provider/tool/MCP/client edges, opt-in via `createTuvren({ payloadCodec })` and back-compatible (no codec → plaintext). Keys stay host-held and the kernel stores only the opaque `TVE1` ciphertext envelope (`{ keyRef, ciphertext, alg, iv, tag }`) keyed into the content-addressed store, so destroying the host key renders the payload unrecoverable while every referencing Object/turn-node/tree/branch hash stays byte-identical — erasure rewrites no lineage (ADR-001 + Secret Isolation Model preserved). The host-facing maintenance surface on `TuvrenRuntime` drives per-Scope reclamation, and the §4.17 tenant-offboarding flow destroys the scope's keys, reclaims, and drops the scope partition (memory store drop / SQLite file removal / PostgreSQL snapshot-row delete) with no other Scope affected. The `docs/KrakenKernelSpecification.md` §9.4 reclamation claims were promoted through the authority cascade from `missing-conformance-follow-up` to `authority-backed-conformance-covered`: the kernel-protocol authority packet declares `bindingSections.reclamation` and registers the runnable `kernel-reclamation.json` plan (packet 0.7.0 → 0.8.0, plan 0.1.0 → 0.2.0), the memory/SQLite/PostgreSQL conformance adapters advertise `kernel.reclamation` and answer reclaim-probe and erasure-probe operations (the adapter plays the host/edge codec role so the kernel stays codec-unaware), and refreshed per-backend compatibility evidence records all five reclaim/erasure checks passing; the §9.4 non-support `kernel_capability_unsupported` rejection row stays with the capability-gating contract (`missing-conformance-follow-up`, `KRT-AM010`, ADR-034) because the promoted plan only exercises capability-bearing backends. The kernel hashing algorithm and canonical-blob identity are unchanged; only opaque payload bytes are now optionally ciphertext. Freshness pass: `stack.md` already carried the data-lifecycle/erasure posture from v0.30.0 (it described the design BF now realizes) and needed no edit; `guidelines.md`, the `data-models/` and `contracts/` pointer indexes, and `docs/KrakenKernelSpecification.md` were left unchanged — the kernel spec stays frozen human semantic authority (its v0.12 "forthcoming 31st operation / deferred portable authority" framing is intentional, with promotion status tracked only in the machine-authority coverage matrix, matching the BE precedent of not editing the spec at promotion). No product-scope or container redesign; stayed within logical container boundaries (the kernel remains data-only/protocol-only, ADR-001). Traces to PRD CAP-P0-066/067, Architecture v0.10.0 §4.17, and ADR-051.
- v0.30.1 - Epic BE (Tenancy Scope Seam + Isolation-by-Construction) closed, realizing ADR-048 (tenancy-agnostic scope seam bound at backend/connection construction; the kernel syscall surface and gRPC interop stay scope-free) and ADR-049 (isolation-by-construction; durable identity is content-addressed as `(scope, hash)` with no cross-scope existence oracle and no cross-scope dedup) exactly as accepted — no ADR text or decision changed; the realization the ADRs anticipated is now repository reality. The three backend factories accept a host-supplied Scope at construction: `@tuvren/backend-memory` keys its stores by Scope (scope-keyed `MemoryScopeStore`), `@tuvren/backend-sqlite` realizes a Scope as a database file (default Scope = the supplied `databasePath` verbatim for back-compat; any other Scope derives a deterministic sibling file — the file-per-scope option from ADR-048, chosen over a discriminator column in SPK-BE002), and `@tuvren/backend-postgres` realizes a Scope as a row-level-isolated snapshot row with composite key `(snapshot_id, scope)` so backends sharing one schema never observe each other's rows (SPK-BE002 corrected the row-level realization to a scope-keyed row rather than a dedicated schema/connection, with a per-schema transaction-scoped advisory lock serializing concurrent first-touch schema initialization). The kernel hashing algorithm and canonical-blob identity are unchanged; only the substrate lookup is scoped, and the host-facing Durable-Read Surface needs no scope parameter (the runtime instance is constructed against one scope-bound backend). The `docs/KrakenKernelSpecification.md` §2.3 scope-resolved-identity claims and the framework scope notes were promoted through the authority cascade from `missing-conformance-follow-up` to authority-backed conformance: the kernel-protocol authority packet declares `bindingSections.scope-isolation` and registers the runnable `kernel-scope-isolation.json` plan (packet bumped 0.5.0 → 0.6.0), and the shared-core operational-telemetry plan gained a `scope-isolation.telemetry-and-transcript-correlation` check (plan 0.2.0 → 0.3.0) proving telemetry and transcripts are scope-correlated and leak no other Scope's data, with `tuvren.runtime.scope.id` added to the telemetry semconv and a required `scope` field on `TelemetryLineage`. The `data-models/` and `contracts/` pointer indexes are unchanged (the cross-implementation authority is boundary-owned, not constitution-owned). Cross-scope isolation conformance passes per backend with refreshed compatibility evidence; `bun run codegen`, the docs-to-authority freeze gate (264 classified claims), framework conformance (462/462 applicable), and a Rust kernel `cargo check` all pass. Stayed within logical container boundaries; the kernel remains data-only/protocol-only (ADR-001). This is the precondition Epic BF (ADR-051 reclamation/erasure) and the ADR-054 freeze sequencing build on.
- v0.30.0 - Specified the SaaS-Readiness delta (PRD v0.10.0 / Architecture v0.10.0; CAP-P0-064 through CAP-P0-070) and amended the docs/ specs in the same pass (resolved fork Q2). Added eight ADRs: ADR-048 (tenancy-agnostic scope seam bound at backend/connection construction, kernel syscall surface stays scope-free — resolved fork Q1), ADR-049 (isolation-by-construction: durable identity is scope-resolved, no cross-scope existence oracle), ADR-050 (backend-authoritative lease clock for shared multi-worker backends; single-writer embedded backends keep the in-process clock), ADR-051 (data lifecycle: kernel reachability reclamation primitive + crypto-shredding erasure of host-key-encrypted untrusted-edge payloads), ADR-052 (side-effect-once under preemption via an idempotency envelope `(runId, callId, fencingToken)`, reusing existing `idempotent`/`nonRetryable`/`maxRetries` metadata, client-result-as-proposal), ADR-053 (Tuvren is the unconditional conversation-state owner; provider state is a reconstructable optimization — extends ADR-005), ADR-054 (public SDK API stability + registry publication, experimental `@tuvren/core/capabilities` subpath, freeze-after-tenancy+GC — resolved fork Q3), and ADR-055 (native provider clients deferred behind a named trigger + a bridge `providerExecuted`/`dynamic` fidelity audit). Annotated ADR-005 as extended by ADR-053/ADR-055. Updated `stack.md` posture (provider state-ownership, scoped-storage backend + shared-lease-clock + reclamation capability bits, new tenancy/isolation, data-lifecycle/erasure, execution-sovereignty, and SDK distribution/stability postures). Amended `docs/KrakenKernelSpecification.md` to v0.12 (§2.3 scope-resolved object identity; §5.2 backend-authoritative lease clock; new §9.4 capability-gated `maintenance.reclamation` reachability primitive; converted the Appendix C deferred-GC note to a maintenance entry; the current syscall surface remains 30 operations across 10 groups with the reachability reclamation primitive documented as the forthcoming, deferred 31st operation) and `docs/KrakenFrameworkSpecification.md` to v0.21 (conversation-state ownership note on `providerMetadata`; backend-authoritative lease clock and side-effect-once-under-preemption / client-result-as-proposal additions at the Running Lease Ownership section; Basis bumped to Kernel v0.12). The new spec sections are marked SaaS-readiness targets whose portable machine authority is deferred until the implementation epics promote them through the authority-packet/conformance/evidence cascade; the docs-to-authority freeze gate classifies them honestly (version-status headers `explicitly-deferred`, each target claim `missing-conformance-follow-up` tracked to `KRT-BE001`/`BF001`/`BG001`/`BH001`), so the gate and `bun run verify` pass green on this planning branch with no false conformance coverage, and each epic's first ticket promotes its tracked claim to `authority-backed-conformance-covered` as it lands. No implementation source, schema, generated artifact, conformance plan, or authority packet was modified; those are Stage 4 epic deliverables. Stayed within logical container boundaries; no product-scope or container redesign. Traces to PRD CAP-P0-064 through CAP-P0-070 and Architecture v0.10.0.
- Migrated the monolithic `constitution/TechSpec.md` into the modular `.constitution/tech-spec/` directory (stack, guidelines, adrs/, data-models/, contracts/). Content preserved verbatim; ADR and section heading slugs preserved for authority-packet anchors (adr-037/039/046, section-3-13, 4.6, 4.15, 2.1-compatibility-record).
- v0.29.7 - Epic BD (Trust-Boundary Security Hardening) closed, realizing ADR-043 (framework-enforced execution bounds) and ADR-044 (secret isolation) and independently verifying the ADR-039/ADR-044 approval and untrusted-input trust boundaries: `ExecutionBounds` and `ExecutionBoundExceededDetails` (§3.11) added to `@tuvren/core/execution` with the `execution_bound_exceeded` code in `@tuvren/core/errors` and the cooperative `TuvrenPrompt.signal` field added to the provider-api contract authority (KRT-BD005); the framework bounds guard implemented in `@tuvren/runtime` enforcing `maxIterations`/`maxToolCalls`/`maxWallClockMs` above driver discretion, clamping `AgentConfig.maxIterations`/`maxParallelToolCalls`/`defaultMaxParallelToolCalls`, throttling tool concurrency to `maxConcurrentToolCalls`, propagating the wall-clock deadline abort through `TuvrenPrompt.signal`/`ToolExecutionContext.signal` (forwarded by the owned `bridge-ai-sdk` and tool paths), ignoring late completions, finalizing a breach as a `failed` `ExecutionResult` with a fatal canonical `error` event before the failed `turn.end`, resolving abandoned work to the terminal `ignored` lifecycle state, and emitting the `execution.bounded` telemetry event (semconv source + regenerated allowlist updated in the same change) (KRT-BD006); the `runtime-api-execution-bounds` check set added with a runaway aimock driver fixture (KRT-BD007); the transcript backend-options redactor added to `@tuvren/repl-host` masking PostgreSQL `connectionString`/`password` and credential-shaped backend options while retaining replay topology (KRT-BD002); edge-confinement READMEs + reusable secret-isolation fixtures added (KRT-BD003); the `secret-isolation` check set added across `providers-mcp-client.json`, `framework-operational-telemetry.json`, and `runtime-api-callables-extended.json`, backed by a new shared runner-owned secret-absence helper and `secretAbsence` assertion kind covering raw, bearer-prefixed, header-normalized, URL-encoded, base64/base64url, and partial-token leak forms over persisted records, stream events, telemetry, and transcript surfaces (KRT-BD004); the independent `trust-boundary` check set added pinning non-bypassable approval gating, `tool_input_validation_failed` for local input violations, and `mcp_tool_input_invalid` for MCP-advertised input violations rejected pre-transport — no implementation gap was exposed (KRT-BD009); §5.6.3/§5.6.4 migration plans recorded as closed outcomes. `docs/KrakenFrameworkSpecification.md` bumped to v0.20 with a normative §4.12 Execution Bounds section (KRT-BD008); docs-to-authority freeze gate extended with the execution-bounds evidence template and §4.12 classifier (253 classified claims verified); compatibility evidence refreshed; `bun run verify` exits 0 across all gates with postgres running. The active execution plan is now empty.
- v0.29.6 - Epic BC (Tooling Restructuring Closeout) closed: `KrakenFrameworkSpecification.md` bumped to v0.19 with normative §11 Capability Orchestration (Tool Surface vs Capability, four execution classes with per-class observation limits, Bindings and EndpointKind values, exposure-time and invocation-time policy decision points, the conceptual invariant); docs-to-authority freeze gate extended with `capabilityOrchestration` evidence template mapping §11 to the capability-orchestration-integration conformance plan and associated authority; portability inventory v0.4.0 registers `boundaries/framework/conformance/plans/capability-orchestration-integration.json` as a required authoritative source for `tuvren.shared.core`; `capability-orchestration-integration` conformance plan (19 checks, `bc.bc001.*` prefix) exercising all four execution classes, MCP-as-binding, exposure/invocation policy, and per-class observation limits registered in `tuvren.shared.core` authority packet; compatibility evidence refreshed; `bun run verify` exits 0 with 446/446 applicable framework checks passing. The Tooling block (Epics AW–BC) is fully closed; Epic BD (Trust-Boundary Security Hardening) is next.
- v0.29.5 - Epic BB (Exposure & Invocation Policy Model) closed: `PolicyCapabilityMetadata` type added to `@tuvren/core/capabilities` (six optional policy fields: riskClass, requiredResidency, requiresUserPresence, requiredCredentialScopes, nonRetryable, requiresActiveEndpoint); `CapabilityPolicyContext` extended with all §4.21 dimensions (BB001 allowedResidencies, BB002 implicit via engine options, BB003 userPresent/unavailableCapabilityIds, BB004 availableCredentialScopes, capabilityMetadata); `TuvrenToolDefinition` gains five optional policy fields; `CapabilityPolicyContextInputs` interface and `AgentConfig.policyContextInputs` wired; `BasicCapabilityPolicyEngine` implements all five dimensions (residency, risk/approval, active-endpoint, user-presence, credential-boundary) with deterministic composition (deny-from-any wins) and non-empty reason string; `CapabilityPolicyEngineOptions` gains `maxExposedRiskClass`/`requireApprovalForRiskClass`; `buildCapabilityMetadataFromTools` helper builds per-capability metadata from tool definitions including auto-setting `requiresActiveEndpoint` for tuvren-client tools; exposure-time filtering wired in `createDriverExecutionContext` — withheld surfaces never reach the driver or model; invocation-time policy context populated from real `AgentConfig.policyContextInputs`; resume-path invocation check added to `resolveResumeDecision` (the tool-resume `createToolBatchEnvironment` local copy now carries the policy engine fields); `nonRetryable: true` overrides `idempotent: true` in the retry budget; `requiresApproval: true` from the policy engine bridges into the pending-approval flow; `capability-policy` conformance check set (26 checks, BB001–BB005 at both decision points + composition + control path) registered in `tuvren.shared.core` authority packet; compatibility evidence refreshed. 472 runtime tests pass; 425/425 framework conformance checks pass. §5.7.2 BB entry updated to reflect closure.
- v0.29.4 - Epic BA (Invocation Lifecycle & Observation Model) closed: `InvocationLifecycleState` union type added to `@tuvren/core/capabilities` (6 phases: resolved → policy-admitted → dispatched → completed/failed/ignored); provider-native/mediated `tool.start`/`tool.result` attribution events routed through `publishRuntimeEvent` (BA002 telemetry gap closed); `null` used as the JSON-serializable "not observed" sentinel for provider tool inputs; cross-class resume/recovery semantics proven (BA003: tuvren-server fails clean per durability, provider classes resolve from observed state, tuvren-client stale/unavailable surfaces CAPABILITY_RESULT_STALE/CAPABILITY_BINDING_UNAVAILABLE, turn abort terminates cleanly); lifecycle telemetry depth confirmed using existing semconv without extension (BA004); `invocation-lifecycle-observation` conformance check set (19 checks) registered as an executable verification path. 424 runtime tests pass; 399/399 framework conformance checks pass; `bun run verify` exits 0. §5.7.2 BA entry updated to reflect closure.
- v0.29.3 - Epic AY (Provider-Native & Provider-Mediated Execution Classes) closed: `ProviderNativeToolDeclaration`/`ProviderMediatedToolConfig` added to `TuvrenPrompt` and `AgentConfig`; `TuvrenModelResponse.providerToolResults` carries provider-owned invocation records; `admitProviderTools` applies invocation-time policy in the react-driver prompt layer; `emitProviderToolAttributionEvents` emits `tool.start`/`tool.result` with `owner:"provider"` and per-class observation limits (canAudit/canCancel/canRetry/canResume: false); pre-staged provider tool messages bypass the Tool Execution Gateway; `assertDriverMessages` guard extended for pre-staged provider messages; `isProviderOnlyResponseEventSet` guard in `validateDriverAssistantEvents`; `createProviderPrompt` and `createAroundModelContextSnapshot` thread provider fields through the prompt chain; `provider-native-execution-class` (10 checks) and `provider-mediated-execution-class` (10 checks) added to `tuvren.providers.provider-api` authority packet; 52/52 provider conformance checks pass. §1.2 posture updated; §4.4 deferral note removed. Known gap: AY005 multi-turn providerContinuity extraction round-trip is structurally wired but not covered by a multi-turn test.
- v0.29.2 - Epic AX (Tuvren-Server Execution Class) closed: `TuvrenToolDefinition` extended with `outputSchema?`, `idempotent?`, `maxRetries?`; `AgentConfig` extended with `serverExecution?` (rate-limit config) and `sandboxExecutors?`; `TOOL_INPUT_VALIDATION_FAILED`, `TOOL_RESULT_VALIDATION_FAILED`, `TOOL_INVOCATION_RATE_LIMITED` added to `@tuvren/core/errors`; `TuvrenSandboxExecutor` interface in `@tuvren/core/capabilities`; `ServerRateLimiter` (per-turn per-instance fixed-window); `ToolAuditEvent` in `TuvrenStreamEvent`; `tuvren-server-execution-class` conformance check set (19 checks). §1.2 current-capability-orchestration-posture updated.
- v0.29.1 - Epic AW (Capability Orchestration Foundation) closed: `@tuvren/core/capabilities` subpath with §3.13 types + `CapabilityPolicyEngine` interface, Capability Registry, Binding & Endpoint Resolver (`capability_binding_unavailable` in `@tuvren/core/errors`), Capability Policy Engine wired into tool dispatch (invocation denials → `tool.result isError:true`), `AgentConfig.capabilityPolicyEngine?` host configuration seam, attribution on `ToolStartEvent`/`ToolResultEvent`/telemetry spans, semconv extended, authority packet v1.2.0, and the `runtime-api-capability-orchestration` foundation conformance check set. §1.2 current-capability-orchestration-posture updated; §5.7.1 step coverage now reflected in repository reality.
- v0.29.0 - Specified the capability-orchestration model (PRD v0.9.0 / Architecture v0.9.0): ADR-046 the compositional model (Tool Surface vs Capability, four execution classes, Bindings, Endpoints, exposure/invocation Policy, per-class Observation, MCP-as-binding, the conceptual invariant; CAP-P0-056 through CAP-P1-063) and ADR-047 the phased, back-compatible execution-class realization (today's `defineTool` / Tool Execution Gateway path is the Tuvren-server class). Added §3.13 concept shapes, §4.21 the capability-orchestration contract, the `@tuvren/core/capabilities` subpath, §5.7 the migration plan mapping the source implementation to the active Tooling block (Epics AW–BC), and §2.1 compatibility entries. Because the Tooling block precedes it, the former Epic AW (trust-boundary security hardening) is renumbered to Epic BD in §1.2 and §5.6.
- v0.28.1 - Maintenance alignment for current repository state: Epic AU (ADR-045 recovery-and-durability verification) is closed with the testkit-only fault-injection seam, `kernel-crash-recovery` conformance coverage, Crash Recovery Invariant note, and refreshed compatibility evidence; Epics AV and AW remain the active production-trust implementation scope. No contract or schema semantics changed.
- v0.28.0 - Specified the production-trust block (PRD v0.8.0 / Architecture v0.8.0): ADR-042 first-class operational telemetry surface plus vendor-neutral OpenTelemetry export (CAP-P0-052 / CAP-P1-053); ADR-043 framework-enforced execution bounds with a typed `execution_bound_exceeded` terminal result (CAP-P0-054); ADR-044 secret isolation across durable, telemetry, and transcript surfaces (CAP-P0-055); ADR-045 recovery-and-durability verification via a test-only fault-injection seam plus a strengthened crash-recovery conformance plan (CAP-P0-005 / CAP-P0-006). Added §3.10–§3.12 data shapes, §4.18–§4.20 interface contracts, §5.6 migration plans, and §2.1 compatibility entries.
- ... [Older history truncated, refer to git logs]
