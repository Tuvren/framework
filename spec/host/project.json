{
  "name": "host-spec",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "spec/host",
  "projectType": "library",
  "tags": ["boundary:framework", "layer:contract"],
  "targets": {
    "codegen": {
      "executor": "nx:run-commands",
      "inputs": [
        "default",
        "^production",
        "{workspaceRoot}/spec/host/typespec/**/*",
        "{workspaceRoot}/spec/host/artifacts/**/*",
        "{workspaceRoot}/bun.lock"
      ],
      "outputs": ["{workspaceRoot}/spec/host/artifacts/json-schema"],
      "options": {
        "cwd": "spec/host",
        "command": "bunx --bun tsp compile ./typespec --pretty false && bunx --bun @biomejs/biome check --write ./artifacts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["default", "{workspaceRoot}/biome.jsonc"],
      "options": {
        "cwd": ".",
        "command": "bunx --bun @biomejs/biome check spec/host"
      }
    }
  }
}
