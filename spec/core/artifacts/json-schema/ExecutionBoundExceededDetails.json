{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tuvren.dev/schemas/shared/core-types/ExecutionBoundExceededDetails.json",
  "type": "object",
  "properties": {
    "bound": {
      "$ref": "https://tuvren.dev/schemas/shared/core-types/ExecutionBoundKind.json"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991,
      "description": "The configured limit for the breached bound."
    },
    "observed": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "The observed value at breach time."
    }
  },
  "required": ["bound", "limit", "observed"],
  "unevaluatedProperties": {
    "not": {}
  },
  "description": "Details carried by the execution_bound_exceeded error, the fatal canonical error event, and the bounded-execution telemetry event when a hard-stop bound is breached. (ADR-043)"
}
