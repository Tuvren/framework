name: tuvren_kernel_adapter
description: Dart kernel conformance adapter speaking the Tuvren adapter protocol.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0
dependencies:
  tuvren_kernel: ^0.1.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
