name: tuvren_kernel
description: Tuvren kernel runtime authority for the Dart port.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0
dependencies:
  crypto: ^3.0.6
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
