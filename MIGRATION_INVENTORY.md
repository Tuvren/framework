# Migration Inventory — Epic #87 (Sovereign Polyglot Framework Restructure)

Tracks GitHub issue [#87](https://github.com/Tuvren/runtime/issues/87), milestone M0 ("Ground rules"). This file is the machine-checkable, 100%-coverage ledger of every contract, driver, runner, hook, script, and implementation touched by the restructure: `old path → new path → status → content hash → notes`. It is deleted at cutover (M10) per issue §12.

Out of scope for this ledger (not part of the physical migration surface): `.constitution/` (a separate, non-frozen governance tree rewritten by M10's own DoD, not relocated by this restructure), `docs/*` (issue §1/§5: "timeless semantic authority" that survives unmoved), and `tests/` (empty root scaffold — `.gitkeep` only, no content to migrate).

## Schema

| Column | Meaning |
|---|---|
| `old_path` | Repo-relative path today. Directory-granularity for uniform boilerplate trees; file/subpath-granularity for anything §12 names explicitly. |
| `new_path` | Destination under `spec/<port>/` or `<lang>/...`, or "no move" for teardown-only/unaffected roots. Where the exact idiomatic subpath is a future milestone's decision (not yet made), this is marked `(idiomatic subpath decided at M<n>)` rather than guessed now — deciding it early would violate the issue's own "vertical, no long red valley" sequencing (§11). |
| `status` | `pending` \| `in-progress` \| `migrated` \| `not-applicable` (no move planned; still enumerated for 100% coverage, never silently omitted, per invariant §10.4 — no deletion without a proven successor). |
| `content_hash` | The **git blob SHA** (files) or **git tree SHA** (directories) via `git rev-parse HEAD:<path>`, captured at this baseline commit — not a separately computed SHA-256. Git already computes this for every path, and it composes directly with the primary move mechanism (`git mv`): a pure rename preserves file blob hashes exactly, so post-move equivalence is a one-line hash comparison instead of a bespoke hashing step. Rows where content is genuinely *transformed* (not moved), e.g. M2's sort of `@tuvren/core` into contract vs. executable tiers, are proven by a documented transformation note instead of expecting hash equality. |
| `notes` | Multi-home/ambiguous cases named in §12, cross-port moves, and any decision rationale. |

All hashes below were captured at the M0 baseline commit on branch `feat/epic-87-sovereign-polyglot-framework` (recorded in the Baseline section appended by M0.2).

## Kernel (→ `spec/kernel/`, `spec/conformance/kernel/`, `spec/interop/`, `rust/kernel`, `typescript/kernel/...`) — M1

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/kernel/contracts/protocol` (port-root `README.md` + `artifacts/README.md` only, 17 lines total, not covered by the row below — `implementations/typescript` remains under `boundaries/` until M1.4, see that row) | `spec/kernel/README.md` (merged with the nested `spec/README.md` below) + `spec/kernel/artifacts/README.md` | migrated (M1.3) | merged, not a pure move — see notes | `artifacts/README.md` pure move, blob unchanged (`22b7cf36fd71f28174c0f033a83b065bcf5a338b`); port-root `README.md` rewritten as `spec/kernel/README.md` (new blob `62dcc8728cc39048b0449fb34fc07932d53f42d1`) folding in the nested `spec/README.md`'s CDDL-promotion note (that file is `git rm`'d, its content preserved in the merged README, not duplicated) |
| `boundaries/kernel/contracts/protocol/spec` | `spec/kernel/` | migrated (M1.3) | tree changed — see notes | `cddl/kernel-records.cddl` pure move, blob unchanged (`694acf4bf065578ed15dd6c1b453d496079effaa`); `authority-packet.json` content-edited (internal path references updated to the new `spec/kernel/` and `spec/conformance/kernel/` locations; `bindingProjections`/`forbiddenAuthoritySources` implementation paths untouched, still M1.4), new blob `32c8941d0098120b615c1fdd173bbc5ec9cb0bdd`; nested `spec/README.md` merged into `spec/kernel/README.md` per the row above |
| `boundaries/kernel/conformance` | `spec/conformance/kernel/` | migrated (M1.3) | `0c49c4ef66fd1d10984695f5b725df7129dd9c48` | fixtures/plans/scenarios/schemas; pure `git mv`, content-identical — tree SHA verified equal to the pre-move HEAD (`cb869829`, M1.2's close), not the stale M0 baseline snapshot recorded here originally, which predates M1.1 adding `fixtures/BUILD.bazel` into this same tree (legitimate baseline drift, not a discrepancy) |
| `boundaries/kernel/interop/grpc` | `spec/interop/` | migrated (M1.5) | tree changed — see notes | IPC transport contract (proto/grammar only). Content-edited, not a pure move: `authority-packet.json`'s internal proto/`generatedArtifacts`/`bindingProjections`/`forbiddenAuthoritySources`/`verificationPaths`/`freshnessChecks` paths recomputed for `spec/interop/`, and its stale `runtime-core` references corrected to the actual current package `runtime` (confirmed via M0's Multi-home §A baselining — `buf.gen.yaml` and `tools/scripts/kernel-interop-governance.ts` already pointed at `runtime`, only the authority-packet.json, `project.json`'s `codegen` inputs/outputs, and `nx.json`'s `interop-smoke` target defaults still named the dead `runtime-core` shim); `project.json`'s `root`/`$schema`/lint-command path recomputed for its new depth-2 home (no other `spec/<port>/` directory carries a `project.json` today, but this one drives real `buf generate`/`breaking`/`interop-smoke` tooling with no other sanctioned home per §5's tree, so it stays co-located with the authority content it governs, mirroring its pre-move co-location). New tree hash `a2254347f56d7486ba6bbbf7b814d32168d8593f` (`git write-tree --prefix=spec/interop/`, recomputed after confirming the edited content was actually staged — an earlier `git add` invocation silently failed on a stale pathspec and the first hash taken was computed against pre-edit content). External consumers fixed: `buf.yaml`'s module path, `rust/kernel-grpc-service/build.rs`'s `proto_root`, `tools/scripts/kernel-interop-governance.ts`'s `PROTO_ROOT`, `nx.json`'s `interop-smoke`/rust-`test` target-default globs (added `spec/interop/**/*`, kept `boundaries/*/interop/**/*` since `boundaries/framework/interop/rust-kernel` is still unmoved, M8's job), and `.constitution/reports/epic-al-portability-inventory.json`'s `tuvren.kernel.interop-grpc` `packetPath` + 2 `sourcePath` entries (a real `portability:check` failure, not a static-grep find — the AL portability gate hard-fails on stale inventory paths). See Multi-home §A: the *telemetry* half of that drift (a different, unrelated stale `outputs` target) stays M8's job, untouched here. |
| `boundaries/kernel/contracts/protocol/implementations/typescript` (`@tuvren/kernel-protocol`) | `typescript/kernel/protocol` | migrated (M1.4b) | `62239c9a8ac39c54173d2135faf7ed9c9df0d1e1` | content-edited (project.json root/schema/inputs, tsconfig* extends/rootDir/references/paths, test-file relative CDDL and core-types imports) — see notes below the table for the shared M1.4b reference-sweep summary |
| `boundaries/kernel/implementations/typescript/runtime-kernel` (`@tuvren/kernel-runtime`) | `typescript/kernel/runtime` | migrated (M1.4b) | `dc495d1d7f8e9e1a33a59f100661ebb24aa37557` | renamed `runtime-kernel` → `runtime` (idiomatic; the package/npm name `@tuvren/kernel-runtime` is unchanged) |
| `boundaries/kernel/implementations/typescript/backend-memory` | `typescript/kernel/backends/memory` | migrated (M1.4b) | `84becb10199c458b70b965defa1a407d6cc41b32` | storage driver; nested one level deeper under a new `backends/` grouping folder (matches issue §4's own "storage drivers (backends: memory / sqlite / postgres)" wording) |
| `boundaries/kernel/implementations/typescript/backend-sqlite` | `typescript/kernel/backends/sqlite` | migrated (M1.4b) | `18a709885c12ce204881d7aeed98911d1fdef3f2` | storage driver; also fixed a hardcoded `.tmp-tests/boundaries/kernel/...` path inside `test/backend-sqlite-test-helpers.ts` that mirrored the package's own old on-disk depth |
| `boundaries/kernel/implementations/typescript/backend-postgres` | `typescript/kernel/backends/postgres` | migrated (M1.4b) | `770b1d2474c49748ddf7bef85b308c223c84a8e4` | storage driver; decided (M1.6) to stay Nx-driven, outside the Bazel graph — see Multi-home §B |
| `boundaries/kernel/implementations/typescript/testkit` (`@tuvren/kernel-testkit`) | `typescript/kernel/testkit` | migrated (M1.4b) | `6b92b9106a4690b9582e257257da20886a21a0bb` | also broadened `test/kernel-testkit.test.ts`'s fault-injection-containment ripgrep search from `boundaries` only to `boundaries typescript`, since the surface it guards now spans both trees |
| `boundaries/kernel/implementations/typescript/conformance-adapter` | `typescript/kernel/conformance-adapter` | migrated (M1.4b) | `7b0f82f81958373e270ca147d424552fa0def748` | one of the 8 `conformance-runner`-family dirs, see Multi-home §C |
| `boundaries/kernel/implementations/typescript/conformance-runner` (`@tuvren/kernel-typescript-conformance-runner`) | `typescript/kernel/conformance-runner` | migrated (M1.4b) | `441334e0178baccaf090fa7b1f8af440f78f45ab` | project ID in `package.json`'s `conformance` script |
| `boundaries/kernel/implementations/typescript/conformance-runner-sqlite` | `typescript/kernel/conformance-runner-sqlite` | migrated (M1.4b) | `9c993e69801d811b21c82e508656da02695f96f4` | project ID `kernel-typescript-sqlite-conformance-runner` |
| `boundaries/kernel/implementations/typescript/conformance-runner-postgres` | `typescript/kernel/conformance-runner-postgres` | migrated (M1.4b) | `cab72dddbf9403a4ebc0f77d3648709638cf9455` | project ID `kernel-typescript-postgres-conformance-runner`; decided (M1.6) to stay Nx-driven, transitionally non-hermetic per invariant §10.5, see Multi-home §B |
| `boundaries/kernel/implementations/rust/kernel` (`tuvren-kernel-rust`) | `rust/kernel` | migrated (M1.4a) | `04dcd517e99de1844b893d48214ba4faa96f1c86` | Cargo workspace member; `path =` in root `Cargo.toml` updated. Tree content-edited, not a pure move: `project.json`'s `root`/`$schema` updated, and `tests/kernel_baseline.rs`'s `include_str!` fixture path recomputed for the new (shallower) depth. Compared against pre-M1.4 HEAD (`68f2e864`), not the stale M0 baseline recorded here originally, which predates M1.1's `BUILD.bazel` addition |
| `boundaries/kernel/implementations/rust/grpc-service` | `rust/kernel-grpc-service` | migrated (M1.4a) | `b670f36e6bcffb11e32274445b2cd93899867e61` | Cargo workspace member; sibling to `rust/kernel`, not nested under it (avoids a `rust/kernel/kernel` double-nest). Content-edited: `project.json`'s `root`/`$schema` updated, `build.rs`'s `proto_root` recomputed (at M1.4a still pointed at `boundaries/kernel/interop/grpc/proto`, correct at the time since that tree hadn't moved yet; M1.5 subsequently re-pointed it to `../../spec/interop/proto`) |
| `boundaries/kernel/implementations/rust/conformance-adapter` | `rust/kernel-conformance-adapter` | migrated (M1.4a) | `15c3293f6f48c1023400a4c78bfafc027e999939` | Pure move — no `project.json` wrapper exists for this crate, and `adapter.json`/`main.rs`/`Cargo.toml` were untouched by this commit, so the tree hash matches the pre-M1.4 HEAD (`68f2e864`) exactly, not the stale M0 baseline recorded here originally (M1.3 had already edited this crate's `adapter.json` `authorityPackets` path, which is why the two differ) |
| `boundaries/kernel/implementations/rust/conformance-runner` (`kernel-rust-conformance-runner`) | `rust/kernel-conformance-runner` | migrated (M1.4a) | `b2caee98eb5796d14726cc121977e809682640a5` | project ID in `conformance` script. Content-edited: `project.json`'s `root`/`$schema`/`inputs`/adapter-path command arg updated, `src/main.rs`'s diagnostic message path updated |

All 10 M1.4b TypeScript rows above are content-edited, not pure moves — every package's own `project.json` (`root`/`$schema`/`cwd`/inputs) needed updating, and most also had `tsconfig*.json` `extends`/`rootDir`/`references`/`paths` entries recomputed for the new depth (kernel packages moved from nested inside `boundaries/` to a root-level `typescript/kernel/` tree, so every cross-package and cross-boundary relative reference's up-count and, for cross-boundary targets, an explicit `boundaries/` segment needed recomputing). The sweep also touched ~17 files **outside** the kernel tree that reference these packages by relative path from their own unmoved locations: `boundaries/framework/contracts/{driver-api,event-stream,runtime-api,tool-contracts}/implementations/typescript/tsconfig.typecheck.json`, `boundaries/framework/implementations/typescript/{conformance-adapter (3 files),drivers/react,runtime-core (2 files),runtime (3 files)}`, `boundaries/hosts/implementations/typescript/repl/{tsconfig.dts,tsconfig.lib,tsconfig.typecheck}.json`, and `boundaries/providers/{contracts/provider-api/implementations/typescript,implementations/typescript/bridge-ai-sdk}/tsconfig.typecheck.json` — none of those rows change (their own locations are unaffected), but they are recorded here since `bun run check`'s affected-typecheck lane initially caught one real miss (`framework-adapter-batteries-included.ts`/`framework-adapter-runtime.ts`) that a plain path-string grep sweep alone did not, because those referenced the kernel packages via relative paths without the literal `boundaries/kernel` substring.

## Core / ABI (→ `spec/core/` + TypeScript libc/SDK tier) — M2

**M2.1 sort decision (recorded before any M2 move, per the M2 DoD's "before this milestone starts" requirement).** Two tracks run in parallel and the rows below name both: (a) the **authority** track — the neutral contract form (TypeSpec `main.tsp`, `authority-packet.json`, `bindings/typescript.md`) lifts from `boundaries/shared/contracts/core/spec/` to root `spec/core/` at M2.2; (b) the **physical TS source** track — the `@tuvren/core` implementation moves to `typescript/core` at M2.3 (npm name and all 11 subpath exports unchanged), `@tuvren/core-types` moves to `typescript/core-types` (M0's `spec/core/` destination guess for it is corrected here: §5's layout rule forbids language-bearing code in `spec/`), and the executable helpers extract to a **new libc/SDK-tier package `@tuvren/sdk` at `typescript/sdk`** at M2.4. The SDK home cannot be `@tuvren/runtime` (the issue's "concrete libc surface today") because `typescript/kernel/conformance-adapter/src/host.ts` and `@tuvren/mcp-client` (whose only `@tuvren` dep is `core`) consume the helpers — routing them through the engine package would invert layering; a small SDK package that depends only on `@tuvren/core` preserves direction for every consumer (runtime, the kernel and framework conformance adapters, mcp-client). Extraction purifies the ABI package: `zod` + `@standard-schema/spec` are `@tuvren/core`'s only (peer) dependencies and exist solely for `schema-authoring.ts`, so they leave with it. See Multi-home §D (resolved) for the file-by-file classification. The M2 DoD's required enumeration is complete: every `@tuvren/core/*` subpath row below now names its destination tier, `core-types`'s destination is corrected, and the four deprecated shims (`runtime-api` → M3-or-M9, `driver-api` → M6, `event-stream` → M8, `tool-contracts` → M5 — rows further below) were measured at M2.1 (amended after review caught the first draft's overclaim) to have **zero value-level import sites**. Residual consumer-rewrite work that *does* hide behind them, for their owning milestones: `runtime-api` retains 9 type-position `import("@tuvren/runtime-api")` references across 4 files (`runtime/src/lib/runtime-core-{hosts,observability}.ts`, `runtime/test/durable-reads.test.ts`, `conformance-adapter/src/framework-adapter-runtime-scenarios.ts`) — the shim must stay resolvable until those are rewritten; `event-stream` retains 2 external `package.json` dependency declarations (framework `testkit`, `conformance-runner`). `driver-api` and `tool-contracts` are genuinely reference-free; `runtime-core` has one import site (its own `smoke/package-exports.ts`).

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/shared/contracts/core/implementations/typescript/src/errors` | `typescript/core/src/errors` (physical, M2.3); vocabulary owned by `spec/core` authority (M2.2) | migrated (M2.3) | `1ecc9c117f4b97326aa9a7d9256554fc2cd42b79` | pure types, per M2 DoD |
| `boundaries/shared/contracts/core/implementations/typescript/src/messages` | `typescript/core/src/messages` (M2.3); vocabulary → `spec/core` (M2.2) | migrated (M2.3) | `c2c96565913caa0381cc2aa46feb5cc064453241` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/events` | `typescript/core/src/events` (M2.3); vocabulary → `spec/core` (M2.2) | migrated (M2.3) | `d17119b90bf18bf6200db2240c443814c576e5f5` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/execution` | `typescript/core/src/execution` (M2.3); vocabulary → `spec/core` (M2.2) | migrated (M2.3) | `a43354c5b61c99956cd412b7bff7c410f8883d4e` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/tools` | `typescript/core/src/tools` (M2.3); its executable re-exports (`asSchema`, `defineTool`, `jsonSchema`, `zodSchema`, `standardSchema`, `schemaSymbol`, and the `Schema`/`FlexibleSchema`/`LazySchema`/`StandardSchema`/`ZodSchema` authoring types) leave for `@tuvren/sdk` (M2.4); tools-port *authority* consolidates at `spec/tools` (M5) | migrated (M2.3 move + M2.4 extraction) | `9219414da2ca938c97465a36f33745974b0b7ce5` | after M2.4 this subpath exports only the contract surface (guards + shapes from `runtime-contract-guards`/`-shapes`) |
| `boundaries/shared/contracts/core/implementations/typescript/src/lib` (15 files) | 13 files stay in `@tuvren/core` at `typescript/core/src/lib` (M2.3); `schema-authoring.ts` moves wholesale to `@tuvren/sdk` (M2.4); `payload-codec.ts` splits intra-file — contract portion stays, implementation portion → `@tuvren/sdk` (M2.4) | migrated (M2.3 move + M2.4 extraction) | `92e552689bd4674bf58b77f0d69a81e542125c3c` | **§D resolved at M2.1** — see Multi-home §D for the file-by-file classification and the intra-file split boundary for `payload-codec.ts` |
| `boundaries/shared/contracts/core/implementations/typescript/src/driver` | `typescript/core/src/runner` (landed at M2.3 as `src/driver`; subpath renamed at M6.4); execution-model *authority* consolidates at `spec/runners` (M6); the subpath's driver→runner rename is M6's | migrated (M2.3) | `742ebd2aa77fa05a32d679b901754ad629294d5c` | "driver" here means execution-model per M6's inversion, not resource adapter |
| `boundaries/shared/contracts/core/implementations/typescript/src/provider` | `typescript/core/src/provider` (M2.3); provider-port *authority* → `spec/providers` (M4) | migrated (M2.3) | `7797c38b224b019cd9bc2b2d5a4a63aa748cff31` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/extensions` | `typescript/core/src/extensions` (M2.3); extension-port *authority* → `spec/extensions` (M7) | migrated (M2.3) | `bfa11ded81c200febb00a3dd163421c9ac4e024e` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/telemetry` | `typescript/core/src/telemetry` (M2.3); telemetry *authority* → `spec/telemetry` (M8) | migrated (M2.3) | `9ddf7e69d30873500607115d725cf372b16e3998` | see Multi-home §A for the telemetry 3-home reconciliation |
| `boundaries/shared/contracts/core/implementations/typescript/src/capabilities` | `typescript/core/src/capabilities` (M2.3); capability *authority* → `spec/tools` (M5) | migrated (M2.3) | `16c2dd586ff333fa9c286cc8311ee34b6a3da41a` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/lifecycle` | `typescript/core/src/lifecycle` (M2.3); after M2.4 it exports only the payload-codec *contract* surface (`PayloadCodec`, `PayloadCodecContext`, `PayloadDecryptResult`, `ErasedPayload`, `isErasedPayload`) — the codec implementations move to `@tuvren/sdk` | migrated (M2.3 move + M2.4 extraction) | `9ebaa250d48b5926750c756076fae071222ad1dc` | |
| `boundaries/shared/contracts/core/implementations/typescript/src/index.ts` (root export) | `typescript/core/src/index.ts` (M2.3), minus any executable re-exports that leave at M2.4 | migrated (M2.3 move + M2.4 extraction) | `6d24e8e9d3d171955b7e1a12a6a75001d99beab2` | |
| `boundaries/shared/contracts/core` (whole port dir: artifacts/spec, not just `src/`) | authority (`spec/` subdir + `artifacts/`) → `spec/core/` — **migrated (M2.2)**; implementation (`implementations/typescript`) → `typescript/core` — **migrated (M2.3)** | migrated (M2.2 authority + M2.3 implementation) | `87129aea2a17660eef2b6a5851f68abc1f8c0b3a` | superset of the `src/` subpath rows above. **M2.3:** `git mv` to `typescript/core`; `src/` and `test/` trees byte-identical pre/post move (`bb74d9d2…` and `fed03300…` respectively); only config files content-edited (project.json root/sourceRoot/$schema/command paths — Nx name `shared-core` kept, matching the kernel precedent of not renaming projects at move time; tsconfig extends depth 6→2). New `typescript/core` tree hash `c9d8bcc3031da1c6be47c2851c1ba2ef10c41fd4`. Packet `bindingProjections.typescript` + both `forbiddenAuthoritySources` implementation entries updated to the new homes (closing M2.2's deliberate deferral). Repo-wide reference sweep done by a resolution-based rewrite script (82 files: every relative/root-relative path string re-resolved under the post-move layout — consumer tsconfig references/paths across all boundaries, root tsconfig.json/tsconfig.base.json, `portability-check.ts`, `migrate-to-core.ts`), plus 4 path arguments embedded mid-string in the two moved project.json commands fixed by hand (the script only matched strings that *start* like a path). Workspaces glob `typescript/*` added (dirs without package.json, e.g. `typescript/kernel`, are skipped by bun); `bun install` re-links `@tuvren/core` → `workspace:typescript/core`, lockfile refreshed. `boundaries/shared/` is now empty and gone entirely. Also folded in: biome formatter normalization of lines M2.2's sed shortened in `docs-authority-freeze-gate.ts`/`epic-af-conformance-gap-plan.ts` (M2.2 left them formatter-dirty; only `verify` runs full-repo biome). The related verify.ts codegen-phase drift (`shared-core-types` → `core-spec`) landed as lead-in commit `bbfe4f1`. M2.2 moved `spec/` → `spec/core/{authority-packet.json,typespec/,bindings/}` and `artifacts/` → `spec/core/artifacts/` (pure `git mv`; the port root had no README). New `spec/core/` tree hash `a4150a30e623bb0dba66af754592d19537394b09` (`git write-tree --prefix=spec/core/`, computed after verifying the packet edits were actually staged, per the M1.5 lesson); `typespec/main.tsp` and `bindings/typescript.md` blobs unchanged (pure moves); `authority-packet.json` and `tspconfig.yaml` content-edited as described; `project.json` new. Packet self-paths recomputed; the superseded `core-types` `authoritativeSources` entry removed (its `errors` bindingSection is already claimed by the consolidated `main.tsp` entry); `bindingProjections`/`forbiddenAuthoritySources` implementation paths untouched, still M2.3 — mirroring M1.3's approach. `tspconfig.yaml` emitter path recomputed for the new depth; regeneration at the new location reproduces the checked-in artifacts byte-identically (verified: `core-spec:codegen` run, zero git diff under `spec/core/artifacts`). **Drift found and fixed (same class as Multi-home §A):** the root `codegen` lane ran `shared-core-types:codegen` (regenerating the superseded duplicate) but never `shared-core:codegen` (the live consolidated authority) — the live core artifacts were regenerable only by hand. M2.2 created `spec/core/project.json` (project `core-spec`, mirroring M1.5's `spec/interop` precedent), removed the codegen targets from both implementation projects, and swapped `shared-core-types` → `core-spec` in the root lane, fixing both halves in one move. The packet's `freshnessChecks.regenerateCommand` updated to `core-spec:codegen` — caught by an actual authority-guardrails freshness failure, not grep |
| `boundaries/shared/contracts/core-types` (`@tuvren/core-types`) — **two-track split, corrected twice** | authority residue (`spec/`, `artifacts/`, port-root READMEs) — **retired (M2.2)**, `git rm` with the equivalence evidence below; `implementations/typescript` (the deprecated shim package) → `typescript/core-types` — **migrated (M2.3)**. Its now-dangling `codegen` target (which regenerated the retired duplicate artifacts) removed from the shim's `project.json` and from the root `codegen` lane at M2.2 | migrated (M2.2 retirement + M2.3 shim move) | `74cc79ea321f488a45a7af49148ed70bc33f0b09` | named explicitly in §12 as a multi-home case. **M2.1 (amended after review):** M0's row sent everything (incl. TS code) to `spec/core/`, violating §5's no-language-code rule; the first M2.1 draft over-corrected, sending everything (incl. neutral authority) to `typescript/`. The port dir actually holds **superseded duplicate authority**: per ADR-037, `core-types` was the *anchor* renamed to `core/` — its authority packet was merged into `core/spec/authority-packet.json` (this residual `spec/` has **no** packet; its READMEs stale-reference one that no longer exists), all 21 of its TypeSpec models are covered by core's consolidated 255-line `main.tsp` (verified: zero models unique to core-types), and all 21 of its `artifacts/json-schema/*.json` are **byte-identical** to core's (verified via `cmp`, 21/21). Successor proven → the residue is `git rm`'d at M2.2 per invariant §10.4, not moved (moving to `spec/core/` would duplicate authority, §5 "not duplicated"; moving to `typescript/` would put authority in a language tree). The shim package itself (deprecation-`console.warn` re-exports from `@tuvren/core`, plus legacy standalone `src/lib` copies that are dead code — even the package's own tests import from `@tuvren/core`, not the local copies) physically moves at M2.3; six packages declare it as a dependency with zero import sites, and the only cross-package consumer is `typescript/kernel/protocol/test/kernel-contract-deterministic.test.ts` importing a test fixture via deep relative path (recomputed at M2.3 to `../../../core-types/test/kernel-record-fixtures.js`, kernel protocol tests green). **M2.3:** shim moved by `git mv`; `src/` and `test/` trees byte-identical pre/post move (`c7a5edd2…`, `36bd8010…`); new `typescript/core-types` tree hash `7831df363b4de0babe48bf8ec85696d2f553198e`; Nx name `shared-core-types` kept; its tsconfig references to `@tuvren/core` recomputed to the sibling `../core/…` form. Consolidation/removal of the shim is a future-epic semantic change, out of restructure scope |
| `boundaries/framework/contracts` (whole port-group dir, superset — catches `client-endpoint-integration.md` as a loose sibling file not covered by any per-port row below) | split across `spec/<port>/` per the per-port rows | **migrated** (flipped M10.1) | `3e2341e41b51cd0f983728824b8ac44cfa38edbf` | superset row added after review found the loose contract doc uncovered; this row's last unresolved child, `client-endpoint-integration.md`, migrated at M9.1 (row below) — `boundaries/framework/contracts` itself was removed whole at M9.2 alongside the rest of `boundaries/framework/` (confirmed on disk at M10.1: `boundaries/` now holds only the `kernel` and `hosts` empty-directory husks) |
| `boundaries/framework/contracts/client-endpoint-integration.md` (174-line `AttachedClientEndpoint` host-integration contract; imports from `@tuvren/core/capabilities`) | `spec/host/client-endpoint-integration.md` | **migrated (M9.1)** | `43cc28f21afcdb1138bfa732506ea5ec9a527c15` | loose sibling of the six per-port contract dirs; not a `tool-contracts` file itself despite the capabilities import. **M9.1:** pure `git mv`, blob unchanged; `spec/host/README.md` documents it with a cross-reference note pointing its capabilities vocabulary dependency at `spec/tools/` (the exact home question this row's original note raised is resolved: no split needed, the file stays whole at the port root) |
| `boundaries/framework/contracts/driver-api` (`@tuvren/driver-api`, deprecated shim) | `spec/runners/` (M6) | **migrated (M6.1b); shim retired (M6.1c)** | `a572ed22b6bc1517244113b2bd27e0c40ff1e4d1` | named explicitly in §12; consolidated with `react-driver` at M6.1b: `spec/typespec`+`spec/bindings`+`artifacts` lifted byte-pure to `spec/runners/{typespec,bindings,artifacts}` (pre-move trees: spec `c520d531`, artifacts `42cd5af6`; all R100), tspconfig emitter depth recomputed (`{project-root}` template class), codegen re-homed to new spec-owned `runners-spec` project (immediate run byte-clean over 41 artifacts; `framework-driver-api` lost its codegen target; root codegen list swapped). Both stale READMEs (claiming a `tuvren.framework.driver-api` packet that ADR-037 absorbed) retired into an accurate `spec/runners/README.md`. **M6.1c:** the import-dead `implementations/typescript` shim retired whole (M5.1b pattern; successor `@tuvren/core/driver` proven — the shim was a pure re-export barrel; import-dead re-verified at deletion: zero live import sites, only its own package.json declared it; §10.4 satisfied). Same-commit prunes: 3 verify.ts curated-list entries, root tsconfig alias+reference, 3 consumers' typecheck paths/includes (react, bridge-ai-sdk, runtime), portability-check.ts entry, core packet's dead forbiddenAuthoritySources entry (live-paths rule), lockfile (1 package removed). `spec/runners/bindings/typescript.md` repointed its binding-projection claim to `@tuvren/core/driver` (the retirement made the old claim stale). M6.1b-review P2 fold-in: the dead `tuvren.framework.driver-api` validate-plans map key actually PRUNED this time (M6.1b repointed it instead — no packet declares that ID since ADR-037). Contract root fully evacuated and removed. **M6.1c-review P2 fix (follow-up commit):** the shim's deleted test file was NOT the M5.1b barrel-test parallel — it held 20 behavioral accept/reject tests over `@tuvren/core`'s driver-contract guards (the successor's real logic); restored as `typescript/core/test/driver-contract-guards.test.ts` importing `../src/driver/index.ts` (20/20 green). **M6.1d-review corrections (M6.2b):** (1) the restored suite exercises 3 of the 6 exported guards (`isRuntimeDriver`, `assertRuntimeDriver`, `assertDriverExecutionResult`) — the original covered the same 3, so no regression, but `assertDriverRuntimeResolution`/`assertDriverHandoffContextPlan`/`assertDriverHandoffSourceContext` have zero dedicated unit coverage anywhere (pre-existing gap, recorded for the M10 close-out notes; authoring new tests exceeds §9 restructure-only scope). (2) **Commit-scope incident:** `9b573a52` (M6.1d) accidentally swept in the already-staged M6.2 `git mv` renames of the react package (27 R100s, content untouched — internal imports dangle at that SHA; `651cf142` completes them one commit later). The pair is coherent in sequence but `9b573a52` is not independently buildable — bisect users should treat 9b573a52..651cf142 as one step. Standing mechanic added: before any targeted commit, verify `git diff --cached --stat` matches the commit's intent — a partially-staged index from an in-progress move WILL leak into pathspec-less commits. Lesson for the remaining shim retirements (event-stream M8, runtime-api M9): classify a shim's tests as barrel-vs-behavioral BEFORE deletion; behavioral suites relocate to the successor's package |
| `boundaries/framework/contracts/event-stream` (`@tuvren/event-stream`, deprecated shim) | authority → `spec/streaming/` (M8.1a); shim retired, successor `@tuvren/core/events` | **migrated (M8.1c)** | `d612b813a2cef253d31980d5b003e367456ce6f2` | named explicitly in §12 |
| `boundaries/framework/contracts/runtime-api` (`@tuvren/runtime-api`, deprecated shim) | authority half → `spec/host/` (M9.1); `implementations/typescript` — measured at M9.2 to be a pure re-export barrel with a proven successor (`@tuvren/core/{execution,events,messages,provider,tools}`), so **retired** per §10.4 rather than moved to `typescript/host/...` as this row's earlier placeholder anticipated | **migrated (M9.1 authority); shim retired (M9.2)** | `3fbb820f296bcb222a9b014d8517901b7bed3079` | named explicitly in §12. M3.5 disposition decision, on M3-measured evidence: the engine has **zero** compile-time `@tuvren/runtime-api` imports since M3.2a (the last two src type-imports were rewritten to `@tuvren/core/execution`), so runtime-api is not an engine seam — it is the host-facing surface contract, exactly §7's `spec/host/`. M3.4 accordingly declared no `spec/host` stub (would front-load M9), and the `runtime-api-*` *plan* family (engine-behavior certification, packet-owned by `tuvren.shared.core`) moved to `spec/conformance/engine/` independently of this authority's disposition. **M9.1:** home 1 of 2 (the neutral authority: `spec/typespec/` → `spec/host/typespec/`, `spec/bindings/` → `spec/host/bindings/`, `artifacts/json-schema/` → `spec/host/artifacts/json-schema/`) lifted byte-pure (all R100; 39 generated schemas verified byte-identical post-regen via `host-spec:codegen`); the stale `spec/README.md` (falsely claiming a standalone `authority-packet.json` declaring `tuvren.framework.runtime-api` existed — never true after the ADR-037 absorption into `tuvren.shared.core`) retired into an accurate `spec/host/README.md`, mirroring the M6.1b/M8.1c stale-packet-claim retirement pattern. `tspconfig.yaml`'s `{project-root}` emitter-output-dir depth recomputed (two levels up under the old contract root → one level up at the new spec-owned home, matching the `runners-spec`/`streaming-spec` shape); codegen re-homed to new spec-owned `host-spec` project (root codegen script list swapped `framework-runtime-api` → `host-spec`; `framework-runtime-api` lost its codegen target, since `implementations/typescript/project.json` no longer owns artifact generation once the artifacts live at `spec/host/`). Home 2 of 2 (the `implementations/typescript` package, its residual consumers, and the contract root's own removal) was pending as of M9.1 and closed at **M9.2** (see that milestone's addendum): the shim was measured to be a pure re-export barrel and retired rather than moved, its 7 type-position `import("@tuvren/runtime-api")` consumer sites (`typescript/runtime/test/durable-reads.test.ts`, `typescript/conformance-adapter/src/framework-adapter-runtime-scenarios.ts`) rewired to `@tuvren/core/execution`/`@tuvren/core/provider`, and `typescript/providers/provider-api`'s stale `package.json` dependency (recorded at M4.2, zero real import sites) pruned; label corrected M10.1 — this cell previously read "stays pending" after M9.2 had already closed it |
| `boundaries/framework/contracts/tool-contracts` (`@tuvren/tool-contracts`, deprecated shim) | authority → `spec/tools/` (**M5.1a** — `typespec/` + `artifacts/` + merged README landed); shim package (`@tuvren/tool-contracts`) → retired M5.1b | **migrated (M5.1a authority) / retired (M5.1b shim)** | `3975de9363276f47ddd33f0d76add7e5e32870b3` | named explicitly in §12. **M5.1a:** `spec/typespec/` → `spec/tools/typespec/` and `artifacts/` → `spec/tools/artifacts/`, both byte-pure moves (pre-move trees, unchanged post-move: typespec `25a8a8ff3c02b3375759f3b5a2e5f9f2b57cc030`, artifacts `acf6f0239c20c18b70ee5ee1348a64a9a00ffae8`); the contract-root README and spec README merged into the new `spec/tools/README.md` port authority README (M1.3/M4.1 merge precedent; the M3.4 seam stub retired on authority arrival). Packet identities deliberately unchanged: these are `tuvren.shared.core` authoritative sources, so only the core packet's `main.tsp` source path was updated (path-only, M1.3 class); a `tuvren.tools.*` re-packeting was considered and rejected (would churn 6 plans' packetIds + the freeze-gate matrix + both portability records for zero DoD gain). New spec-owned regeneration lane `tools-spec` (`spec/tools/project.json`, core-spec/providers-spec pattern): `codegen` + `lint`; the shim's `framework-tool-contracts` project lost its `codegen` target; the root `codegen` run-many list swapped `framework-tool-contracts` → `tools-spec` (verify.ts derives that list from `package.json` since M4.2d — no second copy to update); tspconfig emitter depths corrected `{project-root}/../../artifacts` → `../artifacts` (the M4.2c template-prefix class, caught by the standing per-lift sweep). Standing lesson applied: `tools-spec:codegen` run immediately after the lift — **zero artifact drift** (byte-clean regeneration, committed artifacts remain authority). The shim's test URL base got its load-bearing trailing slash restored (`spec/tools/artifacts/json-schema/`, third occurrence of the class); the shim test's artifacts input glob repointed. Freeze-gate `toolContracts` classification block repointed (compound-string sed class); coverage matrix + AF gap plan regenerated via sanctioned lanes, diffs verified 100% path-only. **M5.1b:** the deprecated `@tuvren/tool-contracts` shim package (`implementations/typescript`, a `console.warn`-carrying re-export of `@tuvren/core/tools`) `git rm`'d with the whole contract root; pre-deletion contract tree `13a89c67b8a89ff64f9fe3c13bae895006d5614d`. Not a bare deletion per invariant §10.4 — successor `@tuvren/core/tools` proven, import-dead re-verified at deletion: zero value- or type-position imports outside the shim's own files, the only `.ts` mentions being the portability registry entry removed here and the historical KRT-AP006 codemod's mapping table (which stays as historical record, minus its now-dead exclusion-path line). Wiring removed in the same commit: `tsconfig.base.json` paths entry; root `tsconfig.json` reference; `verify.ts` curated test/build/export-smoke lane entries (×3 — the codegen lane needed nothing, it derives from `package.json` since M4.2d and was already swapped `framework-tool-contracts` → `tools-spec` at M5.1a); `portability-check.ts` registry entry; workspace member removed from `bun.lock` (`bun install`: "1 package removed"). Validation: `bun run check` green end-to-end (authority gates + discovery parity + affected typecheck/test/lint) |
| `boundaries/framework/implementations/typescript/runtime-core` (`@tuvren/runtime-core`, deprecated shim, `index.ts`-only) | retired (successor `@tuvren/runtime` at `typescript/runtime`) | **retired (M3.2c)** | pre-deletion tree `e473d3bd141670393292cdec5967b10fa43ff922` | named explicitly in §12; retired per M3 DoD — not a bare deletion because `runtime/` is the proven successor already carrying the real generated content (Multi-home §A). **M3.2c evidence:** zero value- or type-position code imports of `@tuvren/runtime-core` existed at deletion (its only import site was its own smoke file, deleted with it). Infrastructure cleaned in the same commit: `telemetry-semconv:codegen` `outputs` repointed from the dead `runtime-core/src/lib/generated/` path to the real artifact `typescript/runtime/src/lib/generated/tuvren-runtime-telemetry.ts` (closing the Multi-home §A telemetry half early — M8 note superseded); `.gitignore`'s runtime-core kernel-interop rule dropped; `docs-authority-freeze-gate.ts`'s §5 framework-integration evidence path repointed from the already-nonexistent `runtime-core/test/runtime-core.test.ts` to the successor's `typescript/runtime/test/runtime-core.foundation.test.ts`; verify.ts `WORKSPACE_BUILD_PROJECTS` + `WORKSPACE_EXPORT_SMOKE_PROJECTS` entries removed; portability-check registry entry removed; root `tsconfig.json` reference + `tsconfig.base.json` paths entry removed; stale `@tuvren/runtime-core` paths/references pruned from drivers/react (dts+typecheck), bridge-ai-sdk (lib+typecheck), and the engine's own dts config; `runtime-api/spec/bindings/typescript.md` binding-projection sentence updated truthfully (no gate pins that doc — verified); stale repl-smoke comment reworded. Workspace member removed from bun.lock (`bun install`: "1 package removed"). **P2 follow-up (review of 4c3f86b9):** drivers/react `tsconfig.lib.json` still referenced `../../runtime-core/tsconfig.lib.json` — a relative path invisible to the full-path grep and to every gate (react's typecheck lane uses `tsconfig.typecheck.json`, its build uses tsup; no gate runs `tsc -b` over the solution chain), but it broke editor tsserver project loading. Reference removed; `tsc -b …/react/tsconfig.lib.json --dry` now resolves (was TS5083 exit 2). No replacement reference needed — react does not import the engine (its typecheck passes with no `@tuvren/runtime` mapping) |

## Framework-wide conformance (cross-port) — split across M2/M5/M6/M8

`boundaries/framework/conformance` (whole tree, 27 git-tracked files: `fixtures/{event-stream-sse-traces,stream-events}.json`; 20 files under `plans/`; `scenarios/{driver-api,event-stream,operational-telemetry,runtime-api}-scenarios.json`; `schemas/fixture-set.schema.json`) — content_hash `d1a5c7141199c0e354d6bbe7bb70e138d966b48d`. This entire tree was omitted from the first draft of this ledger; added after independent review found it unaccounted for. Unlike the kernel/providers conformance trees (each a single directory-level row because they map to one port apiece), framework's conformance spans several future ports and must be broken out by filename prefix so each future milestone knows which plans it owns:

| Plan/fixture/scenario prefix | Destination | Milestone |
|---|---|---|
| `driver-api-*`, `react-driver-*` | `spec/conformance/runners/` | **migrated (M6.1b)** — 4 plans + shared `driver-api-scenarios.json` moved byte-pure (all R100; plans' `../scenarios/` refs unchanged because plans and scenarios moved together); driver-era filenames/IDs deliberately kept until the M6.4 rename commits |
| `event-stream-*` (excl. `-sse`), `event-stream-sse*`, `event-stream-sse-traces.json`, `stream-events.json` | `spec/conformance/streaming/` | **migrated (M8.1a)** — 3 plans + 2 fixtures + 1 scenario landed at `b53cbfc9`; this cell lagged the flip until the M8.4-close review caught it (second instance of the ledger-row-lag class this arc, after `7a08718`) |
| `runtime-api-*` (batteries-included, callables, callables-extended, lifecycle, lifecycle-extended, orchestration, scenarios) | `spec/conformance/engine/` (`plans/` + `scenarios/`, kernel-precedent layout) | **migrated (M3.4)** — 6 of 7 blobs byte-identical to pre-move (verified by hash); `runtime-api-callables-extended.json` differs by exactly 1 line (its cross-boundary `secret-isolation` fixture ref recomputed for the new depth: `../../../providers/…` → `../../../../boundaries/providers/…`; M4.1 recomputes it again when the providers conformance tree lifts). Same-commit follow-ups: `spec/core/authority-packet.json` plan-selection paths; freeze-gate + AF-gap-plan evidence strings, with both generated `.constitution/reports/` artifacts refreshed via their sanctioned lanes (path-only diffs, verified); the two staying M5 plans' scenario refs recomputed (`../../../../spec/conformance/engine/scenarios/…`); all 3 framework runners' conformance `inputs` gained `{workspaceRoot}/spec/conformance/engine/**/*`. Naming note: "engine" per this row's label; M9's runtime-api disposition may reconcile (recorded in `spec/conformance/engine/README.md`) |
| `tool-contracts-extended.json`, `capability-orchestration-integration.json`, `capability-policy.json` | `spec/conformance/tools/` | **migrated (M5.1c)** — capability-policy + capability-orchestration-integration byte-identical to pre-move (blob-hash verified); tool-contracts-extended differs by exactly 1 line (its scenario ref recomputed for the new depth: `../../../../spec/conformance/engine/scenarios/…` → `../../engine/scenarios/…`) |
| `framework-operational-telemetry.json`, `invocation-lifecycle-observation.json`, `operational-telemetry-scenarios.json` | `spec/conformance/telemetry/` | **migrated (M8.2a)** | byte-pure moves; the `secret-isolation` fixture ref in `framework-operational-telemetry.json` recomputed for the new depth (`../../../../spec/conformance/providers/…` → `../../providers/…`); the `operational-telemetry-scenarios.json` sibling-relative ref is unchanged because the plan and its scenario moved together |
| `tuvren-client-execution-class.json`, `tuvren-server-execution-class.json` | `spec/conformance/tools/` (execution class is a tools/MCP-boundary concept per M5) | **migrated (M5.1c)** — tuvren-client byte-identical to pre-move (blob-hash verified); tuvren-server differs by exactly 1 line (same scenario-ref recomputation class as the row above) |
| `schemas/fixture-set.schema.json` | `spec/conformance/schemas/` (shared schema, no single port owner) | **migrated (M8.4)** |

status: fully migrated as of M8.4 — every prefix row above has landed at its owning milestone and `boundaries/framework/conformance/` no longer exists.

**M3.4 addendum — engine-seam spec stubs + a generator-drift discovery.** (a) Per the M3 gate ("only the engine↔port interface seams the engine must compile against are declared in `spec/` (as minimal stubs)"), six stub READMEs were authored at `spec/{runners,providers,streaming,tools,extensions,telemetry}/` — one per port seam the engine measurably compiles against (import census over `typescript/runtime/src`: `core/driver`+`driver-react`, `core/provider`, `core/events`, `core/tools`+`core/capabilities`+`mcp-client`, `core/extensions`, `core/telemetry`+the generated semconv binding). Each stub is an explicit pointer-not-oracle: it names the engine-facing vocabulary (authority `spec/core`), the interim full authority location, and the owning milestone (M4–M8). **`spec/host` is deliberately NOT stubbed:** the engine's source has zero `@tuvren/runtime-api` imports since M3.2a rewrote the last two to `@tuvren/core/execution`, so the host port is not an engine compile-time seam — stubbing it would front-load M9. (b) **Pre-existing drift discovered in `tools/scripts/conformance/generate-framework-plans.ts`:** running it (attempted as a byte-identity proof) deleted ~941 lines of committed plan content — `runtime-api-callables-extended.json` alone loses ~870 lines including the 0.3.0 durable-read check family, `event-stream-extended.json` 74, `runtime-api-lifecycle-extended.json` 29 — and regressed `packetId` fields to pre-M2 per-contract values (committed plans were consolidated onto `tuvren.shared.core`/post-M2 ownership without back-porting the script). Committed JSON is authority: all clobbered files were restored byte-exact from the index (verified by blob hash) and the script now carries a header warning; back-port-or-retire is carried to the plan-family owners (M5/M6/M8 + M3.5 note). The script's output dirs were still updated (engine plans → `spec/conformance/engine/plans/`) so a future back-ported run cannot write to dead paths.

**M6.1a addendum — the framework generator scaffold is RETIRED (exercises the "or retire" arm of the M3.4 carry, for all remaining families at once).** `tools/scripts/conformance/generate-framework-plans.ts` deleted. Measured justification: (a) it is wired into no Nx target, package script, or verify lane (grep at deletion found only prose references); (b) it cannot be run for any single plan family without clobbering the others — one emission table, one run writes all files — so the family-by-family back-port option was illusory; (c) no family back-ported since M3.4, and the drift only widened: a re-run would today regress `driver-api-extended.json`'s packetId to the pre-ADR-037 `tuvren.framework.driver-api` (committed authority: `tuvren.shared.core`) on top of the ~941-line deletion measured at M3.4; (d) M6's driver→runner identity renames would make its 2 runner-family builders (`buildDriverApiExtended`, `buildReactDriverExtended`) doubly wrong. The M3.4 ruling stands: committed plan JSON is authority; regeneration tooling for plans, if ever wanted again, starts from the committed content. The sibling `generate-providers-plans.ts` (its own M4-carry, same class) is NOT decided here — carried to M10.1.

## Engine (Kraken) + libc/SDK — M3

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/framework/implementations/typescript/runtime` (`@tuvren/runtime`) | `typescript/runtime` | migrated (M3.2a) | `436db6090fb42bdade185a2e638f8064a7167fe9` | today's concrete engine package; `createTuvren` already lives here, not in `@tuvren/core`. **M3.2a:** `git mv` to `typescript/runtime` (Nx name `framework-runtime` kept per precedent; npm name unchanged). `test/` byte-identical pre/post (`14f352fa…`); `src/` differs from pre-move `825fc18d…` by exactly two files (`lib/runtime-core-hosts.ts`, `lib/runtime-core-observability.ts`): the move exposed a **phantom dependency** — both used inline `import("@tuvren/runtime-api")` types resolved only through stale undeclared symlinks in the package-local `node_modules` — the dependency was declared historically (added `22b1e7d7`, Epic AI; removed `21dc20e7`, Epic AP), which is exactly where the stale links originate, but it has not been in `package.json` since; rebuilt `node_modules` and repointed both types to their real home `@tuvren/core/execution` (a declared peer). Post-review follow-up: the sweep missed `spec/interop/project.json`'s codegen `inputs`/`outputs` (`{workspaceRoot}/`-prefixed strings evade the quoted-path rewriter, same class as the manually-fixed `nx.json` globs) — fixed, and the cacheable target's restore-at-new-home behavior proven by a cache-replay experiment (generated dir moved aside, cache-hit codegen restored byte-identical content at `typescript/runtime/src/lib/generated/kernel-interop`). Repo-wide sweep: 22 files by the resolution-based rewrite script + a second pass with broadened keys for relative imports lacking the `boundaries/` spelling (10 more files: drivers/react tests, runtime-core tsconfigs) + manual fixes for YAML (`buf.gen.yaml` out path), `nx.json` `{workspaceRoot}` globs, 2 mid-string project.json command paths, and `.gitignore`'s generated-bindings rule (old rule stopped matching post-move and `git add -A` briefly staged the gitignored generated bindings — caught by the tree-hash purity check). M1.5's deferred end-state landed: generated kernel-interop bindings + `tuvren-runtime-telemetry.ts` now live under `typescript/runtime/src/lib/generated/` and both codegen lanes reproduce them at the new home (kernel-interop-grpc:codegen, telemetry-semconv:codegen — zero diff). `BUILD.bazel` M1.2-pattern shim added (`//typescript/runtime:{build,test}`, proven live via `bazel run`) |
| `boundaries/framework/implementations/typescript/testkit` (`@tuvren/framework-testkit`) | `typescript/testkit` | migrated (M3.2b) | `12e7e04b51d0b80ddd8e70fba76d0bb2d8ce887c` | **M3.2b:** `git mv` to `typescript/testkit` (Nx name `framework-testkit` kept; sibling of the engine, mirroring `typescript/kernel/testkit`; no `BUILD.bazel` per the M1.6 scope record — testkits are Nx-only). `test/` byte-identical pre/post (`a888e45d…`); `src/` differs from pre-move `ca8fa979…` by exactly one file, `lib/framework-testkit.ts`: its two runtime fixture-probe strings (resolved at runtime against `import.meta.url`, one src-depth + one dist-depth, both targeting the unmoved `boundaries/framework/conformance/fixtures/stream-events.json`) needed depth recomputation for the new home — the rewrite script correctly recomputed the src-depth probe but treated the dist-depth probe as src-relative (rewriting it to preserve its old *src-resolved* target, a path that never existed); hand-fixed to the correct dist-depth path and proven by uncached `framework-testkit:{build,test}` plus `framework-stream-core:test` (a dist consumer that loads the fixture through the built package). Sweep: 19 files (consumer tsconfigs, root tsconfigs, portability-check packageRoot) + 2 mid-string project.json command paths by hand (the lint command's second path argument `boundaries/framework/conformance` deliberately unchanged — that tree moves at M3.3/M3.4); package-local `node_modules` deleted pre-move and re-linked by `bun install` (M3.2a stale-symlink lesson) |
| `boundaries/framework/implementations/typescript/conformance-adapter` | `typescript/conformance-adapter` | **migrated (M3.3a)** | pre-move tree `3744016b9752caef4b85ec16d31efc2c4b08ed23` (row baseline `8711f534…` is the M0 hash; content-edited since) | **M3.3a home decision:** framework-level packages sit unprefixed at the language root (M3.2 precedent: `typescript/runtime`, `typescript/testkit`); kernel-scoped stay nested (`typescript/kernel/conformance-adapter`). Move proven pure: index tree at the new prefix == pre-move tree before content edits. Content delta = 12 files, all path rewrites (adapter manifests' `command`/dist paths, `$schema` — which also fixes a pre-existing 6-vs-5-up off-by-one, adapter-protocol/plan-compiler imports, kernel-backend + runtime imports, `import.meta` repo-root probes recomputed for the 3-up depth, drivers/react references now explicit `boundaries/` paths until M6 moves react). Nx name + `adapter.json` IDs unchanged (M6 owns renames) |
| `boundaries/framework/implementations/typescript/conformance-runner` (`@tuvren/framework-typescript-conformance-runner`) | `typescript/conformance-runner` | **migrated (M3.3a)** | pre-move tree `39809d845c68e773d87783548ad5fd2e878ce5f7` (row baseline `98577789…` is the M0 hash; wrapper-normalized at M2.9) | project ID in `conformance` script (discovery-verified since M2.9). Content delta = 3 files: project.json (`root`/`$schema`/cwd/command args; conformance `inputs` gained `{workspaceRoot}/typescript/conformance-adapter/**/*` while keeping the `implementations/*/conformance-adapter` glob for the rust adapter until M3.3b), test REPO_ROOT probe depth, tsconfig.typecheck.json (depth + **stale `src/**/*.ts` include pruned — the M2.9-fix P3 carry, src/ was deleted at wrapper normalization**). Package-local node_modules deleted pre-move + `bun install` re-link (M3.2a stale-symlink lesson) |
| `boundaries/framework/implementations/typescript/conformance-runner-batteries-included` (project.json only, no package.json) | `typescript/conformance-runner-batteries-included` | **migrated (M3.3a)** | pre-move tree `2a1e66fced18fc486faae910bcdc5f768ea6f224` (== row baseline, untouched since M0) | project ID `framework-batteries-included-conformance-runner`. Content delta = project.json only (same classes as the runner row). Validation for all three rows: `bun run check` green; both conformance lanes green **uncached** at the new homes (`--skipNxCache`); certification discovery parity green; zero old-path references (full-path grep + the M3.2c relative-reference sweep) |
| `boundaries/framework/implementations/rust/conformance-adapter` | `rust/conformance-adapter` | **migrated (M3.3b)** | pre-move tree `feed694cc49065c957fd6854495880bf2469b45f` (row baseline `d0de85d4…` is the M0 hash; content-edited since) | framework adapter is an explicit not-implemented stub per §6. **Byte-pure move: post-move tree == pre-move tree, zero content edits** (adapter.json invokes by crate name and its `authorityPackets` are root-relative to unmoved targets). Home decision matches M3.3a: framework-level = unprefixed at the language root (`rust/conformance-*`), kernel-scoped stay prefixed (`rust/kernel-conformance-*`). MODULE.bazel.lock regenerated by a real bazel build: the two member-manifest FILE pins repointed with **identical content hashes** |
| `boundaries/framework/implementations/rust/conformance-runner` (`framework-rust-conformance-runner`) | `rust/conformance-runner` | **migrated (M3.3b)** | pre-move tree `96c2acaedd470158ee5076890a951fb7a5ea965b` (== row baseline, untouched since M0) | project ID in `conformance` script (discovery-verified since M2.9); certifies a not-implemented stub today. Content delta = 2 files: project.json (`$schema`/`root` depth; conformance `inputs` swap `implementations/rust/**` for explicit `rust/conformance-{adapter,runner}/**/*` globs matching the kernel runner's style; `--adapter` command arg) and src/main.rs (adapter-manifest path in the stub message). Same-commit follow-ups: root Cargo.toml members, both TS runners' transitional `implementations/*/conformance-adapter` glob collapsed to `{workspaceRoot}/rust/conformance-adapter/**/*` (net input parity with the pre-epic wildcard preserved), nx.json inputs-comment reworded. **Lesson recorded: Nx daemon served a stale project graph after the move (`Cannot find project`) — `bun run nx reset` before trusting post-move Nx invocations.** Validation: cargo check green at new paths; `bazel build //rust/...` green; `bun run check` green post-reset; rust conformance lane green uncached (`--skipNxCache`) |

## Providers — M4

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/providers/contracts/provider-api` (`@tuvren/provider-api`) | authority → `spec/providers/` (**M4.1**); TS binding package → `typescript/providers/provider-api` (M4.2) | **migrated (M4.1 authority / M4.2 implementation)** | authority pre-move trees: spec `e4bc234cb982e65e9dd4a9107718e4091e42d55b`, artifacts `78320c72348e552e662b85bf704b927f4b189de8` (row baseline `1502175d…` is the whole contract dir at M0); TS binding pre-move HEAD tree `98a1a5497314e4eec76844a50ef2dc7981ee7e47` | **M4.1:** `spec/` contents + `artifacts/` lifted to `spec/providers/` (M1.3/M2.2 pattern). Artifacts tree byte-pure (post-move == pre-move hash). Spec-dir delta = exactly 2 files: `authority-packet.json` (internal path refs to its own typespec/plans/fixtures — M1.3 kernel-packet class) and `README.md` (contract-root + spec READMEs + the M3.4 seam stub three-way merged into the port authority README; stub retired on authority arrival, M1.3 merge precedent). Portability inventory `packetPath` updated (sanctioned direct edit, M2.2 precedent, gate green). `implementations/typescript` stays behind for M4.2. **M4.2:** the TS binding package moved to `typescript/providers/provider-api` (Nx project ID `provider-api` and npm name `@tuvren/provider-api` unchanged — M6 owns renames). Its three `tsconfig*.json` variants were rewritten past git's rename-detection threshold, so the move is recorded as delete+add pairs rather than renames (content delta = recomputed relative depths only). The test file's `spec/providers/artifacts/json-schema/` URL base had its load-bearing trailing slash hand-restored after the rewrite script dropped it (same script-mangling class as M4.1), and the stale "Epic X keeps TypeSpec artifacts…" comment was corrected in the same touch (M4.1 review P3 fold-in). An M1.2-pattern `BUILD.bazel` shim was added (`provider-api:build`/`test`) and the package joined the MODULE.bazel shim census. `spec/providers/authority-packet.json`'s `bindingProjections` + certification paths were auto-rewritten to the new homes by the move script. **Shared M4.2 evidence** (all five provider packages moved this milestone): root `workspaces` gained `typescript/providers/*` (kernel precedent); staged census = 52 renames (37 byte-pure R100, 15 path-string-only edits) + 10 `tsconfig` delete+add pairs + 2 new `BUILD.bazel` + 18 modified external-reference files (react + repl tsconfigs, mcp packet, mcp-client tsconfig, root tsconfig pair, tools scripts, `spec/providers` README + packet, MODULE.bazel, `package.json` + `bun.lock`, portability MD record); consumer symlinks re-linked to the new homes and verified (repl→bridge, react→provider-api). Validation: `bun run check` green (all authority gates + affected typecheck/test/lint); providers conformance 60/60 green with `--skipNxCache` at the new homes; all four new `bazel run` shim targets green; a repo-wide tsconfig `extends`/`references` existence probe passes; residual old-path greps exit 1. **M4.2c** (M4.3 full-`verify` surfaced two stale codegen wirings the M4.2 sweep missed): the run failed at the machine-authority-guardrails freshness phase — `provider-api:codegen` still compiled `./spec/typespec` relative to `cwd: boundaries/providers/contracts/provider-api`, the unmoved contract-root husk, so the resolution-based rewrite script correctly left it alone and the `./spec/typespec` argument sat mid-command (the known mid-string class); neither `bun run check` nor any review caught it because the guardrails freshness lane is the **only** gate that executes regenerate commands and it runs only in full `verify`. Second stale piece: `spec/providers/typespec/tspconfig.yaml`'s `emitter-output-dir: "{project-root}/../../artifacts/…"` — a **new evasion sub-class: template-prefixed paths** (`{project-root}/…` joins the known `{workspaceRoot}/…` class; the value is quoted but starts with a template token rather than `./`/`../`/`boundaries/`, so the rewrite regex never matches); its depth had gone stale because `typespec/` and `artifacts/` became siblings under `spec/providers/` at M4.1 (one level further apart pre-move). Fix (core-spec M2.2 precedent — the spec home owns its own regeneration lane): new `spec/providers/project.json` Nx project `providers-spec` with `codegen` (cwd `spec/providers`) + `lint`; tspconfig depths corrected; both packet `regenerateCommand`s repointed `provider-api:codegen` → `providers-spec:codegen`; the `codegen` target removed from the implementation package (typescript/core parity); root `codegen` script run-many list updated. Proof: `providers-spec:codegen` green with **zero artifact drift** (git-clean after regeneration — committed artifacts remain authority). Standing lesson for the M5–M9 lifts: after every authority lift, run the port's spec `codegen` target immediately — do not wait for the close-gate `verify` to exercise the freshness lane. **M4.2d:** the M4.2c review's P2 — `verify.ts`'s inlined copy of the codegen run-many list still named the removed `provider-api` target (Nx run-many exits 0 for missing targets, so the freshness phase silently regenerated nothing for the port) — fixed by deriving the verify list from the root `package.json` codegen script (single source of truth, loud parse failure), killing the two-copy drift class |
| `boundaries/providers/conformance` | `spec/conformance/providers/` | **migrated (M4.1)** | `8b089e33f3404f6ab901b3cf9f7aaadbea20ea61` (== M0 baseline, untouched since) | **Byte-pure move: post-move tree == pre-move == M0 baseline, zero content edits.** Includes `plans/providers-mcp-client.json` — its port-final home is M5's call (`spec/conformance/tools/`?, one more hop when MCP re-categorizes); portability-inventory `sourcePath` updated for the interim home. Same-commit follow-ups: freeze-gate + AF artifacts regenerated via sanctioned lanes; engine plan's `secret-isolation` fixture ref recomputed (predicted at M3.4); `framework-operational-telemetry.json` fixture ref recomputed; providers runner/testkit `{workspaceRoot}` inputs + testkit lint arg repointed; `generate-providers-plans.ts` output dir repointed **and flagged as a stale scaffold (drift measured at M4.1: regenerating regresses 6 assertion `kind` fields of `provider-api-bridge-extended.json` — committed JSON restored byte-exact, warning header added, same class as the M3.4 framework-generator discovery)**. **New evasion class discovered and fixed: segment-array walk-up probes** — `MANIFEST_PATH_SEGMENTS` ancestor-walks in the providers testkit + providers conformance-adapter joined `["conformance","scenarios","suite-manifest.json"]` per ancestor (no greppable path string; found only by the test lane); both re-anchored to repo-root segments `["spec","conformance","providers",…]` matching the kernel testkit's M1.3-era walker. Validation: check green; providers conformance lane green uncached **M5.1c:** `plans/providers-mcp-client.json` made the hop this row predicted — `spec/conformance/providers/plans/` → `spec/conformance/tools/plans/` (delta = exactly 1 line: the `secret-isolation` fixture ref recomputed `../fixtures/…` → `../../providers/fixtures/…`; the fixtures stay providers-owned); portability-inventory `sourcePath` updated to the final home (manifest v0.4.2) |
| `boundaries/providers/implementations/typescript/bridge-ai-sdk` (`@tuvren/provider-bridge-ai-sdk`) | `typescript/providers/bridge-ai-sdk` | **migrated (M4.2)** | pre-move HEAD tree `9f3786d037ef1f00558f74d776ff9cf0aaa13ffc` (row baseline `9d87eb0f…` is the M0 hash; content-edited since) | M1.2-pattern `BUILD.bazel` shim added (`providers-bridge-ai-sdk:build`/`test`); joined the MODULE.bazel census. Two test files' relative refs to the engine's `typescript/runtime/test/fake-kernel.ts` helper recomputed. `project.json` lint/typecheck command-argument paths were the known mid-string evasion class (post-script sed) |
| `boundaries/providers/implementations/typescript/testkit` (`@tuvren/provider-testkit`) | `typescript/providers/testkit` | **migrated (M4.2)** | pre-move HEAD tree `bd1838d887631f65ed412f5da593a9ae65ccd2c8` (row baseline `2b139f8e…` is the M0 hash; content-edited since) | the M4.1 root-anchored `MANIFEST_PATH_SEGMENTS` walk-up probe needed **zero** changes for this move — position-independence the M4.1 re-anchor was designed for, now proven |
| `boundaries/providers/implementations/typescript/conformance-adapter` | `typescript/providers/conformance-adapter` | **migrated (M4.2)** | pre-move HEAD tree `ec8a85f8a71f6dd0397a45d1fc3dbf53062f49d6` (row baseline `96d7bc7a…` is the M0 hash; content-edited since) | `adapter.json` internal host path rewritten; `src/host.ts` refs INTO the stay-behind `mcp-client` lengthened to full `boundaries/` paths (mcp-client moves at M5); the two execution-class files' refs to the react driver recomputed |
| `boundaries/providers/implementations/typescript/conformance-runner` (`@tuvren/providers-typescript-conformance-runner`) | `typescript/providers/conformance-runner` | **migrated (M4.2)** | pre-move HEAD tree `e72daace1be8b6cd0a9cde90030ba8dc071dc2e2` (row baseline `6e44d847…` is the M0 hash; content-edited since) | conformance-target input glob `{workspaceRoot}/boundaries/providers/implementations/*/conformance-adapter/**/*` narrowed to the literal `{workspaceRoot}/typescript/providers/conformance-adapter/**/*` (the language-wildcard parity layout no longer exists); stale `src/**/*.ts` pruned from `tsconfig.typecheck.json` include (executed the carried M2.9-fix review P3); project ID `providers-typescript-conformance-runner` unchanged (still discovery-selected in the `conformance` script); certification discovery parity gate green |
| `boundaries/providers/contracts/mcp` | `spec/tools/` (**cross-port move**, M5 — not M4) | **migrated (M5.1c)** | `a37841418925943402a15e38bf72df781003bf73` | leaves providers for tools per M5 DoD: execution class is decided by who invokes the server, not by the protocol. **M5.1c:** the packet moved to `spec/tools/mcp/authority-packet.json` — sub-surface nesting deliberate (the packet covers only the mcp-client translation contract, so port-root placement would over-claim); packetId `tuvren.providers.mcp` unchanged per the M5.1 design lock. Pre-move packet blob `cd9e36e312a9d9d9f28f7a99ead1ced70cae1ede`; delta = exactly 3 internal conformance-plan path refs (root-relative `spec/…` strings — the rewrite script's STRING_RE gained `spec/`-prefixed matching with the same resolver guard; zero false rewrites on dry run). Same-commit follow-ups: providers adapter manifest packet ref; portability pair updated (JSON `packetPath` + both MD citations, manifest v0.4.2 — the M4.2b MD-anchor gate exercised by a real packet move for the first time); freeze-gate compound-string sed (4 movers repointed; `invocation-lifecycle-observation` stays for M8); `generate-framework-plans.ts` gained `TOOLS_PLANS_DIR` for its tool-contracts-extended entry; three runner conformance `inputs` gained `{workspaceRoot}/spec/conformance/tools/**/*`; the core packet's dead `forbiddenAuthoritySources` tool-contracts entry pruned (M5.1b review "Investigate" resolved: the list tracks live paths — the runtime-api/event-stream/driver-api siblings follow at their retirement milestones); coverage matrix + AF artifacts regenerated via sanctioned lanes (diffs verified 100% path-only); empty contract husk dirs removed |
| `boundaries/providers/implementations/typescript/mcp-client` (`@tuvren/mcp-client`) | `typescript/tools/mcp-client` (**cross-port move**, M5.2) | **migrated (M5.2)** | pre-move HEAD tree `8271a425163627dbf324faebad06266058e5c950` (row baseline `d68469e5…` is the M0 hash; content-edited since) | the MCP bus-driver, first implementation package under `typescript/tools/`. 14 tracked files moved; package-internal deltas = recomputed relative depths only (tsconfig `extends`/`paths`/`references`, project.json `$schema`/root/sourceRoot). The mid-string command-arg class hit again: the package's own `project.json` lint/typecheck `command` args carried the old path mid-command (invisible to the rewrite regex) — fixed by exact sed, caught by `check`'s affected lane (Biome os-error-2 + TS5058). **New minor class: formatter array-collapse** — shortened relative paths made multi-line `paths` arrays fit one line in `tsconfig.dts/typecheck.json` (own) + `typescript/runtime/tsconfig.typecheck.json`; Biome lint fails until `biome check --write` collapses them (run it over rewritten JSON after every move). External refs auto-recomputed by the rewrite script: providers conformance-adapter `host.ts` ×3 (the M4.2-lengthened `boundaries/` imports shortened to `../../../tools/mcp-client/…`), `tsconfig.base.json` alias, runtime tsconfig pair, `spec/tools/mcp/authority-packet.json` bindingProjections + forbiddenAuthoritySources. Root `workspaces` gained `typescript/tools/*`; `bun.lock` workspace key updated (`bun install`: no package changes); runtime's `node_modules/@tuvren/mcp-client` symlink re-linked to the new home and verified. New M1.2-pattern `BUILD.bazel` shim (`//typescript/tools/mcp-client:{build,test}` → `NX_TARGET providers-mcp-client:*`) + MODULE.bazel census comment extended. Nx project name `providers-mcp-client` and `boundary:providers` tag deliberately unchanged (M4.2 precedent — M6 owns renames; nothing gates on Nx tags; tag taxonomy is M10.1's sweep). **`boundaries/providers/` is now fully evacuated and its empty dir tree removed** (zero tracked files and zero symlinks verified pre-removal) — the second whole boundary gone. M5.1c review fold-in landed: the frozen generator scaffold's tools-family block made internally consistent (stale comment + long-form emitted scenario ref). Validation: `bun run check` green end-to-end; `providers-mcp-client` build/test/exports-smoke green at the new home; both new bazel shim targets green; providers conformance 60/60 green uncached (the one affected lane consumer of the moved sources via host.ts) **M5.2b addendum (review-carried lesson):** the formatter class also manifests as import-order invalidation — the shortened `host.ts` import paths re-sorted under organizeImports — and `check`'s affected lint lane is blind to root-level configs (`tsconfig.base.json` belongs to no Nx project's lint target), so only verify's full-repo `biome check .` catches this class; run full-repo biome after every move, not just the affected lane (2-file format-only fixup `4ce3180c`, reviewed CLEAR) |

## Tools + capabilities + MCP — M5

Authority sources are the `spec/tools/` destination rows already listed above (`core/src/tools`, `core/src/capabilities`, `framework/contracts/tool-contracts`, `providers/contracts/mcp`, `providers/implementations/typescript/mcp-client`). No standalone tool-driver packages exist today beyond MCP — `drivers/` (see M6 row below) contains only the ReAct runner, so Exa/Slack-style tool drivers (issue §4) are illustrative future adapters, not part of this move.

## Runners (driver→runner rename) — M6

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/framework/contracts/react-driver` | `spec/runners/` | **migrated (M6.1b)** | `7817365fa5fb2819f8da2179339d5be728bad8f9` | consolidated with `driver-api` into the runner port: packet → `spec/runners/react/authority-packet.json` (sub-surface nesting per the M5 mcp precedent; packetId `tuvren.framework.react-driver` unchanged until M6.4; pre-move blob `2d562304`, delta = exactly 6 internal plan-path refs). Same-commit follow-ups: 3 adapter manifests' authorityPackets (rust + ts + batteries-included — a manifest class first seen here), spec/core packet plan paths, freeze-gate compound seds, portability pair v0.4.3, 3 runner conformance inputs gained `spec/conformance/runners/**/*`, AF gap-plan + coverage matrix regenerated via sanctioned lanes (diffs verified 100% path-only). Contract root fully evacuated and removed |
| `boundaries/framework/implementations/typescript/drivers` (contains only `react/`, package `@tuvren/driver-react`) | `typescript/runners/react` (idiomatic subpath decided at M6) | **migrated (M6.2)** | `511dc9d87b3ba3a96b042aa9f79cb8a44477f0e7` (pre-move tree) | today's only execution-model adapter, migrated AS a runner (the DoD's phrasing) — the move and the package's OWN rename land together per the M6.0 sanctioned exception: npm `@tuvren/driver-react`→`@tuvren/runner-react`, Nx `framework-driver-react`→`runner-react`, 14 `react-driver-*` files→`react-runner-*`, exported API symbols `ReActDriver*`→`ReActRunner*`/`createReActRunner`/`REACT_RUNNER_ID` (const value stays `"react"` — identity-neutral, no evidence impact). Resolver script rewrote 27 files incl. 5 previously-unsurveyed relative-path importers in both conformance adapters; consumers updated (runtime, repl, bridge-ai-sdk, both adapters, portability-check, verify.ts lists); workspaces glob `drivers/*`→`typescript/runners/*`; new M1.2 BUILD.bazel shim + MODULE.bazel census; project.json mid-string command args sed'd (standing class); react packet bindingProjections value updated (key `typescript-react-driver` + all conformance IDs stay driver-era until M6.4). Package tests 82/82 green post-rename; framework conformance lane certifies; `bun run check` green. Engine/core `Driver*` vocabulary (RuntimeDriver etc.) deliberately untouched — M6.4 |
| `tools/conformance/runner` (the shared conformance engine) | `tools/conformance/harness/` (the certification harness) | **migrated (M6.3)** | `5ba709cfebcc386d4b9e584cbb5341611ac4db56` | disambiguates from the M6 "runner" = execution-model meaning; per §5, other `tools/*` unchanged, this stays under `tools/`, just renamed |
| 8 `conformance-runner`-family directories (kernel×2, framework×3, providers×1 already tabled above; batteries-included and sqlite/postgres variants) | renamed off "runner" terminology: `certification` dirs + `*-certification` Nx project IDs (see M6.3 addendum for the full map) | **migrated (M6.3)** | — (see individual rows above) | see Multi-home §C: the 8 script IDs in `package.json`'s `conformance` list and the 8 runner-named directories are independent sets that happen to both total 8 today |

## Extensions — M7 (extraction only, no new behavioral coverage)

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/shared/contracts/core/implementations/typescript/src/extensions` | code: `typescript/core/src/extensions` (landed M2.3, see Core/ABI row); port authority: `spec/extensions/README.md` (M7) | **migrated (M7)** | (see Core/ABI row above) | type-surface. This row's original `spec/extensions/` destination was written pre-M2.1 and is superseded by the M2.1 layout ruling (§5: no language-bearing code in `spec/`) — the code moved to `typescript/core` at M2.3 and STAYS there; M7 authors only the port README at `spec/extensions/` |
| `boundaries/framework/implementations/typescript/runtime/src/lib/extension-runtime.ts` | `typescript/runtime/src/lib/extension-runtime.ts` (landed M3.2 with the engine; stays — port-authored from this + the two rows above) | **migrated (M7)** | `7a80bc74ff0f0a8304efb8c9bc4b1988160aa90a` (captured at M7 per the deferred-hash note) | runtime facade — the hook execution engine (dispatch order, timeouts, resolution precedence, extension state, shared exports, system prompts) |
| `boundaries/hosts/implementations/typescript/repl/src/lib/proof-extension.ts` | `typescript/host/repl/src/lib/proof-extension.ts` (moved with the REPL, **M9.3**) | **migrated (M9.3)** | `e5bdc1cb61e812e29428caa7db7c97ab7bcfd2c2` (blob unchanged, captured at M7 per the deferred-hash note; verified byte-identical post-move) | REPL proof shim — minimal working TuvrenExtension proving the seam in the reference shell |

## Streaming + Telemetry — M8

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/framework/contracts/event-stream-sse` | `spec/streaming/sse/` (packet + typespec + bindings + artifacts) | **migrated (M8.1a)** | `0fd1f0db303e111f57787800f7f8944c6c71baba` | contract root fully evacuated and removed; codegen re-homed to `streaming-spec` |
| `boundaries/framework/implementations/typescript/stream-core` (`@tuvren/stream-core`) | `typescript/streaming/core` | **migrated (M8.1b)** | `77db7471794a1eb7847df2f7a2bb1622ba01e504` | |
| `boundaries/framework/implementations/typescript/stream-sse` (`@tuvren/stream-sse`) | `typescript/streaming/sse` | **migrated (M8.1b)** | `fd3ff32ec7d815cef546f44c1c856a6985417c89` | |
| `boundaries/framework/implementations/typescript/stream-agui` (`@tuvren/stream-agui`) | `typescript/streaming/agui` | **migrated (M8.1b)** | `4c0f26350999ec6b3118c9e3517cefc9d6b0795b` | |
| `boundaries/telemetry/semconv/spec` | `spec/telemetry/` | **migrated (M8.2a)** | `377baba85c33e716f97e672aad8b571de3125e3b` | vocabulary authority — home 1 of 3, see Multi-home §A. `authority-packet.json` content-edited: every internal `telemetry/…` path (`humanAuthorityRefs`, `authoritativeSources`, `generatedArtifacts`, `forbiddenAuthoritySources`, `verificationPaths`, `freshnessChecks`) recomputed to `spec/telemetry/…`, and the four `freshnessChecks.regenerateCommand` strings repointed `telemetry-semconv:codegen` → `telemetry-spec:codegen` |
| `telemetry/` (top-level: `project.json`, `otel-attributes.json`, `semantic-conventions.md`, `semconv/`) | `spec/telemetry/` (authority parts) + `typescript/telemetry/semconv` (generated sink) | **migrated (M8.2a)**, home 3 (`@tuvren/telemetry-otel`) closed at **migrated (M8.2b)** | `30476ea556fcca921fe1d8961a74f6ea82c6cb9e` | home 2 of 3; its Nx `outputs` target is the stale one, see Multi-home §A. `project.json` renamed `telemetry-semconv` → `telemetry-spec` (naming locked to the core-spec/providers-spec/tools-spec/runners-spec/streaming-spec precedent), `root`/outputs recomputed; `otel-attributes.json` → `spec/telemetry/artifacts/`, `semantic-conventions.md` → `spec/telemetry/artifacts/`, `semconv/` → `spec/telemetry/semconv/` (subdir kept); `tools/scripts/telemetry-codegen.ts`'s hardcoded `SEMCONV_REGISTRY_PATH`/`REGISTRY_MANIFEST_PATH`/`MARKDOWN_OUTPUT_PATH`/`JSON_OUTPUT_PATH` constants and its two embedded "Generated from …" doc-comment templates repointed to `spec/telemetry/…`; regeneration at the new home reproduces the checked-in artifacts byte-identically (`telemetry-spec:codegen`, zero git diff). **M8.2b:** home 3 closed — `@tuvren/telemetry-otel` moved `boundaries/framework/implementations/typescript/telemetry-otel` → `typescript/telemetry/otel` (Nx project name `framework-telemetry-otel` unchanged); the generated TS sink itself (home tracked separately, see the dedicated row below) left the engine for a new leaf package `@tuvren/telemetry-semconv` at `typescript/telemetry/semconv`, reused Nx name `telemetry-semconv` (free since M8.2a's `telemetry-spec` rename) |
| `boundaries/framework/implementations/typescript/telemetry-otel` (`@tuvren/telemetry-otel`) | `typescript/telemetry/otel` | **migrated (M8.2b)** | `80833c8c02a2e2e5724223cb331429fbed1e6f2d` | home 3 of 3 — the OTel syslog driver; moved at `e02c4ef8` (npm name and Nx `framework-telemetry-otel` unchanged; first-ever BUILD.bazel shim added); row flip landed one commit late — flagged P2 by the M8.2b review |
| `boundaries/framework/interop/rust-kernel` | `spec/conformance/interop/rust-kernel/` | **migrated (M8.3)** | `340850d236746ccff29cd535d12851f3fb99aa14` | this is conformance, not transport — distinct from `spec/interop/` (kernel §M1). Landing shape nests under `interop/rust-kernel/` (not a flat `interop/`) to preserve room for future interop suites, mirroring how `spec/conformance/{engine,providers,tools,runners,streaming,telemetry}/` are organized — the sole occupant today is rust-kernel, so the sibling-room argument is a forward-looking structural call, not a present multi-suite need. |

## Host — M9

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `boundaries/hosts` (plural) | `spec/host` (singular port) | **migrated (naming collision resolved: `spec/host` authored M9.1; `boundaries/hosts`'s sole implementation lifted out at M9.3, evacuating the plural boundary tree entirely)** | *(directory contained only the repl row below — no separate top-level hash needed)* | naming collision flagged in §12: plural boundary vs. singular port; both halves now arrived at their idiomatic homes |
| `boundaries/hosts/implementations/typescript/repl` (`@tuvren/repl-host`) | `typescript/host/repl` (idiomatic subpath decided at M9; Nx project name `host-repl` and npm name `@tuvren/repl-host` KEPT — `host-repl` already matches the category-component convention and is referenced pervasively) | **migrated (M9.3)** | pre-move tree `14a35b5fe2edb4f012f0fe84e0ca7741996472f0` | reference shell; only first-party host migrated per §9 non-goals. **M4.2d:** the M4.3 close-gate `verify`'s proving-host smoke (a verify-only lane, like the M4.2c freshness lane) failed with "unable to locate mock MCP stdio server" — `repl-shell.ts`'s `MOCK_MCP_STDIO_SERVER_PATH_SEGMENTS` was a third instance of the M4.1-discovery segment-array walk-up class, joining the providers testkit's pre-move `boundaries/…` home per ancestor; re-anchored to repo-root segments `["typescript","providers","testkit","src","bin","mock-mcp-stdio.ts"]`, which stays valid when repl itself moves at M9. `host-repl:interop-smoke` green `--skipNxCache` after the fix. A repo-wide sweep of the class (`_SEGMENTS = [` arrays + inline `"boundaries",` literals) now shows all four walker sites root-anchored and only one non-path-dependent match remaining (the kernel-testkit test's rg search-scope `"boundaries"` arg, deferred to M10 deletion). **M9.3:** the walker claim was verified by reading `findMockMcpStdioServerPath()` directly (not assumed) — it walks up from both `process.cwd()` and its own `import.meta.url` dirname joining the root-anchored segment array at each ancestor, so it is depth-independent by construction; confirmed move-proof, no code change needed. See M9.3 addendum below for the full move accounting |

## Not moved by this restructure (still enumerated for 100% coverage)

| old_path | new_path | status | content_hash | notes |
|---|---|---|---|---|
| `reports/compatibility` | `reports/compatibility` (unchanged) | not-applicable | `df11c28d0a334231bbfdababc3e123149e212c80` | generated evidence tree; explicitly stays top-level per §15, not `spec/` (authority-only). The harness (renamed `tools/conformance/runner`) continues writing into it through M8/M10 |
| `docs/KrakenKernelSpecification.md` | unchanged | not-applicable | `53543f0664d21c9e54188ccc48152596d9351d9e` | timeless semantic authority, survives per §1/§5 |
| `docs/KrakenFrameworkSpecification.md` | unchanged | not-applicable | `d449d2341c14f92e8f2dea9c13844980dc1aefd6` | |
| `docs/KrakenKernelDesignRationale.md` | unchanged | not-applicable | `ea1e8bc12e91a77b53df5adeb6d5aff6821edf22` | |
| `docs/KrakenFrameworkDesignRationale.md` | unchanged | not-applicable | `4b443633146068066de2260504e01d0468c4edfc` | |
| `docs/perf-benchmarks.md` | unchanged | not-applicable | `9ada000a202aac99f34a57440db4d9e2ad67f8b5` | |
| `tests/` | unchanged | not-applicable | `f9c80aba7548da2a5cc9044837fcc0b75fb4b4ab` | empty scaffold (`.gitkeep` only); not an authority home per `CLAUDE.md` |
| `boundaries/` (root, post-teardown) | deleted | pending | — | deletion is the *sum* of every `migrated` row above reaching that state, per invariant §10.4 — never deleted as a bulk action with unmigrated children remaining |
| `MIGRATION_INVENTORY.md` (this file) | deleted at cutover | pending | — | per §12, deleted once M10 completes |

## Multi-home / ambiguous cases (§12 call-outs, verified against current disk state)

### §A — Telemetry's three homes, including a *confirmed* generated-bindings drift

Verified directly (not assumed from the issue text):

- `telemetry/project.json` line 19's Nx `outputs` target names `boundaries/framework/implementations/typescript/runtime-core/src/lib/generated/tuvren-runtime-telemetry.ts`.
- That path **does not exist** — `runtime-core/src/lib/generated/` contains only the gitignored `kernel-interop/` protobuf output (confirmed via `git check-ignore`), not the telemetry file.
- The telemetry file is actually git-tracked (confirmed via `git ls-files`) at `boundaries/framework/implementations/typescript/runtime/src/lib/generated/tuvren-runtime-telemetry.ts` (hash `0a03de0a1108512c990ec44a15771d5f8fe8df86`).
- Kernel-interop generated bindings are confirmed gitignored in **both** `runtime/` and `runtime-core/` copies (`.gitignore` lines 14–15); only the generator config `runtime/tsconfig.kernel-interop.generated.json` is checked in (`runtime-core/tsconfig.kernel-interop.generated.json` does not exist at all, only a build-artifact `.tsbuildinfo`).
- **M1 action** (interop) and **M8 action** (telemetry) must each reconcile config-vs-disk-reality before moving anything, per the issue's own M1 DoD text. This inventory baselines the verified on-disk truth so neither milestone moves the wrong (stale-config) target.
- **M1.5 resolved the interop half.** `spec/interop/authority-packet.json`'s `generatedArtifacts`/`bindingProjections`/`forbiddenAuthoritySources`/`freshnessChecks` and `spec/interop/project.json`'s `codegen` inputs/outputs, plus `nx.json`'s `interop-smoke` target defaults, all named the dead `runtime-core` shim instead of the real `runtime` package that `buf.gen.yaml` and `tools/scripts/kernel-interop-governance.ts` already correctly targeted — confirmed and fixed as part of the M1.5 move (see the Kernel table row above). The telemetry half (`telemetry/project.json`'s `outputs` still naming `runtime-core/src/lib/generated/tuvren-runtime-telemetry.ts`, which does not exist) remains untouched, deliberately, for M8.

### §B — Postgres hermeticity (kernel backend + conformance runner)

`boundaries/kernel/implementations/typescript/backend-postgres` and `.../conformance-runner-postgres` are devenv-managed and non-idempotent (per `AGENTS.md`/`CLAUDE.md`: `devenv up -d` a second time fails). **Decided (M1.6):** the postgres backend and its conformance runner (now at `typescript/kernel/backends/postgres` and `typescript/kernel/conformance-runner-postgres` since M1.4b) stay Nx-driven, outside the hermetic Bazel graph, per invariant §10.5's explicit sanctioned exception ("may run Nx-driven transitionally... until M1's hermeticity decision lands"). This decision *is* that landing: no `BUILD.bazel` target exists or is planned for either package for the remainder of this epic; postgres conformance keeps running exactly as it does today (`bun run nx run kernel-typescript-postgres-conformance-runner:conformance`, requiring `bun run services:up` first). Nothing else migrated so far needed this exception — `typescript/kernel/protocol`, `typescript/kernel/runtime`, and `rust/kernel` are the only packages M1's own DoD requires to certify under Bazel (`typescript/kernel/protocol/BUILD.bazel`, `typescript/kernel/runtime/BUILD.bazel`, `rust/kernel/BUILD.bazel`); the memory/sqlite backends and every conformance-adapter/runner package (postgres included) remain Nx-only for now, which is consistent with M1 being a tracer-bullet slice, not a full-coverage Bazel migration. Recorded here and in `MODULE.bazel`'s module docstring rather than silently excluded; folded into the M1.7 closing `gh issue comment 87` as part of M1's overall close-out.

### §C — Two independent "eights"

The `conformance` script in `package.json` lists exactly 8 project IDs; there are exactly 8 `conformance-runner`-family directories tabled above (kernel-TS ×3 incl. sqlite/postgres, kernel-rust ×1, framework-TS ×2 incl. batteries-included, framework-rust ×1, providers-TS ×1). These are **independent sets that currently happen to both total 8** — per issue §14, they need not stay equal, and the M6 "runner"→"certification harness" rename must not assume a 1:1 mapping between them.

### §D — `tools`/`capabilities` split (core → spec vs. libc) — **RESOLVED at M2.1**

The classification axis (from issue §3's own line: "core — the ABI... Contract-only, no behavior. Executable helpers currently bundled — schema-authoring, payload codecs — are not ABI"): **contract-derived validation** (type guards, predicates, error classes — the TS expression of the contract's validation semantics, which every language reimplements from `spec/core`) **is ABI and stays in `@tuvren/core`**; **behavior that does work beyond validating shapes** (schema conversion/authoring over external libraries, cryptography) **is libc and moves to `@tuvren/sdk`**. File-by-file, verified by reading export signatures and bodies (not filename-guessed):

| `src/lib` file | Classification | Destination |
|---|---|---|
| `capability-shapes.ts`, `driver-contract-shapes.ts`, `runtime-contract-shapes.ts`, `driver-contracts.ts` (barrel) | pure types, zero functions | stays (`@tuvren/core`) |
| `capability-error-codes.ts` | const error-code strings (data) | stays |
| `kernel-records.ts`, `scope.ts`, `tuvren-error.ts` | types/constants + is/assert guards + `TuvrenError` class family (the "errors" vocabulary is explicitly ABI per issue §3) | stays |
| `driver-contract-guards.ts`, `runtime-contract-guards.ts`, `runtime-contract-predicates.ts`, `runtime-content-approval-predicates.ts`, `runtime-context-manifest-predicates.ts` | is/assert contract guards and predicates only — no I/O, no library coupling, no policy | stays |
| `schema-authoring.ts` (388 lines) | executable: `defineTool`, `asSchema`, `jsonSchema`, `zodSchema`, `standardSchema` factories + `schemaSymbol`, plus the `Schema`/`FlexibleSchema`/`LazySchema`/`StandardSchema`/`ZodSchema` authoring types that carry the `zod`/`@standard-schema/spec` coupling | **moves wholesale** to `@tuvren/sdk` (M2.4). Nothing else in `core/src/lib` imports from it (verified), so the move is clean; core's only peer deps (`zod`, `@standard-schema/spec`) leave with it |
| `payload-codec.ts` (447 lines) | **intra-file split**, per the file's own docstring ("The contract here is the authority; `createAesGcmPayloadCodec` is one batteries-included implementation of it"): contract portion (`PayloadCodec`, `PayloadCodecContext`, `PayloadDecryptResult`, `ErasedPayload`, `isErasedPayload`) stays — `runtime-contract-shapes.ts` imports `ErasedPayload`, confirming it is vocabulary; implementation portion (`createAesGcmPayloadCodec`, `createIdentityPayloadCodec`, `IDENTITY_PAYLOAD_CODEC`, `isPayloadEnvelope` + envelope parsing/magic-byte internals, `AesGcmPayloadCodecOptions`, `PayloadKeyring`) → `@tuvren/sdk` (M2.4) | split |

Consumer blast radius of the extraction (measured; corrected after review — the first draft counted raw symbol-name grep hits, which included comments and unrelated local variables in repl and the providers adapter): direct import/re-export sites of the moving `defineTool`-family symbols via `@tuvren/core/tools` are **3 files** — `framework-adapter-schema-authoring.ts` (framework conformance-adapter), `mcp-client/src/lib/mcp-tool-source.ts`, and `@tuvren/runtime`'s root `src/index.ts` re-export block (through which downstream hosts consume them indirectly; runtime's tests import only `TuvrenToolDefinition`, which stays). Payload-codec implementation symbols are consumed by **10 files** (runtime src ×3, runtime tests ×6, `typescript/kernel/conformance-adapter/src/host.ts`). The kernel adapter and `mcp-client` consumers are why the SDK tier must be a standalone package below `@tuvren/runtime` (see the M2.1 preamble in the Core/ABI section).

## Naming-map addenda

None at M0 baseline. Every path discovered during the inventory pass matched an existing pattern in issue §15's naming map.

**M1.4b addendum** — concrete idiomatic subpaths decided under `typescript/kernel/` (§15 left these as "idiomatic subpath decided at M1"):
- `typescript/kernel/protocol` (was `contracts/protocol/implementations/typescript`)
- `typescript/kernel/runtime` (was `implementations/typescript/runtime-kernel`; package name `@tuvren/kernel-runtime` unchanged)
- `typescript/kernel/backends/{memory,sqlite,postgres}` (was `implementations/typescript/backend-{memory,sqlite,postgres}`; grouped under a new `backends/` folder per issue §4's own "storage drivers (backends: memory / sqlite / postgres)" wording)
- `typescript/kernel/testkit`, `typescript/kernel/conformance-adapter`, `typescript/kernel/conformance-runner{,-sqlite,-postgres}` (flat siblings, names unchanged)

**M2.1 addendum** — concrete homes decided for the M2 (core/ABI) milestone (§15 patterns: "boundary-owned `contracts/` → `spec/<port>/`", "`boundaries/<area>/implementations/<lang>/…` → `<lang>/…`"):
- `spec/core/` (was `boundaries/shared/contracts/core/spec/`) — the neutral authority: TypeSpec `main.tsp`, `authority-packet.json`, `bindings/typescript.md`
- `typescript/core` (was `boundaries/shared/contracts/core/implementations/typescript`; npm name `@tuvren/core` and all 11 subpath exports unchanged)
- `typescript/core-types` (was `boundaries/shared/contracts/core-types/implementations/typescript` — the shim package only; the port's residual `spec/`+`artifacts/` authority is a superseded duplicate of core's consolidated authority and is retired at M2.2 with equivalence evidence, see its ledger row; corrects M0's `spec/core/` guess per §5's no-language-code-in-spec rule)
- `typescript/sdk` — **new package `@tuvren/sdk`**, the TypeScript libc/SDK tier born at M2.4 to receive the extracted executable helpers (`schema-authoring.ts` + payload-codec implementations). Not in §15's starter map (the map has no libc row); added here as the naming-map addendum the M0 DoD anticipated. Depends only on `@tuvren/core`; M3's libc/SDK milestone may grow or fold this surface, but its tier position (above core, below/independent of the engine) is fixed by its consumers (kernel conformance-adapter, `mcp-client`)

**M2.2 addendum** — `core-spec` is the Nx project name for `spec/core/` (owns the `codegen` target that compiles `spec/core/typespec` → `spec/core/artifacts/json-schema`, plus a `lint` target), following M1.5's `spec/interop` (`kernel-interop-grpc`) precedent of authority-governing tooling living with the authority it governs. Future `spec/<port>/` projects should follow the `<port>-spec` naming

**M2.4 addendum** — `@tuvren/sdk` at `typescript/sdk` is the TypeScript libc/SDK-tier package decided at M2.1; Nx project name `sdk` (tags `boundary:shared`, `layer:implementation`; standard build/test/typecheck/lint targets mirroring `@tuvren/mcp-client`'s house pattern, including the src-based `tsconfig.typecheck.json` that avoids the dist-race flake the M2.3 review diagnosed in `shared-core-types:typecheck`). Contents: `schema-authoring.ts` moved wholesale by `git mv` (its contract imports rewritten to `@tuvren/core/{tools,errors}`); the payload-codec **implementation half** split out of core's `payload-codec.ts` per the M2.1 intra-file boundary — `PayloadKeyring`, the identity codec (`IDENTITY_PAYLOAD_CODEC`, `createIdentityPayloadCodec`), the envelope wire format (`isPayloadEnvelope` + private serialize/parse/AAD/key helpers), and `createAesGcmPayloadCodec`/`AesGcmPayloadCodecOptions` — while the contract half (`PayloadCodec`, `PayloadCodecContext`, `PayloadDecryptResult`, `ErasedPayload`, `isErasedPayload`) stays in `@tuvren/core/lifecycle`. `zod` + `@standard-schema/spec` optional peer deps moved from core to sdk, leaving `@tuvren/core` dependency-free (behavior-free ABI per the M2 DoD). Tests followed their code (`schema-authoring.test.ts` + the impl describes of `payload-codec.test.ts` moved by `git mv`; core keeps a contract-side `isErasedPayload` test so its test target stays meaningful). Consumer rewrites (all value-level import sites of moved symbols): `runtime` (root index re-exports the moved symbols from `@tuvren/sdk` so `@tuvren/runtime`'s public surface is unchanged; `runtime-core.ts`, `payload-codec-seam.ts`, 6 test files), kernel conformance-adapter `host.ts`, framework conformance-adapter `framework-adapter-schema-authoring.ts`, mcp-client `mcp-tool-source.ts`. Wiring: `tsconfig.base.json` paths + root `tsconfig.json` reference; consumer tsconfig lib/dts/typecheck configs mirror their existing `@tuvren/core` entries for `@tuvren/sdk` (the two conformance-runner typecheck configs inherit paths from the root base config and needed nothing); `runtime` + `mcp-client` declare `@tuvren/sdk` as a non-optional peer dep (their house pattern for `@tuvren/core`)

**M2.5 addendum (milestone close)** — Bazel shim targets added for the three M2 packages (`//typescript/core`, `//typescript/core-types`, `//typescript/sdk`, each `:build` + `:test`), M1.2-style `native_binary`→`nx-run.sh` shims; M3.0 converts these to real `aspect_rules_js`/`rules_ts` targets per the recorded plan amendment. Conformance-list determination: M2 created no conformance-runner project and renamed none, so the root `conformance` lane's 8 project IDs are unchanged — TypeScript certifies against `spec/core` through the framework adapter manifests/plans repointed at `spec/core/authority-packet.json` in M2.2 (enforced by the authority-packet, plan-compiler, and freeze gates, all green). The `shared-core-types:typecheck` flake (diagnosed at the M2.3 review: its typecheck read `../core/dist/*.d.ts` while `shared-core:build`'s tsup `clean` raced it under uncached parallel runs) is fixed by giving it the house-pattern src-based `tsconfig.typecheck.json`; this also starts typechecking its `test/` files for the first time, which exposed two pre-existing TS7015s (deliberate symbol-keyed writes onto arrays in guard-rejection tests) resolved with behavior-preserving casts. Fix proven by four consecutive `--skipNxCache` `build,typecheck` runs of both projects

**M2.9 addendum (discovery-based certification, pulled forward per the recorded plan amendment)** — The certification fleet is now discovery-driven instead of hand-listed. Discovery uses the repo's existing tag vocabulary: the 8 certification runners already carried `layer:conformance-runner` and the two non-certification `conformance` targets (`kernel-testkit`, `providers-testkit` package-local suites) already carried `layer:testkit`, so no project.json changed. New pieces: `tools/conformance/certification/certified-projects.json` (the registered fleet) and `tools/conformance/certification/validate-certification-discovery.ts` (the parity gate: filesystem-walk discovery of tagged projects, hard-fail on manifest↔discovery divergence in either direction, on a tagged project missing its `conformance` target, on any target invoking the shared engine `tools/conformance/runner/run.ts` without the tag, and on any `conformance` target that is neither certification-tagged nor explicitly `layer:testkit`). `bun run conformance` is now `tools/scripts/conformance.ts`: gate first, then `nx run-many` over exactly the validated fleet — the hardcoded 8-ID list left `package.json`. The gate joined verify's shared `AUTHORITY_GATE_STEPS` (id `certification discovery parity`) and check's inner-loop subset (44ms), and the root `codegen` validator chain. All three failure directions proven by mutation (manifest entry removed; runner untagged; the untagged case also trips the unclassified-target check). From M3 on, a project moved without certification wiring fails the gate instead of silently dropping — the standing-discipline item "update package.json conformance list at milestone close" is retired in favor of deliberate manifest maintenance; M6's rename of the `layer:conformance-runner` tag value and runner IDs updates the tag constant + manifest together, which this gate makes atomic and loud

**M2.9 review follow-up (runner-command normalization)** — The independent review's P2: two runners (`framework-typescript-conformance-runner`, `providers-typescript-conformance-runner`) invoked the shared engine through package-local wrapper scripts, so their `conformance` commands did not contain the engine path and the gate's engine-invocation check could not see them — a triple-edit misclassification (tag swap + wrapper) would have passed silently. Both wrappers were pure pass-throughs (spawn `tools/conformance/runner/run.ts` from repo root with a fixed `--adapter` + `--summary-only`), so their `conformance` targets were normalized to invoke the engine directly, matching the other six runners, and the wrapper scripts (`src/framework-typescript-conformance.ts`, `src/providers-typescript-conformance.ts`) were retired per invariant §10.4 with the direct command as the proven successor (both runners re-proven green uncached after normalization). The engine-invocation guarantee now genuinely covers all 8 runners, and the `layer:testkit` exemption no longer shields any engine-invoking command. Also from review: `tools/scripts/conformance.ts` forwards CLI argv to the underlying `nx run-many`, and both new tools scripts use node APIs (`node:child_process`, `node:fs` walk) instead of Bun globals — the Bun globals were latent full-repo lint failures that only verify's workspace-lint phase (not `check`) would have caught.

**M10.3a addendum — `.constitution/prd/` and `.constitution/architecture/` rewritten to the post-#87 world (issue #87 §13 M10 DoD; §14 naming re-centering); `.constitution/tech-spec/`, `.constitution/tasks/`, and `AGENTS.md` are explicitly out of scope for this sub-milestone and carried to a later M10.x.** This resolves, for these two layers only, the "stale-pending-M10-rewrite" state both layers' own v0.10.1 changelog entries recorded (and which the M0.3-era entries first promised). Re-measured against the pre-edit tree rather than trusting any prior estimate. `git grep -c "Tuvren Runtime"` per file: PRD `actors.md` 2, `capabilities.md` 4, `domain-model.md` 3, `glossary.md` 1, `vision.md` 13 (PRD subtotal 23); architecture `containers.md` 2, `resilience.md` 1, `strategy.md` 2 (architecture subtotal 5); `risks.md` and every `flows/*.md` had zero. `git grep -ic "\bdriver"` per file: PRD `actors.md` 0, `capabilities.md` 9, `domain-model.md` 0, `glossary.md` 4, `vision.md` 5, `out-of-scope/README.md` 1 (PRD subtotal 19; `constraints.md` had zero); architecture `containers.md` 39, `resilience.md` 6, `risks.md` 13, `strategy.md` 10 (body subtotal 68), plus `flows/flow-bounded-execution-runaway-turn.md` 5, `flow-driver-handoff-child-orchestration.md` 5, `flow-exposure-time-tool-surface-policy.md` 4, `flow-host-adds-external-mcp-server.md` 3, `flow-invocation-time-binding-policy-attribution.md` 3, `flow-operational-telemetry-export.md` 2, `flow-react-driver-turn-execution.md` 8, `flow-reference-host-proves-sdk.md` 4, `flow-tool-approval-pause-resume.md` 8 (flows subtotal 42; the remaining 9 flow files had zero) — architecture grand total 110. `git grep -n "boundaries/"` was 1 hit in each layer, and in both cases the single hit was the layer's own v0.10.1 changelog line quoting the M0.3-era promise itself, not a stale physical-path citation elsewhere in the layer's body files — so the "boundaries-path-fixed" count for both layers is **0 body-file fixes, 2 changelog citations left verbatim as historical record** (superseded by the new v0.11.0 entries added in this pass, which state the promise is now resolved for these two layers).

**Per-layer classification counts (all execution-strategy; zero resource-adapter "driver" usage found in either layer's prose — tty/output, syslog, bus, and provider drivers are implementation/architecture-detail terms this product- and logical-layer prose never names):**
- PRD (`actors.md`, `capabilities.md`, `constraints.md` unaffected — zero hits, `domain-model.md`, `glossary.md`, `out-of-scope/README.md`, `vision.md`): 19 driver→runner renames (Epic "Driver Modularity" → "Runner Modularity"; glossary `Driver`/`ReAct Driver` rows → `Runner`/`ReAct Runner`; "driver registry" → "runner registry" ×2; "Driver hot-swap" → "Runner hot-swap"; "Framework vs. driver" distinction → "Framework vs. runner"; remaining plural/singular prose hits across `capabilities.md`, `glossary.md`, `vision.md`, `out-of-scope/README.md`), 0 resource-adapter drivers kept, 0 `boundaries/*` body-file paths found (the changelog's own citation is historical, not a body-file defect), 23 "Tuvren Runtime" → "Tuvren" renames across 5 files (13 in `vision.md` alone, 4 in `capabilities.md`, 3 in `domain-model.md`, 2 in `actors.md`, 1 in `glossary.md`) plus the two naming-definition sentences (`vision.md` §1.1, `glossary.md` row) rewritten in full per §14 rather than mechanically substituted.
- Architecture (`containers.md`, `resilience.md`, `risks.md`, `strategy.md`, `flows/*.md`): 110 driver→runner renames (68 across the four top-level files, 42 across nine `flows/*.md` files; Driver Runtime → Runner Runtime container/participant/diagram-ID renames across `containers.md` and every flow's mermaid diagram; `ReAct Driver` → `ReAct Runner`; "Mechanism-policy-driver separation" → "Mechanism-policy-runner separation"; "Driver plurality" → "Runner plurality"; two flow files renamed — `flow-driver-handoff-child-orchestration.md` → `flow-runner-handoff-child-orchestration.md`, `flow-react-driver-turn-execution.md` → `flow-react-runner-turn-execution.md`, confirmed zero cross-references to either filename anywhere in `.constitution/` before renaming), 0 resource-adapter drivers kept, 0 `boundaries/*` body-file paths found, 5 "Tuvren Runtime" → "Tuvren" renames (`containers.md` ×2 including the System_Boundary diagram label, `resilience.md` ×1, `strategy.md` ×2).

**Product-naming rewrite (§14) — exact before/after text:**
- `.constitution/prd/vision.md` §1.1, before: `Tuvren is the company brand, Tuvren Runtime is the runtime product, and Kraken is the engine identity behind it.` — after: `Tuvren is both the company brand and the product name; Kraken is the engine identity behind it. (Naming re-centered per issue #87 §14 at M10.3a: the prior definition — "Tuvren is the company brand, Tuvren Runtime is the runtime product, and Kraken is the engine identity" — treated "Tuvren Runtime" as a separate product name distinct from the brand; that separation is retired, and ordinary library consumers are never expected to type `Kraken*`. This is a naming re-centering only — it does not itself rename the repository, which currently remains the framework repo pending its own post-merge rename.)`
- `.constitution/prd/glossary.md` row, before: `| Tuvren Runtime      | The overall product surface that enables durable, stateful agent execution and orchestration.                                                | engine, bot framework, wrapper         |` — after: `| Tuvren              | The company brand and product name for the overall product surface that enables durable, stateful agent execution and orchestration; re-centered away from the prior "Tuvren Runtime" naming per issue #87 §14 (M10.3a). | Tuvren Runtime, engine, bot framework, wrapper |`
- Both layers' `## 0. Version` headers bumped `v0.10.0` → `v0.11.0` (PRD `vision.md`, architecture `strategy.md`), matching new `v0.11.0` changelog entries appended to `.constitution/prd/changelog.md` and `.constitution/architecture/changelog.md` describing this exact rewrite.

**Judgment calls flagged for human review (not guessed destructively):**
1. The two renamed flow files (`flow-runner-handoff-child-orchestration.md`, `flow-react-runner-turn-execution.md`) were renamed via plain filesystem move (not `git mv`) and left as untracked-new/tracked-deleted pairs in the working tree per this sub-milestone's no-commit constraint; SATISFIED AT COMMIT (`e713f637` staged both sides; git recorded R73%/R76% renames) — the guidance was: whoever commits M10.3a should use `git add`/`git rm` (or re-run as `git mv` against the already-edited content) so the rename is recorded as a rename, not an add+delete, in history.
2. `.constitution/prd/constraints.md` and `.constitution/prd/out-of-scope/README.md`'s non-driver prose, and `.constitution/architecture/changelog.md`/`.constitution/prd/changelog.md`'s pre-v0.11.0 historical entries, were deliberately left untouched beyond the one out-of-scope "Driver hot-swap" rename — they contain no other stale terminology or paths on re-measurement, so no additional edit was judged necessary; flagged here only so a reviewer doesn't read the silence as an oversight.
3. The architecture-layer "Runner Runtime" container name (renamed from "Driver Runtime") is a naming inference from the live `spec/runners/`/`typescript/runners/react` authority, not a literal citation from issue #87's own text (the issue names the npm package `@tuvren/runner-react` and the port `spec/runners/`, but does not itself spell out a logical-container name) — flagged as a judgment call in case a later milestone wants a different logical-container label.
4. `spec/core/src/runner` note: the worker found the M2 ledger row (line ~56) still carried `typescript/core/src/driver` in its `new_path` cell even though the M6.4 subpath rename it deferred to had long since landed (live tree confirmed at `typescript/core/src/runner`). RESOLVED IN THIS SAME COMMIT by the orchestrator: the cell now records both states ("landed at M2.3 as `src/driver`; subpath renamed at M6.4") — the M10.3a review initially caught this addendum still describing the drift as outstanding while the same commit fixed it (P2), corrected here.

**M10.2 addendum — the repo's first CI lane (`.github/workflows/ci.yml`), authored greenfield (no `.github/` existed before this milestone).** DoD re-scope carried in: the issue's original "Bazel is the canonical CI path; Nx demoted to optional inner-loop convenience" was already reversed at M3.0 (rules_js has no bun.lock path; every TS suite here is bun:test-based with no upstream Bazel support), so this milestone authors CI against the reversed scope rather than the stale original one — `bun run verify` stays the PRIMARY semantic gate, and Bazel gets a standing but narrower hermetic lane. **The `.bazelignore` finding (carried from a prior interrupted attempt, re-measured here, kept):** devenv's `.devenv/` is a symlink farm into the Nix store; Bazel's default package-discovery walk descends into it and chokes on a foreign vendored `upb` source tree pulled in by a Nix package's debug symbols, failing every wildcard Bazel command (`bazel query/build/test //...`) outright once `devenv shell`/`direnv allow` has run at least once. Root `.bazelignore` excluding `.devenv` fixes it; re-verified green both before and after this milestone's edits. **Local proof of the Bazel gate's real scope:** `bazel build //...` succeeds across the full 1970-target graph (9,681–10,281 actions depending on cache state, all cache-hit or trivial on repeat runs) — cheap even for the 17 TypeScript shim packages, because building a `native_binary` only compiles its `tools/bazel/nx-run.sh` launcher, it does not execute the wrapped Nx target (that only happens via `bazel run`, which needs `BUILD_WORKSPACE_DIRECTORY` and is out of scope for a wildcard invocation). `bazel test //...` (`kind(test, //...)` query confirms this) selects exactly one real test target in the whole graph, `//rust/kernel:kernel_baseline_test` — the TS shims' `:test` targets are `native_binary` (runnable), not `native_test`/`sh_test`, so Bazel's test-kind selector correctly never touches them; their actual execution already happens through Nx/bun:test inside `bun run verify`, not through `bazel test`. No scope narrowing was needed beyond what M3.0 had already recorded — both wildcard commands are green as-is, so the "bazel" CI job runs them unmodified rather than carving out a `//rust/...`-only subset. **Workflow design:** one file, two jobs, push-to-master + pull_request triggers, a `cancel-in-progress` concurrency group keyed on workflow+ref. Job `verify` installs Nix (`DeterminateSystems/nix-installer-action`, pinned by major version only — this authoring session has no network path to confirm the current marketplace release tag) + `devenv` (`nix profile install nixpkgs#devenv`, avoiding a second marketplace Action), then runs `devenv shell -- bash -c 'bun install --frozen-lockfile && bun run services:up && bun run verify'` (Postgres via the M1.6-sanctioned `devenv processes` path, generous 60-minute ceiling). Job `bazel` provisions the same way and runs exactly the two proven commands (30-minute ceiling). Both jobs are independently named and commented so a first failed live run is diagnosable without re-deriving this rationale. **Validated locally, what couldn't be:** the workflow YAML parses clean (verified via the repo's own bundled `yaml` package, since no system Python/yq was available); `nix run nixpkgs#actionlint` (fetched successfully once concurrent stray `nix run` invocations stopped contending for the local eval-cache SQLite lock) reports zero findings against `ci.yml`. Not verifiable offline: the exact current major-version tag for `DeterminateSystems/nix-installer-action`, whether `nix profile install nixpkgs#devenv` is the idiomatic non-interactive devenv-install invocation GitHub's Ubuntu runners expect, and — by construction, since this is the repo's first workflow — actual GitHub Actions runner behavior; the first live push/PR run is the real proof and the orchestrator watches it. **MODULE.bazel diary trim:** the ~30-line M1.1/M1.6/M3.0 milestone-diary module docstring collapsed to an ~13-line summary keeping only the two governing rulings (postgres exception, TS-hermeticity reversal) and pointing to issue #87 + `MIGRATION_INVENTORY.md` for full history; `bazel build //...`, `bazel test //...`, and `bun run check` re-verified green after the trim (change is comment-only, no target-graph impact). `[DEBUG-87-M10]` marker sweep across the full tree came back empty (matches the M10.1 addendum's prior sweep — nothing new introduced this milestone).

**M10.2 postscript (post-authoring updates + first live run; the addendum above describes the worker's offline authoring state, this paragraph is the shipped truth).** (1) Action pins were live-verified via the GitHub releases API before the push and bumped from the offline placeholders: `actions/checkout@v7`, `DeterminateSystems/nix-installer-action@v22` — the addendum's "not verifiable offline" pin caveat is resolved. (2) The shipped trigger set is master + pull_request + `workflow_dispatch` + a TEMP push trigger for the epic branch (marked `TEMP(87-M10.2)` in-file with an explicit REMOVE-before-merge instruction; removal happens in the PR-preparation commit) so the greenfield workflow validated live before the PR. (3) The push required switching origin to SSH — the HTTPS OAuth token lacked the `workflow` scope for pushing workflow files. (4) **First live run (28674899850): bazel job GREEN; Nix+devenv provisioning GREEN; verify job FAILED on a real cold-start bug the run exposed** — the gitignored kernel-interop generated bindings (`typescript/runtime/src/lib/generated/kernel-interop`) don't exist on a fresh checkout, and verify's authority-packet + guardrails freshness gates run BEFORE its own interop-codegen phase; locally the artifact persists between runs so this never surfaced. The root `codegen` script cannot bootstrap it (circular: it runs the failing validators first). Fix: the CI verify step now runs `bun run nx run kernel-interop-grpc:codegen` after `bun install` as the minimal honest bootstrap. **Follow-up (open):** decide whether verify's phase ordering should make it cold-start-correct (interop codegen before the freshness gates) — a semantic change to the release gate deliberately not made as a CI side-effect; candidates: M10.5 or post-epic.

**M10.1 addendum — the residual sweep: every live-tree reference to the evacuated `boundaries/` world dies or is justified (`git ls-files boundaries/` has been zero since M9.3; only empty untracked directory husks remain).** Ten gate/tooling scripts still scanned a dead `boundaries` root as one of two (or more) search roots feeding a scan list, a resolver, or a validation allowlist — because every one of them guarded the walk with `existsSync`/filtered-root patterns, the dead root silently contributed zero paths instead of failing loud, which is a "gate quietly stops checking real files" class of bug, not a cosmetic one. Fixed by removing the dead root and its consuming code path outright (not just the constant), keeping the live `spec/`/`typescript/`/`rust/` roots: `tools/conformance/adapter-protocol/validate-adapter-protocol.ts` (adapter-manifest discovery repointed from `boundaries` to `typescript`+`rust`, the two homes where every real `adapter*.json` now lives); `tools/conformance/plan-compiler/index.ts` (conformance-plan discovery narrowed to `spec/` only, stale "flat legacy shape" comment describing the old `boundaries/<port>/conformance/plans/` layout removed); `tools/conformance/vocabulary/validate-vocabulary.ts` (authority-packet-manifest discovery narrowed to `spec/` only); `tools/scripts/authority-guardrails/authority-guardrails.ts` (three separate dead-root uses: packet discovery narrowed to `spec/`; the framework-owned-fixture scan's `FRAMEWORK_TYPESCRIPT_ROOT` constant — which pointed at the single pre-M3 contract root that held every framework TypeScript package — repointed to the whole `typescript/` tree, since the framework boundary's packages are now scattered across `typescript/{runtime,testkit,conformance-adapter,streaming/*,telemetry/otel,runners/react}`; the conformance-source-root walker (`findConformanceSourceRoots`/`collectConformanceSourceRoots`, which looks for `certification`/`conformance-adapter` dirs with a `src/` subdir) repointed from `boundaries` to `typescript`+`rust`. **Proof the fix actually re-arms the gate, not just moves the no-op:** a probe file containing `frameworkStreamTestFixtures` dropped into `typescript/conformance-adapter/src/` before the fix passed silently (both scan roots resolved to zero files under `boundaries`); after the fix the same probe trips both `typescript-owned-conformance-source` and `typescript-owned-framework-fixture` guardrail failures, then was removed and guardrails re-verified green); `tools/scripts/authority-packet/validate-authority-packets.ts` (manifest discovery narrowed to `spec/`); `tools/scripts/docs-authority-freeze-gate.ts` (the `EVIDENCE_PATH_PATTERN` regex's root alternation swapped `boundaries` for `typescript`+`rust` so doc-cited evidence paths under the live implementation roots are recognized; the generated summary's two "Source Inputs" bullet path literals corrected to `spec/*/authority-packet.json` and `spec/conformance/*/plans/*.json` — regenerated via `bun run docs:authority-freeze`, diff is exactly those two lines, `docs:authority-freeze:check` green after); `tools/scripts/epic-af-conformance-gap-plan.ts` (`BOUNDARIES_ROOT` constant and its use in `readImplementedCheckEvidence` removed, plan discovery narrowed to `spec/`, matching comment updated; `docs:af-gap-plan:check` clean, zero drift); `tools/scripts/portability-gate.ts` (`BOUNDARIES_ROOT` removed from both the authority-packet-manifest and adapter-manifest discovery unions, `findFiles`/`findFilesByName` already guarded with `existsSync` so the removal is a pure narrowing, not a behavior change for the live roots; `portability-gate.ts` green, 9 packets/3 standing exceptions/12 required sources unchanged). **`tools/scripts/migrate-to-core.ts` special case — retired, not repointed.** This was the one-shot KRT-AP006 codemod that rewrote imports off the five now-fully-retired contract packages (`@tuvren/core-types`, `@tuvren/runtime-api/*`, `@tuvren/event-stream`, `@tuvren/tool-contracts`, `@tuvren/driver-api`) onto `@tuvren/core` subpaths; the M9.2 addendum had already flagged its `EXCLUDE_SRC_PREFIXES` boundaries-path entries as stale and explicitly deferred the cleanup, and the M6.4b addendum flagged the disposition question itself as "M10.1 disposition." Measured before deciding: zero wiring (`grep -rn migrate-to-core` across every `package.json`/`project.json` in the repo returns nothing — it has never been an `nx`/`bun run` target); all five packages it targets are already fully migrated/retired (M2.1–M9.2), so its `DIRECT_REMAPS`/`RUNTIME_API_ROOT_ROUTING` tables describe import specifiers that no longer exist anywhere in the tree — the script is not merely unwired, it is unrunnable-to-any-effect; and its full per-symbol mapping value is already preserved in prose across the M2.1, M6.4b, and M9.2 ledger addenda (the DIRECT_REMAPS list and RUNTIME_API_ROOT_ROUTING table were read in full before this decision — nothing in them is lost by deleting the file). Applying the same §10.4/M5.1b/M9.2 precedent this task cited for import-dead shims: `git rm tools/scripts/migrate-to-core.ts` (staged, uncommitted). The two literal fixture paths it also touched (see below) were fixed independently since they exercise a still-live guardrail self-test, not this script. Config residue: `reports/compatibility/project.json`'s three targets (`codegen`, `check`, `evidence-refresh`) each carried two dead input globs (`boundaries/*/conformance/**`, `boundaries/*/implementations/*/conformance-adapter/**`) — repointed to `spec/conformance/**/*` plus one explicit glob per live adapter home (`typescript/conformance-adapter`, `typescript/kernel/conformance-adapter`, `typescript/providers/conformance-adapter`, `rust/conformance-adapter`, `rust/kernel-conformance-adapter` — the full adapter census, confirmed by grepping every `adapter*.json` in the tree), verified via `nx run compatibility-reporting:check` green; `nx.json`'s `conformance` target's `//` doc-comment cited the dead literal `boundaries/*/contracts/**/spec/authority-packet.json` describing the packet-selection mechanism — reworded to the live `spec/*/authority-packet.json` shape; `.gitignore`'s `!boundaries/hosts/implementations/typescript/playground/.env.example` negation was already inert before this edit (the current repl `.env.example` lives at `typescript/host/repl/.env.example`, untracked and already `.env.*`-ignored with no live negation covering it — confirmed via `git check-ignore -v` both before and after removing the line, so this is a pure dead-line deletion with no tracked-file impact) — deleted; `.repomixignore`'s `boundaries/kernel/implementations/typescript/backend-sqlite/src/` entry (the sqlite backend now lives at `typescript/kernel/backends/sqlite/src/`, uncovered by any repomix-ignore entry post-move — no repoint added, since the task scope was deletion of the dead line, not curating a new repomix-size exclusion) — deleted. Docs/test residue: `docs/perf-benchmarks.md`'s four path examples in the W1/W2 method sections repointed to their live equivalents (`spec/conformance/**`, `spec/conformance/kernel/plans/kernel-protocol-core.json`, `typescript/tools/mcp-client/src/index.ts` — the measurements themselves are historical evidence and were not re-run or fabricated); the W2 project-name list's `framework-runtime-core` entry annotated as a historical project name recorded at measurement time, retired at M3.2c when `@tuvren/runtime-core` folded into the successor engine package now at `typescript/runtime` (Nx project `framework-runtime`) — left as originally recorded rather than silently rewritten. `typescript/kernel/testkit/test/kernel-testkit.test.ts`'s `rg` search-scope argument list dropped the `"boundaries"` literal (flagged for this exact deletion by the M9.3 addendum's repo-wide walker-class sweep), keeping `"typescript"`; `kernel-testkit:test` re-run green (13/13) after. `tools/scripts/authority-guardrails/__fixtures__/forbidden-authority-evidence/evidence.json`'s synthetic `authority` value cited the already-retired `boundaries/framework/contracts/runtime-api/implementations/typescript` path as a forbidden-authority example — read what the fixture exercises (the guardrails self-test's `checkForbiddenAuthorityEvidence` assertion path, which needs *any* string that appears in a packet's `forbiddenAuthoritySources` list to prove the detector fires) before choosing a replacement: `spec/core/authority-packet.json`'s live `forbiddenAuthoritySources` array was read for a current example and `typescript/certification` was picked (matching both the fixture JSON and the guardrails script's own inline synthetic-manifest `forbiddenAuthoritySources` array at the same call site, which cited the identical dead path); `authority-guardrails.ts` self-test re-run green after. **Generator retirement (locked decision, §10.4/M6.1a precedent).** `tools/scripts/conformance/generate-providers-plans.ts` carried its own header flagging itself `⚠️ STALE SCAFFOLD — DO NOT RUN`, measured at M4.1 to regress 6 assertion `kind` fields of `provider-api-bridge-extended.json` from the committed `resultField` back to a stale `evidenceField` if ever run, and explicitly "carried to 87-M10.1" in that same header. Re-verified zero wiring (no `project.json`/`package.json`/README reference anywhere) before deleting; the committed plans at `spec/conformance/providers/plans/` are the successor per the M3.4 "committed JSON is authority; the generator is tooling" ruling (this repo's CLAUDE.md codifies the same rule for every canonical encoding). No committed plan or packet carries a warning header naming this generator (checked `spec/conformance/providers/plans/*.json` and `spec/providers/authority-packet.json`'s `generatedArtifacts` entries — both cite only the unrelated TypeSpec generators), so no header rewrite was needed. `git rm tools/scripts/conformance/generate-providers-plans.ts` (staged, uncommitted); `tools/scripts/conformance/` still holds `format-generated-json.ts`, `generate-kernel-fixtures.ts`, and `generate-kernel-plans.ts` (all still wired/live for the kernel port), so the directory itself stays. **Measure-and-report only, no edit:** `typescript/conformance-adapter/src/framework-adapter-runtime.ts:79`'s `RUNNER_ID = "typescript-conformance-runner"` was traced through every consumer (`framework-adapter-invocation-lifecycle.ts`, `framework-adapter-capability-orchestration-integration.ts`, `framework-adapter-secret-isolation.ts`, `framework-adapter-execution-bounds.ts`, and 15 more `framework-adapter-*.ts` files) — it flows only into `defaultRunnerId` options passed to `createTuvreRuntimeCore`/`createTuvrenRuntimeCore`, which configure the engine's fallback runner identity for scenarios that don't register one explicitly. It does **not** appear in any committed conformance plan assertion (the plans that reference `runnerId` check boolean inheritance flags like `$.orchestration.inheritance.runnerIdInherited`, never the literal runner-id string), any adapter manifest (`adapterId` is the separate, unrelated `"typescript-framework"` value), or any checked-in `reports/compatibility/evidence/*.json` file (grepped all nine; the one textual hit is an unrelated assertion-ID label containing the substring "runner-id", not this constant's value). Conclusion: **not identity-bearing evidence** — a rename would touch ~19 adapter-source files but zero checked-in evidence, so this is a cosmetic-naming question outside this milestone's scope, not a residual-boundaries defect; left unchanged. Mechanics: Biome `--write` applied to every rewritten source/config file (one reflow fix in `tools/scripts/portability-gate.ts`, zero semantic changes elsewhere); `[DEBUG-87-M10]` marker sweep across the full tree came back empty. Validation: every edited gate script re-run standalone and green (`validate-adapter-protocol.ts`, `validate-plans.ts` 32/32, `validate-vocabulary.ts`, `validate-authority-packets.ts`, `portability-gate.ts`, `authority-guardrails.ts` self-test, `docs:authority-freeze:check`, `docs:af-gap-plan:check`); `nx run kernel-testkit:test` 13/13 green; `bun run check` green.

**M9.3 addendum — the reference shell moves to its idiomatic home; `boundaries/hosts` is fully evacuated.** `boundaries/hosts/implementations/typescript/repl` → `typescript/host/repl` (`git mv`; Nx project name `host-repl` and npm name `@tuvren/repl-host` KEPT — `host-repl` already matches the category-component convention and is referenced pervasively, so M6-style renames do not apply here). Depth recomputation (5-deep → 3-deep, same arithmetic class as M8.1b/M8.2b): `tsconfig.json`/`tsconfig.lib.json` `extends` → `../../../tsconfig.base.json`; `tsconfig.lib.json`'s 9 cross-package `references` and `tsconfig.dts.json`'s 7 `paths` entries collapse from `../../../../../typescript/...` to `../../../typescript/...`; `tsconfig.typecheck.json`'s `rootDir` → `../../../` and its 10 `include` cross-package globs follow the same collapse. `tsconfig.typecheck.json` also had a dead `@tuvren/sdk` path mapping (`["../../../../../typescript/sdk/src/index.ts"]`) — PRUNED after confirming zero `@tuvren/sdk` imports anywhere in `src`/`test`/`smoke` (grep, zero hits); this closes the SDK-exclusivity evidence for the DoD's SDK-tier clause (see proof below). `project.json`: `$schema`/`root`/`sourceRoot` plus 6 `cwd` values and 2 mid-string command-path arguments (`lint`, `typecheck`) rewritten from the old boundary path to `typescript/host/repl` (9 internal strings total); `tags: ["boundary:hosts", "layer:implementation"]` deliberately left unchanged — tags record historical semantic boundary, not filesystem location, matching the `framework-runtime`/`framework-telemetry-otel` precedent of keeping `boundary:framework` post-move. Repo-wide reference sweep (9 sites, all verified by `git grep -n "boundaries/hosts"` before and after): root `package.json` workspaces gained `typescript/host/*` and dropped the `boundaries/*/implementations/*/*` glob (verified dead first — `find boundaries -path "*/implementations/*/*/package.json"` returned zero matches once repl was gone, confirming repl was its sole match); `tsconfig.base.json`'s `@tuvren/repl-host` path mapping and root `tsconfig.json`'s project reference; `typescript/conformance-adapter/src/framework-adapter-proving-host.ts:27` and `tools/scripts/repl-host-interop-smoke.ts:43,47`'s hardcoded `dist/{index,cli}.js` paths; `spec/conformance/interop/rust-kernel/spec/authority-packet.json:21,25`'s `bindingProjections.typescript-host` and `forbiddenAuthoritySources` entries; `spec/extensions/README.md`'s proof-extension pointer ("moves to the host tree at M9" → arrived-state sentence); `spec/host/README.md`'s two stale-still-at-`boundaries/hosts` sentences (the port-vs-implementation distinction paragraph, and the "still lives at ... it moves at 87-M9.3" closing note) corrected to the arrived state. `tools/scripts/portability-check.ts` was checked for a repl/host `packageRoot` registry entry — none exists (the file's only repl mention is the unrelated `WORKSPACE_RUNTIME_CLASSIFICATION`-style `packageName`-keyed "mixed-runtime validated" entry, not a path-keyed `packageRoot`), so no edit was needed there. P3 carry from the M9.2 review: `typescript/providers/provider-api/src/index.ts:19-20`'s stale comment naming the retired `runtime-api` subpath reworded to name the actual successor, `@tuvren/core/provider`. New M1.2-pattern `BUILD.bazel` shim at `typescript/host/repl/BUILD.bazel` (mirrors `typescript/telemetry/otel/BUILD.bazel`; `NX_TARGET` strings use `host-repl:build`/`host-repl:test`); MODULE.bazel's bun:test census sentence gained the reference host shell (`typescript/host/repl joined at M9.3`) after confirming all 6 of its test files import `bun:test`. The M4.2d walker claim ("`repl-shell.ts`'s `MOCK_MCP_STDIO_SERVER_PATH_SEGMENTS` walker ... stays valid when repl itself moves") was VERIFIED by reading `findMockMcpStdioServerPath()` rather than assumed: it walks up from both `process.cwd()` and `dirname(fileURLToPath(import.meta.url))`, joining the root-anchored segment array `["typescript","providers","testkit","src","bin","mock-mcp-stdio.ts"]` at each ancestor level until a match exists on disk — this construction is depth-independent, so the repl package's own move changes nothing about it; confirmed move-proof, zero code change required. **SDK-exclusivity proof (DoD clause, SDK-tier interpretation):** deduplicated `@tuvren/*` imports across `typescript/host/repl/{src,test,smoke}` — `@tuvren/backend-memory`, `@tuvren/backend-postgres`, `@tuvren/backend-sqlite`, `@tuvren/provider-bridge-ai-sdk`, `@tuvren/repl-host` (self), `@tuvren/runner-react`, `@tuvren/runtime`, `@tuvren/stream-agui`, `@tuvren/stream-core`, `@tuvren/stream-sse` — matches the expected M9.0 set exactly; a repo-wide grep of the same three dirs for `Kraken`, dynamic `import(...@tuvren...)`, and direct `@tuvren/core`/`@tuvren/core-types` imports returned zero hits in all three cases. Mechanics: `bun run nx reset` run before any graph-dependent command; `bun install` after the workspaces edit (lockfile updated, "no changes" to installed packages — pure workspace-path rewrite); one dangling `node_modules/@tuvren/driver-react` symlink found and removed from the package-local `node_modules` (pre-existing stale artifact from the pre-M6.2 `drivers/react` era, carried along by the directory move; not referenced by `package.json`); zero `[DEBUG-87-M9]` markers found. Post-move `boundaries/` state: zero git-tracked files (`git ls-files boundaries/` empty); three empty, untracked directory husks remain (`boundaries/hosts`, `boundaries/hosts/implementations`, `boundaries/hosts/implementations/typescript`) alongside the pre-existing `boundaries/kernel/{contracts,implementations}/...` husks — left alone per M10's ceremonial-deletion ownership, not removed here. Biome reformatted two files after the path-string edits shortened line lengths past the wrap threshold (`typescript/host/repl/tsconfig.typecheck.json`, `typescript/conformance-adapter/src/framework-adapter-proving-host.ts`) — both are pure reflow, no semantic change. Validation: `host-repl:typecheck`/`:build`/`:test` (127/127)/`:exports-smoke` (1/1) all green; `host-repl:interop-smoke` green against live postgres/MCP infra (11/11 scenarios, headless JSONL smoke passed); `validate-plans.ts` 32/32; `portability-gate.ts` green (9 packets, 3 standing exceptions, 12 required sources); `bun run check` fully green (affected typecheck/test/lint plus the Rust workspace gates, all pass).

**M9.2 addendum — `@tuvren/runtime-api` shim retired (M6.1c/M8.1c pattern; successor `@tuvren/core/{execution,events,messages,provider,tools}`, import-dead-of-original-code at deletion).** Home 2 of 2 (deferred at M9.1: "implementations/typescript → typescript/host/..., idiomatic subpath decided at M9.2") is settled by measurement, not by the earlier placeholder plan: all 6 src files (`index.ts`, `execution.ts`, `events.ts`, `tools.ts`, `provider.ts`, `orchestration.ts`) were pure `export {...} from "@tuvren/core/<subpath>"` re-export barrels — zero original definitions, same shape as the already-retired `@tuvren/driver-api` (M6.1c) and `@tuvren/event-stream` (M8.1c) shims — so the retirement pattern applies instead of the anticipated physical move; `spec/host/README.md`'s M9.1-era sentence claiming the eventual destination was `typescript/host/...` is corrected in place, not carried forward as a stale claim. Live consumers measured at M9.0/M9.1: 7 type-position `import("@tuvren/runtime-api")` sites — `typescript/runtime/test/durable-reads.test.ts:294,316,445` (`TurnSnapshot`) and `typescript/conformance-adapter/src/framework-adapter-runtime-scenarios.ts:73,78,87,107` (`TuvrenModelResponse` ×2, `TuvrenPrompt`, `StructuredOutputRequest`) — all rewired to `@tuvren/core/execution` and `@tuvren/core/provider` respectively (subpaths match the shim's own re-export source, not a new decision). `typescript/providers/provider-api/package.json`'s `@tuvren/runtime-api` dependency had zero import sites in that package's `src`/`test`/`smoke` (the one grep hit was a stale comment, not an import) — pruned. Test classification before deletion (M6.1c lesson applied): the shim's 6 test files exercised real `@tuvren/core` guard-function behavior (`isTuvrenMessage`, `isApprovalRequest`/`isApprovalResponse`/`isApprovalResponseForRequest`, `isProviderStreamChunk`, `isTuvrenStreamEvent`, `isTuvrenToolDefinition`, `isExecutionStatus`, and their `assert*` counterparts) for which **no other test file in the repo provides dedicated coverage** — `typescript/core/src/lib/runtime-contract-guards.ts` (906 lines, defines all of the above) had zero existing test file, confirmed by grepping every `typescript/core/test/*.test.ts` for these guard names before porting. This is UNIQUE coverage, not barrel-reexport duplication (unlike M8.1c's event-stream case, where one of two tests was a full duplicate) — ported nearly byte-close to `typescript/core/test/runtime-contract-guards{,.approval,.execution-shape,.manifest-status,.tool-definition,.validation}.test.ts` (import sources changed to the matching `@tuvren/core/*` subpath, describe titles renamed, nothing else) plus `runtime-contract-guards-fixtures.ts` (verbatim copy — the fixture module already imported `@tuvren/core/*` directly, never the shim). Two classes of the original `runtime-api.test.ts` content were deliberately NOT ported (recorded here per §9, not silently dropped): (1) the "exposes narrow runtime-api subpaths without changing contract behavior" test and its file.done subpath variant asserted that the shim's OWN `./events`/`./execution`/`./orchestration`/`./provider`/`./tools` re-export barrels behaved identically to its root — shim-internal structural coverage that dies with the shim, not `@tuvren/core` coverage (the file.done guard assertion itself is preserved as a plain test); (2) "exposes the orchestration contract surface..." and "exposes a host-facing type surface..." exercised the test harness's own hand-written `TuvrenRuntime`/`OrchestrationRuntime` mock (`frameworkContractFixtures.runtime`/`.orchestrationRuntime`) — the assertions verify the mock does what its own code says, not any real `@tuvren/core` logic, so porting them would manufacture coverage that doesn't exist. 79 ported tests green standalone and in the full `typescript/core/test` run. Same-commit prunes: `tsconfig.base.json` (`@tuvren/runtime-api` + 5 subpath aliases), root `tsconfig.json` reference, `tools/scripts/verify.ts` (3 curated-list entries: `WORKSPACE_TEST_LANE_PROJECTS`, `WORKSPACE_BUILD_PROJECTS`, `WORKSPACE_EXPORT_SMOKE_PROJECTS`), `tools/scripts/portability-check.ts` registry entry, 17 consumer tsconfig files' path-mapping/reference/include entries across `typescript/{streaming/core,testkit,providers/{testkit,provider-api,bridge-ai-sdk,certification},kernel/protocol,runners/react,runtime}` (grep-verified zero residual `runtime-api` strings post-edit), root `package.json` workspaces glob `boundaries/*/contracts/*/implementations/*` (verified dead first — it matched exactly this one path, nothing else, before deletion), `spec/core/authority-packet.json`'s `forbiddenAuthoritySources` impl-path entry (M9.1 had deliberately left it untouched pending this milestone; M6.1c/M8.1c precedent — the list tracks live paths, not historical ones), `tools/scripts/migrate-to-core.ts`'s now-dead `EXCLUDE_SRC_PREFIXES` entry (its `DIRECT_REMAPS`/per-symbol routing table for `@tuvren/runtime-api` stays as historical codemod record, M5.1b precedent — the sibling `event-stream`/`driver-api` exclusion lines were found still present from their own earlier retirements, an inconsistency not fixed here as out of scope), workspace member removed from `bun.lock` (`bun install`: "1 package removed"), 3 dangling `node_modules/@tuvren/runtime-api` symlinks removed (`typescript/runners/react`, `typescript/providers/provider-api`, `boundaries/hosts/implementations/typescript/repl` — none of their `package.json`s declare the dependency; standing class per M8.1c). `boundaries/framework/contracts/runtime-api/` (including its tombstone `README.md`) and the now-fully-evacuated `boundaries/framework/` removed entirely (the sibling `implementations/typescript` under it was already file-empty per the M8.4 note). `spec/host/README.md`'s M9.1 sentence anticipating a move to `typescript/host/...` corrected to record the actual retirement outcome. Validation: `bun run nx run shared-core:test` green (includes the 79 ported tests), `typescript/conformance-adapter` typecheck green, `bun tools/conformance/plan-compiler/validate-plans.ts` pass, `bun tools/scripts/portability-gate.ts` pass, `bun run check` green.

**M9.1 addendum — the host port is authored; runtime-api's neutral authority arrives.** Per this milestone's DoD ("author `spec/host` and lift the runtime-api authority into it"), the port lands in arrived-port form mirroring `spec/runners/`/`spec/streaming/`: `spec/host/{typespec,bindings,artifacts}` (39 schemas, byte-clean regen verified via `host-spec:codegen`) plus the loose sibling `client-endpoint-integration.md`. Both stale claims retired: the contract root's `spec/README.md` (falsely claiming a standalone `authority-packet.json` declaring `tuvren.framework.runtime-api`) is replaced by an authored `spec/host/README.md` recording the true ADR-037 disposition (vocabulary is `tuvren.shared.core`, no standalone packet), and `spec/host/bindings/typescript.md`'s own stale packet-name references got the same correction. Same-commit reference sweep: `spec/core/authority-packet.json`'s `main.tsp` authoritativeSources path repointed (its `forbiddenAuthoritySources` impl-path entry deliberately untouched — that package doesn't move until M9.2); `tools/scripts/docs-authority-freeze-gate.ts`'s 2 `generatedArtifact` anchors repointed to `spec/host/artifacts/json-schema`; the dead `tuvren.framework.runtime-api` `validate-plans.ts` map key PRUNED (M6.1c/M8.1c precedent — verified by grep that no plan declares that packetId; the `runtime-api-*` plans all declare `tuvren.shared.core`); `spec/conformance/engine/README.md`'s naming note reconciled to a permanent ruling (directory stays `engine`-named, does not rename to `host`); `.repomixignore:44`'s stale `boundaries/framework/conformance/plans/` entry (M8-carry, that tree no longer exists) repointed to `spec/conformance/engine/plans/`; the contract root's own `README.md` rewritten as a tombstone pointing at `spec/host/` (its `implementations/typescript` subtree stays live, M9.2 scope). Portability inventory: NOT bumped — `epic-al-portability-inventory.json`'s `tuvren.shared.core` `requiredAuthoritativeSources` entry never cited the runtime-api contract path (only `spec/core/typespec/main.tsp`), so no path in that file changed; `portability-gate.ts` passes unmodified, confirming the no-op. Root `package.json` codegen script list swapped `framework-runtime-api` → `host-spec`; `implementations/typescript/project.json` lost its `codegen` target (the spec-owned project now owns regeneration, matching every other port). Validation: `host-spec:codegen` byte-clean (39/39 schemas identical to pre-move blobs), `validate-plans.ts` 32/32 plans green, `portability-gate.ts` green, freeze-gate check green, `bun run check` green.

**M8.4 addendum — M8 close: the shared fixture-set schema finds its home; the framework conformance tree is gone.** Ownership decision for `fixture-set.schema.json` (deferred from M8.0 to here): measurement found ZERO code consumers — no tool, harness, plan, or fixture loads it by path; the only live references were the three certification projects' whole-tree input globs and the framework testkit's lint command path. It is a shared conformance asset with no single port owner (exactly as the prefix table predicted), so it landed at `spec/conformance/schemas/fixture-set.schema.json` (byte-pure `git mv`; `$id` left as nominal identifier per the M8.3 ruling). Repoints: `typescript/certification/project.json`, `typescript/certification-batteries-included/project.json`, `rust/certification/project.json` conformance `inputs` swap `{workspaceRoot}/boundaries/framework/conformance/**/*` → `{workspaceRoot}/spec/conformance/schemas/**/*`; `typescript/testkit/project.json` lint command's second path arg follows. With this, `boundaries/framework/conformance/` is fully evacuated and removed. **Correction to the M8.3 commit message (`96d76419`, message-overclaim class, M6.4a precedent — pushed messages are immutable, corrections live here):** its final sentence claimed boundaries/framework/ then held only `conformance/schemas/fixture-set.schema.json` and `implementations/typescript`; the true state also included `contracts/runtime-api` (the M9-scope shim tree, very much file-bearing), and `implementations/typescript` was actually file-empty (unversioned leftover dirs only). Post-M8.4 remaining `boundaries/` content: `boundaries/framework/contracts/runtime-api/` (M9) and `boundaries/hosts/implementations/typescript/repl/` (M9) — nothing else.

**M8.3 addendum — the rust-kernel interop suite moves to its spec conformance home (byte-pure move, identities unchanged).** `boundaries/framework/interop/rust-kernel/` → `spec/conformance/interop/rust-kernel/`: this suite is conformance evidence (a suite manifest, its JSON Schema, and the `tuvren.framework.interop-rust-kernel` authority packet), deliberately distinct from `spec/interop/` (the kernel IPC transport contract lifted at M1.5). Landing shape nests under `interop/rust-kernel/` rather than a flat `interop/` root, preserving room for future interop suites and mirroring how `spec/conformance/{engine,providers,tools,runners,streaming,telemetry}/` are organized; rust-kernel is the sole occupant today, so this is a forward-looking structural call, not a present multi-suite need. `git mv` was a pure directory move (3 files, no content restructuring). Authority-packet edits: `spec/conformance/interop/rust-kernel/spec/authority-packet.json`'s `authoritativeSources[].path` (×2) and `verificationPaths[].target` (×2) recomputed to the new prefix; `bindingProjections`/`forbiddenAuthoritySources` untouched (they name external consumer roots, not the packet's own tree, so the move does not touch them). The schema's `$id` (`https://tuvren.dev/schemas/framework/interop/rust-kernel/suite-manifest.schema.json`) was deliberately left as-is — it is a nominal identifier convention, not a resolved filesystem path, and no other file references it; the sibling `kernel`/`providers` conformance schemas' `$id`s follow a `<boundary>/conformance/...` convention this one predates, so realigning it was judged out of scope for a path-only move (identities stay unchanged, matching every other M8.x path-only precedent). External consumers fixed: `tools/scripts/compatibility-report.ts`'s `INTEROP_RUNNERS[0].manifestPath`; `nx.json`'s `interop-smoke` target-default inputs (`boundaries/*/interop/**/*` dropped entirely — confirmed by repo-wide grep that no other `boundaries/*/interop` directory exists post-move — and `spec/conformance/interop/**/*` added alongside the existing `spec/interop/**/*`); `.constitution/reports/epic-al-portability-inventory.json`'s `tuvren.framework.interop-rust-kernel` `packetPath` + 2 `sourcePath` entries (manifestVersion 0.7.0 → 0.8.0, description sentence appended mirroring the v0.7.0 style) and its MD companion `epic-al-portable-surface-conformance-gap-inventory.md` (4 path mentions across §6.G5 and the current-packets list; the MD↔JSON gate asserts `packetPath` verbatim so this was load-bearing, not cosmetic). Confirmed NOT coupled (repo-wide grep of the old path found no hits): `tools/scripts/kernel-interop-governance.ts`, `tools/scripts/repl-host-interop-smoke.ts` (both surveyed at M8.0 and re-verified here). Checked-in compatibility evidence under `reports/compatibility/` does not embed the manifest path as a string (only `pairId`/`evidencePath`/`implementationId`, none path-shaped to the moved tree), so `compatibility:evidence` regeneration was skipped as unnecessary rather than run speculatively. `.constitution/archived/spikes/*` and `.constitution/tech-spec/stack.md` mentions of the old path were left untouched as historical/narrative content per repo convention (stack.md already carries an unfixed M1.5-era stale path for the same reason — its refresh is an epic-close pass, not a per-milestone one). No Nx project lived at the moved path, so no `nx reset` was strictly required, but it was run anyway per the mandatory mechanics since a project-graph-adjacent boundary directory changed shape. New tree hash `340850d236746ccff29cd535d12851f3fb99aa14` (`git write-tree --prefix=spec/conformance/interop/rust-kernel/`). Validation: `portability-gate.ts` green, `validate-plans.ts` green, `bun run check` green.

**M8.2b addendum — the generated TS telemetry sink leaves the engine into its own leaf package, and `@tuvren/telemetry-otel` closes the deferred M8.2a move (Multi-home §A home 3, closed).** New package `@tuvren/telemetry-semconv` at `typescript/telemetry/semconv` (Nx project name `telemetry-semconv`, free since the spec project took `telemetry-spec` at M8.2a — confirmed by grep, zero live registrations of the old name remained); `git mv` carried the weaver-generated `tuvren-runtime-telemetry.ts` from `typescript/runtime/src/lib/generated/` into the new package's `src/lib/generated/` unchanged, with a thin `src/index.ts` re-export barrel as the published entrypoint (mirrors the runtime engine's own re-export-barrel convention). `tools/generators/telemetry/generator-plan.json`'s `typescript.outputPath` and `spec/telemetry/project.json`'s codegen `outputs` repointed to the new path (Rust target untouched); `telemetry-spec:codegen` reproduces the file byte-identically at the new home (verified: zero diff after a full regen). `@tuvren/runtime` depends on the new package (`workspace:*`, added via `bun add` per repo CLI-first convention) and keeps re-exporting the same symbols from its own `src/index.ts` for public-API compatibility (the DoD only requires the sink to leave the engine's source tree, not that runtime's consumers change their import path); the two other internal consumers (`telemetry-secret-screening.ts`, `invocation-lifecycle.telemetry-depth.test.ts`) now import `@tuvren/telemetry-semconv` directly. `spec/telemetry/authority-packet.json`'s `bindingProjections.typescript`, `forbiddenAuthoritySources`, `generatedArtifacts`, and `verificationPaths`/`freshnessChecks` targets recomputed from `typescript/runtime` to `typescript/telemetry/semconv`.

`@tuvren/telemetry-otel` (npm name unchanged, Nx project name `framework-telemetry-otel` KEPT per the locked decision) moved `boundaries/framework/implementations/typescript/telemetry-otel` → `typescript/telemetry/otel` (5-deep → 3-deep; `tsconfig.json`/`tsconfig.lib.json` `extends` recomputed to `../../../tsconfig.base.json`, `tsconfig.lib.json`'s core reference and `tsconfig.typecheck.json`'s paths/rootDir recomputed to `../../core/...` and `../../..` respectively — the same 3-deep arithmetic as the `typescript/streaming/*` precedent). No external consumers existed beyond `tsconfig.base.json`, `portability-check.ts`, and `verify.ts`'s three curated lists (all path-only edits; the Nx name itself needed no edit anywhere). Both packages gained M1.2-pattern `BUILD.bazel` shims (otel never had one before this move) and joined the MODULE.bazel bun:test census sentence. `package.json` workspaces gained `typescript/telemetry/*`; the `boundaries/*/implementations/*/*` glob stays — it still matches exactly one package, `boundaries/hosts/implementations/typescript/repl`, until M9. `portability-check.ts` gained both packages' `packageRoot` entries (Bun-and-Node validated classification, matching the sibling `stream-*`/`telemetry-otel` pattern). `bun install` relinked all workspace symlinks at the new depths (verified via `readlink -f`); `find node_modules -xtype l` found no dangling telemetry-scoped links post-move (the pre-existing `driver-react`/`driver-api` dangling links elsewhere are unrelated legacy drift, left untouched). `bun run nx reset` run before trusting the graph. Validation: `telemetry-spec:codegen` byte-clean at the new location; `telemetry-semconv` build/typecheck/test/exports-smoke green (1 unit test, 21 assertions, plus an exports-smoke); `framework-telemetry-otel` build/typecheck/test/exports-smoke green (3 unit tests, 1 exports-smoke — the earlier `../../` extends-depth typo was caught by tsup's own config-resolution warning and fixed before the typecheck run, which failed loud with TS5083 until corrected); `framework-runtime:typecheck` green (proves the rewire); `validate-plans.ts` 32/32; `verify.ts`'s workspace test-lane coverage gate updated (`telemetry-semconv` added to `WORKSPACE_TEST_PROJECTS`/`WORKSPACE_BUILD_PROJECTS`/`WORKSPACE_EXPORT_SMOKE_PROJECTS` since the new package declares a `test` target).

**M8.1b addendum — the three tty-driver packages move to the idiomatic tree (npm + Nx names unchanged).** `stream-core|sse|agui` → `typescript/streaming/{core,sse,agui}` (dir names drop the `stream-` prefix, M6.2 runners/react precedent). Resolver script rewrote 27 files for the 4-deep→2-deep depth change (incl. 6 external consumers repl/bridge-ai-sdk/react/runtime tsconfigs + the sse packet bindingProjections/forbiddenAuthoritySources + portability-check packageRoots — the script caught those as standalone quoted strings). Hand-fix classes: project.json mid-string command args ×6, backtick markdown paths ×2 (the M8.1a-discovered class, applied). 3 new M1.2 BUILD.bazel shims + MODULE.bazel census sentence; workspaces gained `typescript/streaming/*` (boundaries impl glob retained for telemetry-otel until M8.2b). Link hygiene: 9 dangling consumer symlinks (certification, react, repl) removed + bun install re-link — the M6.2 class, now a standing post-move step. Validation: 7/23/9 package tests + 3 exports-smokes green, certification 463/463, packets green, `bun run check` fully green, bazel builds of all three shims green (16 actions). Zero leftover old-path references.

**M8.1c addendum — `@tuvren/event-stream` shim retired (M5.1b/M6.1c pattern; successor `@tuvren/core/events`, import-dead at deletion).** Test classification BEFORE deletion (the M6.1c lesson, applied): of the shim suite 2 tests, the guard-vocabulary test was fully duplicated in the runtime-api suites (not ported); the JSON-Schema-artifacts-vs-fixture conformance test was UNIQUE coverage — ported to `typescript/core/test/events-json-schema.test.ts` (relative-import convention; green standalone + in shared-core:test 22/22). Registrations pruned: tsconfig.base alias, root tsconfig reference, 3 verify.ts curated entries, portability-check entry, the 2 unused external package.json deps (testkit, certification — verified unimported first), `@tuvren/event-stream` tsconfig path/references/include entries across 12 files in 4 packages (streaming ×3 + testkit), core packet forbiddenAuthoritySources entry, and the DEAD `tuvren.framework.event-stream` validate-plans map key (M8.1a review carry — no plan declares that packetId; M6.1c convention). 6 consumers had dangling node_modules symlinks removed (bun install does not auto-clean nested workspace links — standing class). spec/streaming README + bindings appendix flipped to past-tense/successor references. Contract root `boundaries/framework/contracts/event-stream/` fully removed. Validation: plans 32/32 (operation validation still real), packets green, shared-core 22/22, `bun run check` fully green.

**M8.1a addendum — spec/streaming lift (byte-pure moves, identities unchanged).** Both streaming authority trees lifted: plain event-stream (typespec/bindings/artifacts — NO packet of its own; the old contract-root README claim that `spec/authority-packet.json` was the authority was STALE-FALSE, that file never existed; plain event-stream is `tuvren.shared.core`-owned, corrected in the merged `spec/streaming/README.md`) and event-stream-sse (the real packet + typespec + bindings + artifacts → `spec/streaming/sse/`, sub-surface nesting per the mcp/react precedent). Conformance family → `spec/conformance/streaming/{plans,fixtures,scenarios}` (3 plans, 2 fixtures, 1 scenario). 51 artifacts byte-verified pre/post; `streaming-spec` Nx project owns both codegens (compiling `./typespec` + `./sse/typespec`; sse contract-root project.json deleted, shim project.json kept minus its codegen target; root codegen list swaps `framework-event-stream,framework-event-stream-sse` → `streaming-spec`); byte-clean regeneration proven uncached. Hand-fix classes re-confirmed: tspconfig `{project-root}` depth ×2, the freeze-gate compound `"a; b"` string, the testkit SECOND (dist-depth) fixture probe, and a NEW class — backtick markdown paths in bindings appendices (the quote-only STRING_RE cannot see backticked paths; add to every future lift sweep). Portability pair → manifestVersion 0.6.0 (sse packetPath + 3 requiredAuthoritativeSources + MD mentions). Certification inputs gained `spec/conformance/streaming/**/*` (boundaries glob retained for the telemetry family until M8.2). Validation: plans 32/32, packets green, testkit 2/2 + all three stream suites green (both probe depths proven), certification 463/463 applicable, `bun run check` fully green.

**M7 addendum — the extensions port, authored from existing material only (no code move, no new packet, no new conformance).** Measured shape (Sonnet survey, 2026-07-03): the extension surface = a 38-line core barrel (`typescript/core/src/extensions/index.ts`, re-exporting 16 vocabulary symbols from `runtime-contract-shapes.ts`; ~30+ consumer files), the 433-line hook execution engine (`typescript/runtime/src/lib/extension-runtime.ts`, 19 engine-internal importers), and the 47-line REPL proof shim. **Zero TypeSpec models back the extension VOCABULARY** (`TuvrenExtension`, `ExtensionContext`, the lifecycle handler types) — the review scoped this precisely: the runner-owned `RunnerExtensionStateUpdate` model in `spec/runners/typespec/main.tsp` DOES model extension-emitted state, and its living in the react-runner packet reinforces the disposition below; the `extensions` bindingSection in `spec/core/authority-packet.json` is a named slot with no neutral models behind it, a structural asymmetry from the tools/providers/runners ports (which had real TypeSpec to lift); so M7 physically produces only the port README upgrade (`spec/extensions/README.md` stub → port-authored form). Packet disposition (NO new packet), grounded: the freeze-gate's §9 routing already sends hook-ordering claims to `EVIDENCE.reactRunner` (KRT-AF003 pending-promotion) and classifies the rest of §9 implementation-defined; all extension-behavior conformance that exists lives in `react-runner-extended.json` check families; the portability gate's closed-state `expectedPackets` (9 packets, v0.5.0) contains no extensions entry and no gate/doc references one; CLAUDE.md's "add a packet when a surface lacks one" is satisfied because the surface does NOT lack one — vocabulary is `tuvren.shared.core`-owned (bindingSection) and behavior is `tuvren.framework.react-runner`-owned. Row 135's pre-M2.1 `spec/extensions/` destination re-derived (code stays at `typescript/core`, §5 rule); rows 136-137 deferred hashes captured (`7a80bc74`, `e5bdc1cb`); proof-extension.ts row stays pending for its M9 physical move only. "Conformance list updated; green" is satisfied trivially — no new lane exists to register — stated explicitly rather than silently skipped.

**M6.4d addendum — the docs half of the rename (final M6.4 quarter).** `docs/KrakenFrameworkSpecification.md` fully swept (~90 hits → 0): headings `## 0. Driver Framing` → `Runner Framing` and `### 5.6 Driver Contract` → `Runner Contract`, all backticked identifiers corrected to code truth (`driverId`→`runnerId`, `DriverRuntimePort`/`DriverHandoffPort`/`DriverExecutionContext`/`DriverResumeContext`/`DriverExtensionStateUpdate`/`DriverExecutionResult` → Runner forms, `EventSource.driver?`→`runner?`, pseudocode), all prose; `docs/KrakenFrameworkDesignRationale.md:19` "execution driver" → "execution runner". Freeze-gate couplings updated in the same change: the ONE functional string (`text.includes("future drivers")` → `"future runners"` — a substring match against live doc prose that would have failed the gate if either side renamed alone) + 4 cosmetic label/rationale strings; the gate re-derives section routing from numeric sectionKeys, so heading renames were structurally safe (measured by the M6.4d survey). React packet `humanAuthorityRefs[0]` docs anchor `#56-driver-contract` → `#56-runner-contract` — **load-bearing, not cosmetic**: `validate-authority-packets.ts` resolves anchors LIVE against the target file's parsed headings (worker-verified at `validateHumanAuthorityRefs`/`markdownAnchorExists`); `humanAuthorityRefs[1]` (`.constitution/…#46-driver-runtime-contract`) stays byte-identical — it keeps validating because the frozen constitution file itself is untouched until M10. `spec/runners/README.md` corrected to on-disk reality (packetId react-runner, `runner` binding section, renamed plan/scenario filenames, the now-false "deliberately retain driver-era names" caveat replaced with the landed-at-M6.4b statement); `bindings/typescript.md` + `bindings/rust.md` prose corrected (rust.md's reference to the never-existing `tuvren.framework.driver-api` packet repointed at the real authority: `tuvren.shared.core`'s `runner` binding section); `AGENTS.md:84` + `spec/interop/proto/README.md:12` swept. Deliberate historical stays: README provenance mentions of the old `driver-api`/`react-driver` boundary roots and the `@tuvren/driver-api` shim-retirement note in typescript.md; "bus-driver" (resource-adapter sense) in MODULE.bazel + spec/providers/README + spec/tools/README. Generated epic-ad (4 files) + epic-af (2 files) reports regenerated through their lanes. Validation: freeze-gate 264 claims generate+check green, AF gap plan green, `bun run check` fully green incl. authority-packet live-anchor validation.

**M6.4c addendum — the semconv telemetry attribute follows the rename.** `tuvren.runtime.driver.id` → `tuvren.runtime.runner.id` in the owned source `telemetry/semconv/tuvren-runtime.yaml` (id + brief); regenerated through `telemetry-semconv:codegen` (all four outputs: `telemetry/semantic-conventions.md`, `telemetry/otel-attributes.json`, generated TS `typescript/runtime/src/lib/generated/tuvren-runtime-telemetry.ts`, generated Rust `rust/kernel/src/generated/tuvren_runtime_telemetry.rs`); hand-edited emit/consumer sites: engine emit `runtime-core-telemetry.ts:454`, otel-sink test (3 assertions), `repl-scenarios-support.ts:224`. Kernel-interop evidence regenerated through the sanctioned `compatibility:evidence` lane (which runs the live interop smoke): 21 attribute occurrences now `runner.id`, zero old. Riding along: `spec/core/artifacts/json-schema/{ErrorEnvelope,ExecutionBounds}.json` description-prose regens — **the M6.4b review's single P1**: M6.4b renamed the describing `@doc` prose in `spec/core/typespec/main.tsp` but did not commit the artifact regen (only `bun run check` was run, which does not execute codegen; the reviewer proved the drift by rerunning the exact core codegen command). Resolved through the sanctioned codegen lane, not hand-edits — but in TWO commits: the reviewer's working-tree cleanup silently reverted the orchestrator's uncommitted regens, so `bd5ebbe6` claimed the fix while shipping without it; caught immediately post-push by re-checking the committed stat, regenerated again, landed as `3a31287f`. Third commit-content incident (M6.1d scope leak, M6.4a message overclaim, this): the standing 5e guard now extends to verifying the CLAIMED CONTENT is in `git diff --cached`, not just the file list — and reviewers run in the shared working tree, so any uncommitted work must be committed or stashed before a review that may regenerate/revert files. Standing lesson: every TypeSpec prose touch needs its artifact regen committed in the same change — `check` will not catch it, only `codegen`/`verify`'s worktree-purity guard will. Also folded from the M6.4b review flags: `.repomixignore:57` stale `orchestration-runtime-driver-helpers.ts` glob → `-runner-helpers.ts` (M6.4a rename); `rust/conformance-adapter/src/main.rs:107` not-implemented message "ReAct Driver" → "ReAct Runner" (cargo check green); engine-scenario flavor text `"driver should not run"` → `"runner should not run"` (`spec/conformance/engine/scenarios/runtime-api-scenarios.json:378`, not asserted by any check — evidence regenerated clean). MODULE.bazel:23 "MCP bus-driver" left deliberately: bus-driver is the resource-adapter sense, not the execution-model concept (same ruling as mcp-client at M6.4b). Validation: `bun run check` fully green including the Rust workspace lint/test gates on the regenerated Rust file; interop-smoke lane green (3 projects); full evidence fleet zero `status:"fail"`.

**M6.4b addendum — the conformance-identity half of the driver→runner rename (the issue-mandated semantic churn; every identity renamed per the M6.0 minimal-churn scheme, evidence regenerated through gate runs only).** Identity map landed: packetId `tuvren.framework.react-driver` → `tuvren.framework.react-runner`; planIds `tuvren.framework.driver-api.{core,extended}` → `…runner-api.{core,extended}` (packet stays `tuvren.shared.core`) + `…react-driver.{callables,extended}` → `…react-runner.{callables,extended}`; all driver-bearing checkIds → `runner-api{,-ext,-af}.*` / `react-runner-{callable,ext,af}.*` + engine-plan checkIds; capabilities `framework.{driver-api,react-driver}` → `framework.{runner-api,react-runner}` (adapter.json + freeze-gate EVIDENCE keys `driverApi`/`reactDriver` → `runnerApi`/`reactRunner`); operations `driver.{execute,resume,checkpoint}` → `runner.*` (TypeSpec op literals + adapter dispatch + plans); plan/scenario files renamed (`runner-api-core`, `runner-api-extended`, `react-runner-callables`, `react-runner-extended`, `runner-api-scenarios`); TypeSpec `Driver*` models → `Runner*` + namespace `Tuvren.Framework.RunnerApi` + schema base URL `…/runner-api/` (9 Driver-named artifacts deleted, 9 Runner-named regenerated via `runners-spec:codegen`); result-shape keys `driver:{}` → `runner:{}` + plan-asserted fields (`driverIdInherited`→`runnerIdInherited`, `driverObservedMessageCount`→`runnerObservedMessageCount`, `driverExecuteCalls`→`runnerExecuteCalls`, `$.driver.*`→`$.runner.*`); error codes `invalid_driver_result`→`invalid_runner_result`, `driver_error`→`runner_error`, `react_driver_*`→`react_runner_*` (12 codes; cross-package consumer repl-live-output.ts + authority-guardrails regex followed), `driver_resume_*`→`runner_resume_*`; public API `CreateTuvrenOptions.driver`→`.runner` (the M6.4a commit-message overclaim corrected here), `driverRegistry`→`runnerRegistry`, request field `driverId`→`runnerId`, event key `driver:`→`runner:` + **event-stream authority followed its implementation**: `EventSource.driver`→`.runner` in the M8-owned TypeSpec + regenerated `EventSource.json` (M6 owns the vocabulary even on surfaces M8 moves); custom event name `driver.executed`→`runner.executed` (stream-core + fixture + tests); spec/core packet bindingSection `driver`→`runner`; react packet surface/bindingProjections renamed (humanAuthorityRefs frozen until M6.4d/M10); portability pair bumped to manifestVersion 0.5.0. **Five new rename-evasion classes found by orchestrator validation (recorded in working notes):** (1) plan `evidence`/`requiredEvidence` NAME arrays are identity strings checkId seds miss — detected only by counting `status:"fail"` in regenerated evidence (25 failures; the `--allow-failing-evidence` lane exits 0 — never trust its exit code); (2) fixture-text equality — assertion `equals` literals + scenario message texts must co-rename with code-side literals (the adapter's renamed hook-guard literal silently stopped matching the fixture's old text); (3) scenario-fixture top-level keys + `$.scenarioPath` refs are a paired class (8 keys + 34 refs); (4) authority-follows-implementation (the EventSource case above); (5) validator self-staleness — `validate-plans.ts`'s OPERATION_LITERAL_PATTERN hardcoded `driver` in regex position, so post-rename its declared-operations set went empty and the `size > 0` guard silently skipped ALL operation validation while reporting green (fixed to `runner`; also `epic-af-conformance-gap-plan.ts`'s own requiredEvidence declaration). Worker sweeps must cover `test/`/`bench/` trees explicitly: ~640 residual lowercase tokens lived in `typescript/runtime/test/` (test-local runner IDs `test-driver`→`test-runner`, `driver-tuvren-*`→`runner-tuvren-*`, `ba00x-*-driver`→`…-runner`, locals, engine guard messages "drivers must not …"→"runners must not …" with their test assertions). Deliberate stays: `tuvren.runtime.driver.id` semconv attribute everywhere incl. generated TS/Rust + kernel-interop evidence (M6.4c); docs + `spec/runners/{README.md,bindings/*.md}` prose + freeze-gate doc-citation strings (M6.4d); `.constitution` normative chain (M10); `migrate-to-core.ts` historical codemod literals (renaming would falsify the record — M10.1 disposition). Validation: `bun run check` fully green (authority gates + affected 40-project lane + rust gates), full 8-project conformance fleet green, evidence fleet regenerated with zero `status:"fail"` across all 8 files and zero stale codes, validate-plans 32/32 with operation validation restored.

**M6.4a addendum — the code-vocabulary half of the driver→runner rename (identifiers only; conformance identities untouched until M6.4b).** `@tuvren/core`'s execution-model surface renamed: subpath `./driver` → `./runner` (package exports + tsconfig.base alias + 86 importer files), `src/driver/` → `src/runner/`, lib files `driver-contract-{shapes,guards}.ts`/`driver-contracts.ts` → `runner-contract-*`, all capital-D `Driver*` exported symbols → `Runner*` (incl. `RuntimeDriver`→`RuntimeRunner`, `DriverRegistry`→`RunnerRegistry`, `DriverAttributedEventSource` in events); engine files `driver-registry.ts`/`runtime-core-driver{,-support}.ts` → runner forms + public API (`DriverKind`→`RunnerKind`, `createDriverRegistry`→`createRunnerRegistry`, `CreateTuvrenOptions.driver`→`.runner`); adapter module `framework-adapter-driver.ts` → `framework-adapter-runner.ts`; 4 driver-named test files renamed. Renamed as part of the capital-D sweep: `activeDriverId` → `activeRunnerId` (engine-internal loop state, never plan-asserted — safe here). DELIBERATELY untouched (M6.4b/c): the plan-asserted lowercase result fields (`driverIdInherited`, `driverObservedMessageCount`, `driverExecuteCalls`, `$.driver.phase`), the public request field `driverId`, all packet/plan/check IDs, capabilities, `driver.*` operation strings, TypeSpec models/artifacts, and the `tuvren.runtime.driver.id` semconv attribute. New evasion classes found: (1) bare sibling import specifiers (`./driver-registry.js`) escape `lib/…`-prefixed seds; (2) **tsup.config.ts entry arrays** (`src/driver/index.ts`) escape path seds AND the failure is masked by Nx build-cache replay — host-repl integration tests were the only lane that caught the missing `dist/runner/index.js`; fixed + rebuilt uncached; (3) a stale **materialized copy** (not symlink) of core under `typescript/runtime/node_modules/@tuvren/core` shadowed the workspace link (removed; bun install re-linked) plus a dangling `driver-react` symlink from M6.2 cleaned. M6.3-review fold-ins: `docs/perf-benchmarks.md:50` stale `providers-mcp-client` → `mcp-client` (P2) and the symmetric dead `rust/kernel-conformance-adapter` input glob dropped from `typescript/kernel/certification` (P3). Validation: affected typecheck/test/lint green (repl 127/127 after the dist fix), framework certification lane green, `bun run check` fully green.

**M6.3 addendum — the conformance machinery is renamed off "runner" (issue §14: "runner" is reserved for execution models; the machinery is the certification harness).** Rename map, all token-level (identity semantics unchanged — packet/plan/check IDs untouched, they are M6.4): shared engine `tools/conformance/runner/` → `tools/conformance/harness/` (8 files; meta-conformance's relative import follows); the 8 wrapper dirs swap the `conformance-runner` token for `certification` (`typescript/certification{,-batteries-included}`, `typescript/kernel/certification{,-sqlite,-postgres}`, `typescript/providers/certification`, `rust/certification`, `rust/kernel-certification`) and their Nx project IDs likewise (`framework-typescript-certification`, `framework-batteries-included-certification`, `framework-rust-certification`, `kernel-typescript-certification`, `kernel-typescript-{sqlite,postgres}-certification`, `kernel-rust-certification`, `providers-typescript-certification`); discovery tag `layer:conformance-runner` → `layer:certification` (gate constant + all 8 project.json + manifest description); `certified-projects.json` re-listed; rust crate names `tuvren-{framework,kernel}-rust-certification` (Cargo.lock regenerated; MODULE.bazel.lock re-pinned via a bazel build); `tools/scripts/lib/conformance-runner.ts` → `lib/conformance-suite-manifest.ts` (renamed BEFORE the global sed so the token sweep could not mangle it); evidence filename template → `certification-harness.<project>.json` and all 8 evidence files regenerated through the sanctioned `compatibility:evidence` lane (old `shared-conformance-runner.*` files git-rm'd; contents' `project` fields carry the new IDs from the same run, never hand-patched); unhyphenated "conformance runner" prose swept across verify/check step ids ("certification harness meta-conformance", "Rust kernel certification"), adapter comments, test describe labels, AGENTS.md, nx.json, MODULE.bazel, services-up.sh. Fold-ins landed with the batch: Nx `providers-mcp-client` → `mcp-client` (project.json, BUILD.bazel NX_TARGETs, verify.ts; spec plan filename `providers-mcp-client.json` deliberately NOT renamed — packet-referenced, M5 identity locks hold) and `mcp-client` joined `WORKSPACE_EXPORT_SMOKE_PROJECTS` (M5.2 curation gap closed); the two dead `{workspaceRoot}/rust/conformance-adapter/**/*` input globs dropped from both TS certification project.jsons (M3.3b carry). Ops lesson recorded in working notes (5f): project-dir renames leave the Nx daemon graph stale — run-many reports "No tasks were run" while the disk-reading discovery gate stays green; `bun run nx reset` is mandatory after such renames, then rerun affected lanes. Validation: discovery parity 8/8, full fleet green (providers lane flaked once under run-many, standing-flaky class, green individually 60/60 and cached-green on rerun), `bun run check` green on the fresh graph.

**M3.0 addendum (rules_js adoption REVERTED — user decision 2026-07-02, via the plan's pre-authorized halt-and-consult)** — The research spike measured: rules_js v3.2.2 / rules_ts v3.8.11 current (2026-06-11); `npm_translate_lock` consumes only pnpm lockfiles; `pnpm import` supports package-lock.json/npm-shrinkwrap.json/yarn.lock but **not `bun.lock`**, and the yarn/npm import paths do not support workspaces; a mirrored `pnpm-workspace.yaml` + `pnpm install --lockfile-only` was proven workable in a throwaway worktree (6,796-line lockfile, ~6s, requires `--config.package-manager-strict=false`) but shows **measured resolution divergence** vs `bun.lock` (bun preserves zod 4.3.6 + 4.4.3; pnpm resolves fresh to 4.4.3 only); decisively, 32 of the 39 `typescript/` test files import `bun:test` — including all 16 test files across the 5 Bazel-shimmed packages (`backend-sqlite`'s 7-file `node --test` suite is the exception, and it is Nx-only per M1.6 anyway) — which node cannot execute, and rules_js has **no bun runtime** (FR aspect-build/rules_js#1258 open since 2023-09, experimental branches only, no mature community ruleset — `dukdog/rules_bun` is an untouched template). User chose **revert to the M1.2 shim strategy** for the remainder of the epic (over hybrid build/typecheck-only hermeticity, an in-repo custom bun toolchain, or rewriting tests off `bun:test`); real ruleset adoption is a post-epic follow-up gated on upstream bun support. This supersedes the M2.5 addendum's forward-looking sentence "M3.0 converts these to real `aspect_rules_js`/`rules_ts` targets": no conversion happens; the M1.2-pattern shims (`typescript/{core,core-types,sdk,kernel/protocol,kernel/runtime}`) are the epic-long Bazel posture for TS, and the M1.6 scope record ("memory/sqlite backends and every conformance-adapter/runner package remain Nx-only") stands epic-long rather than "for now". Recorded in `MODULE.bazel`'s module docstring and via `gh issue comment 87` (amendment-update comment).

**M3.1 addendum (workspace test-lane coverage gate)** — Landing the M2.5 review's P2 durably: `verify`'s curated `WORKSPACE_TEST_PROJECTS` ran no lane containing the unit tests of 8 TS projects (`shared-core`, `shared-core-types`, `sdk`, `kernel-contract-protocol`, `kernel-runtime`, `backend-memory`, `backend-sqlite`, `providers-mcp-client`) — enumerated by walking every `project.json` with a `test` target. All 8 are now registered in `WORKSPACE_TEST_PROJECTS`, and a new parity gate `tools/scripts/validate-workspace-test-coverage.ts` (M2.9's discovery pattern, sharing the extracted `tools/scripts/lib/nx-projects.ts` walker with the certification gate) enforces both directions: every test-target project must be registered or carry a documented exclusion (currently the 4 cargo projects, whose tests run in verify's `cargo test --workspace` step — the gate asserts each excluded project's test command still invokes cargo so the exclusion cannot outlive its justification), and every registered/excluded name must still resolve to a real test-target project. Wired as verify authority step `workspace test-lane coverage` and into `check`'s inner-loop subset. Coverage strictly increases (safe direction for the M0.2 equivalence oracle); the added projects' suites are all sub-3s.
## Baseline (M0.2)

Captured on branch `feat/epic-87-sovereign-polyglot-framework` at commit `c910a0dfb20083e881211285ffd3f06296a1ab23`, 2026-07-01T07:26:55Z, after `bun run services:up` (devenv-managed Postgres) followed by `bun run verify` (the full phased release gate defined in `tools/scripts/verify.ts`).

**Result: green. Exit code 0.** All 26 phases passed:

| Phase | Result |
|---|---|
| workspace lint | pass (1.8s) |
| Rust workspace formatting | pass (0.8s) |
| docs-to-authority freeze gate | pass (0.2s) |
| Epic AL portability gate | pass (0.2s) |
| Epic AF conformance gap plan freshness | pass (0.1s) |
| authority packet validation | pass (0.4s) |
| conformance plan validation | pass (2.1s) |
| adapter protocol validation | pass (0.4s) |
| shared conformance runner meta-conformance | pass (0.7s) |
| vocabulary-check verification | pass (0.4s) |
| machine authority guardrails | pass (6.3s) |
| Rust workspace lint | pass (2.9s) |
| Rust workspace tests | pass (3.4s) |
| Rust kernel conformance runner | pass (2.1s) |
| Rust kernel gRPC interop smoke | pass (3.8s) |
| telemetry, compatibility, and interop code generation | pass (4.6s) |
| kernel interop governance smoke | pass (3.8s) |
| workspace typecheck | pass (0.7s) |
| transition-line targeted builds | pass (0.9s) |
| transition-line targeted tests | pass (0.8s) |
| boundary-owned conformance suites (all 8 `conformance` project IDs) | pass (2.1s) |
| cross-language proving-host interactive/headless interop smoke | pass (76.6s) |
| package export smoke tests | pass (1.3s) |
| Bun and Node portability import checks | pass (3.8s) |
| Node-backed proving-host SQLite interactive/headless scenario | pass (2.3s) |
| PostgreSQL-backed proving-host interactive/headless scenario | pass (17.3s) |

This is the equivalence baseline every subsequent milestone's re-certification is measured against: any milestone that leaves the tree unable to reproduce this same all-green result (modulo the physical paths it deliberately moved) is not done.

## M1 close (M1.7) — equivalence proof against the M0.2 baseline

M1 ("Tracer bullet — kernel") is closed. All of its DoD bullets are met:

- `spec/kernel` + `spec/conformance/kernel` exist and are authoritative (M1.3).
- The Rust kernel (`tuvren-kernel-rust`, now `rust/kernel`) and the TypeScript kernel line (`@tuvren/kernel-protocol` at `typescript/kernel/protocol`, `@tuvren/kernel-runtime` at `typescript/kernel/runtime`) build and certify under Bazel (`rust/kernel/BUILD.bazel`, `typescript/kernel/protocol/BUILD.bazel`, `typescript/kernel/runtime/BUILD.bazel` — proven in M1.1/M1.2, re-proven live at every subsequent sub-commit's review gate through M1.6).
- The IPC (gRPC) kernel transport is mapped to `spec/interop/`, with its generated-bindings config drift (stale `@tuvren/runtime-core` references vs. the real `@tuvren/runtime` package) reconciled (M1.5). The literal "generated bindings relocate to the `typescript/` generated area" end-state is deferred until `@tuvren/runtime` itself moves in M3 — see M1.5's own inventory row and Multi-home §A for why treating this as M3-gated, not M1-blocking, is the correct reading.
- The stateful PostgreSQL conformance lane's reconciliation with Bazel hermeticity is decided: it stays Nx-driven, outside the hermetic graph, permanently for this epic, per invariant §10.5's sanctioned exception (M1.6, see Multi-home §B).
- Old and new paths produce equivalent results: **proven below.** Inventory updated: every kernel-section row above is `migrated`, none remain `pending`.

**Equivalence proof.** Re-ran `bun run services:up` (idempotent, no-op — already running) followed by `bun run verify` at commit `131d2db61963d7bf7f583b9c7ab89c3a35922728` (M1.6, HEAD immediately before this closing commit). First run surfaced a real (if purely cosmetic) regression: `workspace lint` failed on 4 files last touched by M1.3 (`tools/conformance/plan-compiler/index.ts`, `tools/conformance/vocabulary/validate-vocabulary.ts`, `tools/scripts/authority-packet/validate-authority-packets.ts`, `tools/scripts/docs-authority-freeze-gate.ts`) — sed-based edits from M1.3's `SPEC_ROOT`-scanning additions had left syntactically valid but non-biome-formatted content (unwrapped-vs-wrapped line breaks) that no narrower `bun run check`/`bun run codegen` lint pass had caught, because those run scoped per-project lint targets rather than `verify`'s full-repo `bunx --bun @biomejs/biome check .`. Fixed via `bunx --bun @biomejs/biome check --write` on the 4 files; diff-reviewed and confirmed purely cosmetic (line-wrap changes only, zero logic/string-value changes). Re-ran `bun run verify` clean:

**Result: green. Exit code 0.** All 26 phases passed — the identical phase set and count as the M0.2 baseline, modulo the physical paths M1 deliberately moved (kernel authority/conformance into `spec/`, kernel adapters into `rust/kernel*` and `typescript/kernel/*`, the gRPC interop contract into `spec/interop/`) and modulo timing variance. No phase was skipped, added, or renamed. This is the equivalence proof M1's own DoD requires.

**Decision recorded at M1's close (folded in from M1.6):** the PostgreSQL kernel backend and its conformance runner (`typescript/kernel/backends/postgres`, `typescript/kernel/conformance-runner-postgres`) stay permanently Nx-driven, outside Bazel's hermetic graph, for the reasons detailed in Multi-home §B and `MODULE.bazel`'s module docstring. This is the landing of invariant §10.5's "until M1's hermeticity decision lands" grace period.
